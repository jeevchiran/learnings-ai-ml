import{r as w,j as e,u as g,B as f,R as u,C as v,Q as b,M as y}from"./index-WYRBR6CI.js";import{P as m}from"./PredictReveal-DnF7AdIF.js";import{A as j,S as A}from"./ui-B8T98xz3.js";const x="#a21caf",n=8,o=[],l=[];for(let i=0;i<n;i++){o.push([]),l.push([]);for(let t=0;t<n;t++)o[i].push(t===3||t===4?1:0),l[i].push(t===i?1:0)}function c({data:i,label:t}){return e.jsxs("div",{style:{textAlign:"center"},children:[e.jsx("svg",{width:n*16,height:n*16,role:"img","aria-label":t,children:i.map((a,s)=>a.map((d,r)=>e.jsx("rect",{x:r*16,y:s*16,width:16,height:16,fill:x,opacity:d,stroke:"var(--border)",strokeWidth:.3},s+"-"+r)))}),e.jsx("div",{style:{fontSize:"0.72rem",color:"var(--text-muted)",marginTop:3},children:t})]})}function k(){const[i,t]=w.useState(.5),h=o.map((a,s)=>a.map((d,r)=>d*(1-i)+l[s][r]*i));return e.jsx(j,{value:x,children:e.jsxs("div",{children:[e.jsx(A,{label:"mix between the two valid answers",value:i,onChange:t,min:0,max:1,step:.05,fmt:a=>a.toFixed(2),width:180}),e.jsxs("div",{style:{display:"flex",gap:"1.2rem",flexWrap:"wrap",marginTop:"0.7rem",alignItems:"flex-start"},children:[e.jsx(c,{data:o,label:"one valid answer"}),e.jsx(c,{data:l,label:"another valid answer"}),e.jsx(c,{data:h,label:"what squared error prefers"})]}),e.jsx("p",{style:{fontSize:"0.78rem",color:"var(--text-muted)",marginTop:"0.6rem",lineHeight:1.6},children:"Set the mix to 0.5. The third grid is not a third valid answer; it is a ghost of both. Squared error rewards it anyway, because the average of several plausible outputs always has a lower expected pixel-wise error than committing to any one of them. That is where the blur in reconstruction-based generators comes from, and an adversarial loss avoids it by asking a different question entirely: not how close is this to the target, but would anyone believe it."})]})})}function p(i){const t={div:"div",em:"em",h1:"h1",h2:"h2",hr:"hr",li:"li",p:"p",strong:"strong",ul:"ul",...g(),...i.components};return e.jsxs(e.Fragment,{children:[e.jsx(t.h1,{children:"Module 1: From Explicit to Implicit Density"}),`
`,e.jsx(f,{children:e.jsx(t.p,{children:"The VAE track ended by putting a name on what a VAE actually is: a likelihood-based generative model, trained by maximizing the ELBO — an explicit, if approximate, statement of how probable the data is under the model. GANs get to the same destination, generating new samples, by refusing to write that statement down at all."})}),`
`,e.jsx(t.h2,{children:"No density, just a game"}),`
`,e.jsx(m,{prompt:"A VAE's decoder defines an explicit p(x|z) and trains by maximizing a likelihood bound. A GAN's generator has no p(x) formula anywhere in it. So how does a GAN's generator know whether an output is any good?",options:["It still computes a likelihood, just implicitly, via a hidden formula","A second network is trained to tell real data from generated data, and that judgment is the only training signal"],correct:1,children:e.jsxs(t.p,{children:["There is no reconstruction loss, no KL term, no formula for p(x) at all. Instead, two networks are trained against each other: a ",e.jsx(t.strong,{children:"generator"})," G that turns noise into candidate data, and a ",e.jsx(t.strong,{children:"discriminator"})," D that tries to tell real examples apart from G's output. G's only feedback is whether it fooled D. That is an ",e.jsx(t.strong,{children:"implicit density model"})," — it can produce samples from a distribution it never writes down."]})}),`
`,e.jsx(u,{items:[{q:"What is the fundamental training signal for a GAN's generator, given that it has no explicit p(x)?",a:"Whether a second network, the discriminator, was fooled into classifying the generator's output as real. There is no likelihood term anywhere — the adversarial classification signal is the only feedback G receives."}]}),`
`,e.jsx(t.h2,{children:"Why not just minimize pixel distance to real images?"}),`
`,e.jsx(t.div,{className:"lesson-visual",role:"region","aria-label":"Blur Average interactive example; scroll horizontally if needed",tabIndex:"0",children:e.jsx(k,{})}),`
`,e.jsxs(m,{prompt:"Suppose instead we train G to directly minimize the average pixel-wise distance to real training images, like an autoencoder without an encoder. For an ambiguous input (say, a digit that could plausibly continue two different ways), what does that objective push G toward?",options:["A sharp, plausible completion matching one specific valid continuation","A blurry average of every plausible continuation"],correct:1,children:[e.jsxs(t.p,{children:["Minimizing expected pixel-wise distance is minimized, on average, by predicting the ",e.jsx(t.strong,{children:"mean"})," of all the plausible outputs — which for images means a blurry average, not any single sharp, realistic one. This is exactly the failure mode that motivated the VAE track's reconstruction-loss discussion, and it does not go away just because the model is unsupervised."]}),e.jsx(t.p,{children:'A discriminator sidesteps this. D does not ask "how close is this to the average real image?" — it asks "does this look like it could have come from the real data distribution at all?" A sharp but different-looking output can fool D just as well as a pixel-perfect match, so G is never pushed toward averaging away detail.'})]}),`
`,e.jsx(u,{items:[{q:"Why does minimizing average pixel-wise reconstruction error tend to produce blurry outputs for ambiguous inputs?",a:"Because the pointwise loss is minimized by the statistical mean of the plausible outputs, and averaging several sharp but different images produces a blurry result. An adversarial loss instead asks whether the output is plausible at all, so it does not reward hedging toward an average."}]}),`
`,e.jsx(t.hr,{}),`
`,e.jsx(v,{title:"TL;DR",children:e.jsxs(t.ul,{children:[`
`,e.jsxs(t.li,{children:["A GAN is an ",e.jsx(t.strong,{children:"implicit density model"}),": it can sample from a data distribution without ever writing down a formula for that distribution's probability."]}),`
`,e.jsxs(t.li,{children:["Training is a two-network game: a ",e.jsx(t.strong,{children:"generator"})," G maps noise to candidate data, a ",e.jsx(t.strong,{children:"discriminator"})," D classifies real vs. generated, and G's only feedback is whether it fooled D."]}),`
`,e.jsxs(t.li,{children:["A pixel-wise reconstruction loss is minimized by averaging over plausible outputs, which produces blur. An adversarial loss instead rewards ",e.jsx(t.em,{children:"plausibility"}),", so it does not push G toward the average."]}),`
`]})}),`
`,e.jsx(b,{question:"What distinguishes a GAN's generator from a VAE's decoder in terms of what each optimizes?",options:["The generator maximizes an explicit likelihood lower bound just like the decoder does","The generator has no explicit density formula at all — its only signal is whether it fooled a discriminator network","The generator minimizes pixel-wise reconstruction error against paired real images","The generator and decoder optimize identical objectives, just with different network architectures"],correct:1}),`
`,e.jsx(y,{question:"Which statements about implicit vs. explicit density generative models are correct?",options:["A GAN is called an implicit density model because it can sample without ever computing p(x)","A VAE writes down an explicit, if approximate, objective related to the likelihood of the data","Minimizing pixel-wise distance to real images is immune to the blurry-average failure mode","An adversarial loss rewards plausibility rather than closeness to an average of valid outputs","GANs and VAEs are both trained with the exact same loss function"],correct:[0,1,3]})]})}function E(i={}){const{wrapper:t}={...g(),...i.components};return t?e.jsx(t,{...i,children:e.jsx(p,{...i})}):p(i)}export{E as default};
