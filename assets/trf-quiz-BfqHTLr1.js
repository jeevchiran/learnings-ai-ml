import{u as s,j as e,C as r,Q as t,M as a}from"./index-Ba5-wm3B.js";function i(n){const o={h1:"h1",h2:"h2",p:"p",...s(),...n.components};return e.jsxs(e.Fragment,{children:[e.jsx(o.h1,{children:"Track Quiz — Transformers"}),`
`,e.jsx(r,{title:"End-of-track checkpoint",children:e.jsx(o.p,{children:'This quiz covers the full track: the static embedding families (Word2Vec CBOW/skip-gram, GloVe, FastText) and their four limitations, self-attention vs cross-attention, encoder (unmasked) vs decoder (masked) self-attention plus cross-attention, positional encoding and why attention needs it, the Query/Key/Value math (scaled dot-product, causal masking via -infinity, the softmax weighted sum), multi-head attention and why it exists, the low-rank Value projection, PyTorch self-attention/multi-head implementation, the feed-forward layer and residual "Add & Norm" connections, sinusoidal vs learned positional encoding, training (parallel next-token loss, teacher forcing, learning-rate warmup), the three model families (BERT/GPT/T5, MLM vs CLM), fine-tuning BERT for classification, translation and question answering with pretrained models, and sentence transformers. Expect conceptual, numerical, and code-reading questions.'})}),`
`,e.jsx(o.h2,{children:"Questions"}),`
`,e.jsx(t,{question:"What is the key difference between self-attention and cross-attention?",options:["Self-attention uses Query/Key/Value and cross-attention does not","Self-attention is a sequence attending to itself; cross-attention is one sequence attending to a different sequence","They are the same mechanism, just different names for the same computation","Cross-attention is only used at inference time, never during training"],correct:1}),`
`,e.jsx(t,{question:"Why does the transformer encoder's self-attention use no causal mask, while the decoder's does?",options:["The encoder is a smaller network so masking is unnecessary","The encoder already has its entire input available with nothing to hide; the decoder generates left to right and must not see future positions","Masking is a training-only detail that applies equally to both","There is no encoder self-attention in a standard transformer"],correct:1}),`
`,e.jsx(t,{question:"Why does a transformer need positional encoding when an RNN does not?",options:["RNNs also need positional encoding, transformers do not","Self-attention compares all tokens to all other tokens with no inherent notion of order, unlike an RNN which reads tokens strictly in sequence","Positional encoding is only needed for very long sequences","It is purely a regularisation technique unrelated to order"],correct:1}),`
`,e.jsx(t,{question:"A causal mask sets a disallowed position's raw score to -infinity before softmax. Why -infinity rather than a large negative number like -1000?",options:["Any sufficiently negative number works identically in practice and -infinity is just convention","Only exactly -infinity guarantees e^score is exactly 0 after the exponential, giving that position exactly zero weight and zero gradient","PyTorch requires -infinity specifically for numerical stability reasons unrelated to the math","-infinity is used only for decoder self-attention, never in code"],correct:1}),`
`,e.jsx(t,{question:'Query vector q="creature" = [1.0, 0.0]. Key vector k="fluffy" = [1.0, 0.1]. What is their dot product q · k?',options:["1.0","1.1","0.1","0"],correct:0}),`
`,e.jsx(t,{question:"Why does scaled dot-product attention divide the raw score by sqrt(d_k)?",options:["To normalise the Key vectors to unit length","The dot product's variance grows with dimension d_k, and without scaling, high-dimensional scores can saturate softmax toward a near one-hot output","To reduce the number of learnable parameters","It has no mathematical purpose, it is a historical convention"],correct:1}),`
`,e.jsx(t,{question:"What does the Value vector represent, and how does it differ from Query and Key?",options:["Value is the same as Key, just renamed for clarity","Query and Key only compute a relevance weight; Value is a separate projection representing what a position actually contributes to the weighted output","Value vectors are only used when the causal mask is disabled","Value replaces the original embedding entirely once attention is applied"],correct:1}),`
`,e.jsx(t,{question:"Is a token's final embedding after attention a replacement of its original embedding, or an addition to it?",options:["A complete replacement — the original embedding is discarded","An addition — e_i' = e_i + Δe_i, where Δe_i is the weighted sum of attended Value vectors","Neither — attention only affects the loss, not the embeddings","It depends on whether causal masking is enabled"],correct:1}),`
`,e.jsx(t,{question:"Why does multi-head attention run several smaller attention computations instead of one larger one?",options:["It reduces total parameter count compared to a single head","Different heads can specialise in different kinds of relationships, which one attention pattern would have to compromise across","Multiple heads are required for backpropagation to work","It removes the need for a causal mask"],correct:1}),`
`,e.jsx(t,{question:"In a multi-head attention layer with embed_dim=512 and num_heads=8, what is head_dim, and what shape does concatenating all heads' outputs produce per token?",options:["head_dim=64; concatenated shape is 512, matching the original embedding dimension","head_dim=512; concatenated shape is 4096","head_dim=8; concatenated shape is 64","head_dim=64; concatenated shape is 64, since concatenation does not change size"],correct:0}),`
`,e.jsx(t,{question:"What does the feed-forward layer in a transformer block do that attention does not?",options:["It mixes information across all positions, same as attention","It processes each position's vector independently — no information is exchanged between positions in this layer","It computes the causal mask","It replaces the need for an embedding table"],correct:1}),`
`,e.jsx(t,{question:"Why can a transformer decoder train on every position's next-token prediction in parallel, unlike an RNN decoder's step-by-step training?",options:["Cross-entropy loss is inherently designed to be computed in parallel","The causal mask already guarantees no position's prediction is computed using information from the token it's graded against, so all positions can be graded in one forward pass","Parallel training is a myth — transformers also train one position at a time","Teacher forcing is not used in transformer training"],correct:1}),`
`,e.jsx(t,{question:"In cross-attention, where do queries come from and where do keys and values come from — and why that direction?",options:["Q from the encoder and K/V from the decoder, because the source drives the output","Q from the decoder and K/V from the encoder output, because the thing being generated is the thing asking what is relevant in the input","Q, K and V all come from the encoder output","The direction is arbitrary and either works equally well"],correct:1}),`
`,e.jsx(t,{question:"Why is it called CROSS-attention rather than just attention?",options:["Because the attention scores are computed twice and averaged","Because queries and keys/values are drawn from two different sequences, rather than all from one as in self-attention","Because it crosses over between layers rather than within a layer","Because the mask crosses out future positions"],correct:1}),`
`,e.jsx(t,{question:"Which Word2Vec training mode predicts the surrounding context words from a given target word, and produces stronger vectors for rare words?",options:["CBOW","Skip-gram","Both do this identically","Neither — that is what GloVe does"],correct:1}),`
`,e.jsx(t,{question:"What can FastText do that Word2Vec and GloVe cannot?",options:["Train without any text corpus at all","Produce a vector for a word it never saw during training, by summing its character n-gram vectors","Give the same word different vectors in different sentences","Capture sarcasm and negation at the sentence level"],correct:1}),`
`,e.jsx(t,{question:"BERT is trained with Masked Language Modelling and GPT with Causal Language Modelling. Why can BERT not simply be trained on GPT's next-token objective?",options:["BERT is too small to support next-token prediction","BERT's self-attention is bidirectional, so every position can already see the next token — the task would be trivial rather than instructive","Masked Language Modelling is strictly more powerful, so there is no reason to","Next-token prediction requires cross-attention, which BERT lacks"],correct:1}),`
`,e.jsx(t,{question:"When fine-tuning a pretrained BERT for classification, which parameters get updated?",options:["Only the newly added classification head; the encoder stays frozen","Both the classification head and the pretrained encoder weights, together","Only the encoder; the head is fixed at initialisation","Only the embedding table"],correct:1}),`
`,e.jsx(t,{question:"Why does a padded position need an attention mask during BERT fine-tuning?",options:["To make the sequences the same length","Without it, real tokens would attend to meaningless padding and softmax would spend probability mass on those positions","To convert the padding into the correct subword tokens","Padding needs no mask — it is ignored automatically"],correct:1}),`
`,e.jsx(t,{question:"T5 performs question answering. What does it actually output?",options:["The start and end token indices of the answer span within the context","The answer text, generated token by token as a string","A probability score for each token in the vocabulary, with no decoding step","A ranked list of candidate context passages"],correct:1}),`
`,e.jsx(t,{question:"A cross-encoder scores a sentence pair by running both sentences through the model together. Why is that impractical for finding the most similar pair among 10,000 sentences?",options:["Cross-encoders are less accurate than bi-encoders","It requires a full forward pass for every pair — roughly N² passes, which takes hours instead of seconds","Cross-encoders cannot process more than two sentences total","The embeddings cannot be compared with cosine similarity"],correct:1}),`
`,e.jsx(a,{question:"Which statements about the three transformer model families are correct?",options:["Encoder-only models (BERT) use fully bidirectional self-attention","Decoder-only models (GPT) enforce causal masking so a token sees only earlier tokens","Encoder-decoder models (T5) use bidirectional attention in the encoder and causal attention in the decoder","Encoder-only models are the natural choice for open-ended text generation","Encoder-decoder models add cross-attention that the other two families do not have","Decoder-only models are trained with Masked Language Modelling"],correct:[0,1,2,4]}),`
`,e.jsx(t,{question:"What does a decoder-only model give up relative to an encoder-decoder, and why is it tolerable?",options:["It cannot attend to the prompt at all; this is tolerable because prompts are short","Prompt tokens never get a bidirectional pass over each other, so disambiguation moves downstream into more attention hops — tolerable because generation happens after the whole prompt is read, so the information stays reachable","It cannot be trained in parallel across positions; this is tolerable because inference is sequential anyway","It gives up nothing — the design is strictly superior"],correct:1}),`
`,e.jsx(t,{question:"Which architecture fits an email spam filter, a translation app, and a general chatbot — in that order?",options:["Decoder-only, encoder-only, encoder-decoder","Encoder-only, encoder-decoder, decoder-only","Encoder-decoder, decoder-only, encoder-only","Encoder-only, decoder-only, encoder-decoder"],correct:1}),`
`,e.jsx(a,{question:"Which statements about Sentence Transformers are correct?",options:["Both sentences pass through encoders with identical, shared weights","Token-level embeddings are pooled into a single fixed vector per sentence","Each sentence must be re-encoded every time it is compared against a new one","Sentence embeddings can be precomputed and indexed ahead of time","Training uses similarity-based objectives that pull similar sentences together","Mean-pooling a vanilla pretrained BERT works just as well as a fine-tuned Sentence Transformer"],correct:[0,1,3,4]}),`
`,e.jsx(a,{question:"Which statements about the transformer architecture are correct?",options:["A decoder block contains both masked self-attention and cross-attention into the encoder","Positional encoding is added once before the first block, not re-added at every block","Residual connections add a sublayer's output back onto its input to keep gradients flowing through deep stacks","The encoder stack uses a causal mask, just like the decoder","Learned positional embeddings generalise to sequence lengths never seen during training","Sinusoidal positional encoding has the property that relative position offsets are expressible as a linear function"],correct:[0,1,2,5]}),`
`,e.jsx(a,{question:"Which statements about training a transformer are correct?",options:["The loss function is ordinary cross-entropy on next-token prediction","Query, Key, and Value matrices require a specialised gradient computation separate from backpropagation","Teacher forcing feeds the ground-truth shifted sequence as decoder input during training","Inference must generate tokens sequentially, one at a time","Learning-rate warmup is used to stabilise early training of deep attention stacks","Training and inference use identical parallel computation, with no difference between them"],correct:[0,2,3,4]})]})}function c(n={}){const{wrapper:o}={...s(),...n.components};return o?e.jsx(o,{...n,children:e.jsx(i,{...n})}):i(n)}export{c as default};
