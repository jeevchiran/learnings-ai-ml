import{u as r,j as e,C as i,Q as t,M as n}from"./index-VIW80S6x.js";function s(a){const o={h1:"h1",h2:"h2",p:"p",...r(),...a.components};return e.jsxs(e.Fragment,{children:[e.jsx(o.h1,{children:"Track Quiz — ETL with PySpark"}),`
`,e.jsx(i,{title:"End-of-track checkpoint",children:e.jsx(o.p,{children:`This quiz covers all 10 modules of the ETL with PySpark track: pipeline architecture and idempotency, in-memory processing and lazy evaluation, RDD operations, DataFrames vs Pandas, PySpark SQL, UDFs, data cleaning, exploration aggregations, transformation operations (joins, pivot, explode), and caching and broadcast strategies. Expect conceptual, numerical, code-reading, and "what's wrong" questions.`})}),`
`,e.jsx(o.h2,{children:"Questions"}),`
`,e.jsx(t,{question:"Which of the three ETL stages is responsible for ensuring data conforms to the target schema — renaming columns, casting types, and normalising formats?",options:["Extract — data is standardised as it is pulled from the source","Transform — raw data is reshaped and cleaned into the target structure","Load — the destination database enforces schema on write","Validate — a separate fourth stage handles schema conformance"],correct:1}),`
`,e.jsx(t,{question:"A dataset of 10 million rows is partitioned across 200 executors. How many rows does each executor process on average?",options:["5,000 rows","50,000 rows","500,000 rows","2,000,000 rows"],correct:1}),`
`,e.jsx(n,{question:"Which of the following are true about Spark's lazy evaluation model? (Select all that apply)",options:["Transformations such as filter() and map() are not executed when called — they build a DAG","An action such as collect() or count() triggers execution of the full lineage","Each transformation immediately shuffles data across the network","Lazy evaluation allows Spark to optimise and combine multiple operations before running","Calling show() on a DataFrame is considered a transformation, not an action"],correct:[0,1,3]}),`
`,e.jsx(t,{question:"What does the following PySpark code return?\\n\\ndf.groupBy('city').agg({'revenue': 'sum', 'orders': 'count'}).orderBy('sum(revenue)', ascending=False)",options:["A DataFrame with one row per city, showing total revenue and order count, sorted from lowest to highest revenue","A DataFrame with one row per city, showing total revenue and order count, sorted from highest to lowest revenue","A single row containing the grand total revenue and order count across all cities","An error — you cannot mix string aggregation syntax with orderBy on a computed column name"],correct:1}),`
`,e.jsx(n,{question:"Which statements correctly describe the difference between PySpark DataFrames and Pandas DataFrames? (Select all that apply)",options:["PySpark DataFrames are distributed across a cluster; Pandas DataFrames live in a single machine's memory","PySpark enforces a schema at the column level; Pandas infers types but does not enforce them","Calling toPandas() on a PySpark DataFrame collects all data to the driver node","PySpark show() prints the first n rows without collecting the full DataFrame to the driver","Pandas groupBy() triggers a distributed shuffle across worker nodes"],correct:[0,1,2,3]}),`
`,e.jsx(t,{question:"Which PySpark method registers a DataFrame as a temporary SQL view so that spark.sql() can query it by name?",options:['df.registerTable("view_name")','df.createOrReplaceTempView("view_name")','df.toSQL("view_name")','spark.registerView(df, "view_name")'],correct:1}),`
`,e.jsx(n,{question:"What are the performance costs of using a PySpark UDF instead of a built-in function? (Select all that apply)",options:["UDFs are executed row-by-row in Python, bypassing Spark's JVM-level optimiser (Catalyst)","Each row must be serialised from JVM memory, passed to a Python process, and deserialised back","UDFs cannot accept multiple column arguments","Spark cannot push predicates through a UDF, preventing filter push-down optimisations","UDFs automatically trigger a full dataset shuffle"],correct:[0,1,3]}),`
`,e.jsx(t,{question:"A data engineer runs df.dropDuplicates(['order_id', 'timestamp']) and then df.fillna({'discount': 0.0}). What is wrong with this sequence if the goal is to clean the data before analysis?",options:["dropDuplicates() should be called after fillna() — filling nulls first may cause rows with null discount to be incorrectly treated as distinct","fillna() cannot operate on float columns; it only works on string and integer types","dropDuplicates() with a subset of columns will silently drop rows that have the same order_id and timestamp but different values in other columns — this may be intentional but should be verified","You must call cache() between the two operations or Spark will recompute the lineage twice"],correct:2}),`
`,e.jsx(t,{question:"Which PySpark function is used to break a column containing an array (e.g. a list of product tags) into one row per array element?",options:["split()","flatten()","explode()","unnest()"],correct:2}),`
`,e.jsx(n,{question:"Which of the following are valid reasons to use .cache() on a PySpark DataFrame? (Select all that apply)",options:["The same DataFrame is the input for three separate downstream aggregations","An iterative machine-learning algorithm reads the training DataFrame on each epoch","A DataFrame is used exactly once at the end of a pipeline","The DataFrame is the result of a complex multi-step join and cleaning operation","You want to persist data across separate Spark sessions after the driver exits"],correct:[0,1,3]}),`
`,e.jsx(t,{question:"What is the key reason a broadcast join is faster than a default sort-merge join when one table is small?",options:["Broadcast joins skip schema validation, reducing overhead","The small table is sent to every worker node so each partition of the large table can do a local lookup with no network shuffle","Spark caches the large table in memory before performing the join","Broadcast joins use columnar storage, which compresses data more efficiently during the join"],correct:1}),`
`,e.jsx(t,{question:"A data exploration step runs df.describe() on a PySpark DataFrame. Which statistics does describe() compute for numeric columns?",options:["count, mean, stddev, min, max","count, mean, median, mode, variance","count, sum, mean, min, max","count, mean, stddev, 25th percentile, 75th percentile"],correct:0}),`
`,e.jsx(n,{question:"Which of the following correctly describe how .persist() differs from .cache() in PySpark? (Select all that apply)",options:["cache() is equivalent to persist(StorageLevel.MEMORY_ONLY)","persist() accepts a StorageLevel argument such as MEMORY_AND_DISK or DISK_ONLY","cache() stores serialised bytes to reduce memory usage; persist() stores deserialized objects","With MEMORY_AND_DISK, partitions that do not fit in memory spill to disk instead of being recomputed","persist() can only be called once per DataFrame; a second call raises an error"],correct:[0,1,3]})]})}function l(a={}){const{wrapper:o}={...r(),...a.components};return o?e.jsx(o,{...a,children:e.jsx(s,{...a})}):s(a)}export{l as default};
