import{u as a,j as e,C as i,Q as t,M as r}from"./index-Ba5-wm3B.js";function s(o){const n={code:"code",h1:"h1",h2:"h2",p:"p",...a(),...o.components};return e.jsxs(e.Fragment,{children:[e.jsx(n.h1,{children:"Track Quiz — Attention & Encoder-Decoder Models"}),`
`,e.jsx(i,{title:"End-of-track checkpoint",children:e.jsxs(n.p,{children:["This quiz covers the full track: word embedding limitations (context-independence, out-of-vocabulary, no order), the encoder-decoder architecture (context vector handoff, autoregressive decoding, ",e.jsx(n.code,{children:"SOS"}),"/",e.jsx(n.code,{children:"EOS"})," tokens), BLEU score (n-gram precision, clipping, brevity penalty), teacher forcing, the encoder-decoder bottleneck and why it degrades with sentence length, the attention mechanism's core idea (per-step context vectors), the four-step attention pipeline (score, normalize, combine, generate), end-to-end training of attention weights, the three score functions (dot, general, additive) and scaled dot-product, and the limitations that survive attention (sequential computation, quadratic scoring cost). Expect conceptual, numerical, and code-reading questions."]})}),`
`,e.jsx(n.h2,{children:"Questions"}),`
`,e.jsx(t,{question:'In a classic word2vec/GloVe embedding table, "bank" in "river bank" and "bank" in "savings bank" get:',options:["Different vectors, computed from each sentence's context","The exact same vector, since the table has one fixed vector per word","No vector at all, since 'bank' is ambiguous","A vector that is the average of all possible senses, recomputed per sentence"],correct:1}),`
`,e.jsx(t,{question:"In the plain encoder-decoder architecture, what does the encoder pass to the decoder?",options:["Every hidden state it computed, one per source token","Only its final hidden state — the context vector c","The raw source token embeddings, unmodified","A separately trained alignment matrix"],correct:1}),`
`,e.jsx(t,{question:'Reference: "the cat sat on the mat" (6 tokens, "the" appears twice). Candidate: "the cat the cat on the mat" (7 tokens: the×3, cat×2, on×1, mat×1). What is the clipped unigram precision?',options:["5/7 ≈ 0.714","7/7 = 1.0","4/7 ≈ 0.571","6/7 ≈ 0.857"],correct:0}),`
`,e.jsx(t,{question:"Why does BLEU apply a brevity penalty in addition to clipping n-gram counts?",options:["To penalize candidates that are longer than the reference","Clipping alone doesn't stop a very short, high-precision candidate from scoring artificially well — the brevity penalty punishes candidates shorter than the reference","To normalize scores across different languages","Because clipping already handles length, the brevity penalty is redundant and rarely used"],correct:1}),`
`,e.jsx(t,{question:"Why is teacher forcing used during training but not at inference?",options:["It's used at both training and inference identically","Teacher forcing feeds ground-truth previous tokens to stabilize training; at inference there's no ground truth available, so the model must use its own predictions","Teacher forcing only works with GRUs, not LSTMs","It reduces the number of parameters needed at inference time"],correct:1}),`
`,e.jsx(t,{question:"Why does a plain encoder-decoder's translation quality degrade as source sentence length increases?",options:["Longer sentences have more grammar errors on average","The context vector has a fixed size regardless of input length, so longer sentences must be compressed into the same fixed budget","The decoder's vocabulary shrinks for longer inputs","Longer sentences always contain more out-of-vocabulary words"],correct:1}),`
`,e.jsx(t,{question:"What is the key architectural change attention makes to fix the bottleneck?",options:["It increases the size of the single context vector","It keeps every encoder hidden state and computes a fresh, step-specific weighted combination of them at each decoder step","It removes the decoder's RNN cell entirely","It trains the encoder and decoder on separate loss functions"],correct:1}),`
`,e.jsx(t,{question:"Put the four steps of the attention pipeline (for one decoder step) in the correct order.",options:["Normalize → Score → Generate → Combine","Score → Normalize → Combine → Generate","Combine → Score → Normalize → Generate","Generate → Score → Combine → Normalize"],correct:1}),`
`,e.jsx(t,{question:"Toy example: decoder state s = [1, 1], encoder states h₁ = [1, 0] and h₂ = [0, 1]. Using dot-product scoring, what is the resulting context vector c?",options:["[1, 1]","[0.5, 0.5]","[0, 0]","[1, 0]"],correct:1}),`
`,e.jsx(t,{question:"Why does dot-product attention typically get scaled by 1/√d before the softmax?",options:["To normalize the encoder hidden states to unit length","The dot product's variance grows with dimension d, and unscaled scores can saturate softmax toward a near one-hot output at high d","It reduces the number of learnable parameters","It is required for backpropagation to work correctly"],correct:1}),`
`,e.jsx(t,{question:"How are attention weights learned during training?",options:["From separately labeled human word-alignment data","End to end, via the same backpropagated cross-entropy loss used for the rest of the model — no separate supervision is needed","They are fixed at initialization based on word position","Through a separate unsupervised pretraining phase before translation training"],correct:1}),`
`,e.jsx(r,{question:"Which statements about the attention-based encoder-decoder are correct?",options:["Each decoder step computes its own context vector, tailored to that step","Attention removes the sequential dependency between decoder steps","Attention's scoring cost grows with the product of source and target sequence lengths","The score function's parameters are learned jointly with the rest of the model","Softmax guarantees the attention weights are non-negative and sum to 1","The encoder no longer needs to process the source sequence in order once attention is added"],correct:[0,2,3,4]}),`
`,e.jsx(r,{question:"Which limitations survive in the attention-based encoder-decoder, even after attention is added?",options:["Decoder steps must still be computed sequentially, one after another","The context vector is still a single fixed vector reused at every decoder step","The encoder still reads the source sequence one token at a time","Attention's O(T_src × T_tgt) scoring cost grows as sequences get longer","Training still requires labeled word-alignment data","Out-of-vocabulary handling is fully solved by attention"],correct:[0,2,3]})]})}function d(o={}){const{wrapper:n}={...a(),...o.components};return n?e.jsx(n,{...o,children:e.jsx(s,{...o})}):s(o)}export{d as default};
