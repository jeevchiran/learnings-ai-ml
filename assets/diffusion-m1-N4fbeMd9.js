import{r as u,j as e,u as m,B as g,R as d,C as f,Q as y,M as x}from"./index-WYRBR6CI.js";import{P as c}from"./PredictReveal-DnF7AdIF.js";import{A as v,S as w,R as j}from"./ui-B8T98xz3.js";const p="#7c2d12",l=10;function b(s){let n=s*9301+49297;return()=>(n=(n*9301+49297)%233280,n/233280)}function k(s,n){const i=b(n),r=[];for(let a=0;a<l;a++){r.push([]);for(let o=0;o<l;o++){const t=Math.exp(-(((a-4.5)**2+(o-4.5)**2)/14));r[a].push(Math.max(0,Math.min(1,t*(1-s)+i()*s)))}}return r}function A({data:s,label:n,cell:i=13}){return e.jsxs("div",{style:{textAlign:"center"},children:[e.jsx("svg",{width:l*i,height:l*i,role:"img","aria-label":n,children:s.map((r,a)=>r.map((o,t)=>e.jsx("rect",{x:t*i,y:a*i,width:i,height:i,fill:p,opacity:o},a+"-"+t)))}),e.jsx("div",{style:{fontSize:"0.68rem",color:"var(--text-muted)",marginTop:2},children:n})]})}function N(){const[s,n]=u.useState(5),i=Math.min(s,6),r=Array.from({length:i+1},(o,t)=>k(1-t/i,7)),a=1/s;return e.jsx(v,{value:p,children:e.jsxs("div",{children:[e.jsx(w,{label:"number of denoising steps",value:s,onChange:n,min:1,max:30,width:180}),e.jsx("div",{style:{display:"flex",gap:"0.5rem",flexWrap:"wrap",marginTop:"0.7rem",alignItems:"flex-start"},children:r.map((o,t)=>e.jsx(A,{data:o,label:t===0?"pure noise":t===i?"image":`step ${t}`},t))}),e.jsx(j,{items:[["steps",String(s)],["noise removed per step",`${(a*100).toFixed(0)}%`]]}),e.jsx("p",{style:{fontSize:"0.78rem",color:"var(--text-muted)",marginTop:"0.5rem",lineHeight:1.6},children:s===1?"With a single step the network has to go from pure noise to a finished image in one jump. That is exactly the hard problem a generator faces, and it is why a plain regression loss cannot do it alone.":"Each step now removes a small fraction of the noise, which is an easy prediction problem that ordinary supervised training handles. Many easy steps replace one impossible one — at the cost of running the network once per step."})]})})}function h(s){const n={div:"div",em:"em",h1:"h1",h2:"h2",hr:"hr",li:"li",p:"p",strong:"strong",ul:"ul",...m(),...s.components};return e.jsxs(e.Fragment,{children:[e.jsx(n.h1,{children:"Module 1: From Adversarial Games to Iterative Denoising"}),`
`,e.jsx(g,{children:e.jsx(n.p,{children:"The GAN track closed by noting that diffusion models now lead on sample fidelity for large-scale image generation, at the cost of far slower sampling. This track opens by asking what diffusion actually trades away, and why that trade turned out to be worth it."})}),`
`,e.jsx(n.h2,{children:"No game, no single fast pass"}),`
`,e.jsx(c,{prompt:"A GAN trains two competing networks against a shared adversarial objective. A diffusion model has no discriminator and no minimax game at all. What does it train instead?",options:["A single network trained with an ordinary regression loss to reverse a fixed, known corruption process","Two generators translating between domains, the same as CycleGAN"],correct:0,children:e.jsx(n.p,{children:"A diffusion model defines a fixed, mathematically known process that gradually destroys an image into pure noise — no learning involved in that direction at all. It then trains a single network to reverse one small step of that destruction at a time, using an ordinary supervised regression loss. There is no adversary to fool and no saddle point to search for, which sidesteps the entire training-instability story from the GAN track: no mode collapse, no vanishing-gradient race between two networks, no Nash equilibrium to hope for."})}),`
`,e.jsx(d,{items:[{q:"Why does a diffusion model avoid the GAN track's training-instability problems (mode collapse, vanishing gradients, saddle points)?",a:"It trains a single network with an ordinary supervised regression loss against a fixed, known corruption process, rather than two networks competing in an adversarial game. There is no discriminator, no saddle point, and therefore none of the instabilities that come specifically from a two-player minimax objective."}]}),`
`,e.jsx(n.div,{className:"lesson-visual",role:"region","aria-label":"Denoise Steps interactive example; scroll horizontally if needed",tabIndex:"0",children:e.jsx(N,{})}),`
`,e.jsx(n.h2,{children:"The trade: one fast pass for many slow ones"}),`
`,e.jsx(c,{prompt:"A GAN's generator turns noise into an image in a single forward pass. If a diffusion model instead reverses corruption one small step at a time, how many network evaluations does producing one image typically take?",options:["Exactly one, the same as a GAN","Many — often hundreds to thousands of sequential network evaluations"],correct:1,children:e.jsxs(n.p,{children:["Because the reverse process only reliably undoes ",e.jsx(n.em,{children:"small"})," corruption steps, generating a sample means applying the learned denoising step repeatedly, in sequence — each step depends on the output of the previous one, so they cannot be parallelized away. This is the central cost of diffusion models, and it is a direct, mechanical consequence of the same design choice that removes the adversarial instability: reversing a large corruption in one shot is hard to learn reliably, but reversing many tiny corruptions, one at a time, is comparatively easy to get right."]})}),`
`,e.jsx(d,{items:[{q:"Why does a diffusion model need many sequential network evaluations to generate one sample, unlike a GAN's single forward pass?",a:"The reverse process is only designed to reliably undo small corruption steps, so producing a sample means applying that small step repeatedly, each one depending on the previous step's output. This sequential dependency is the direct cost of the same design choice — small, easy-to-learn steps — that removes GAN-style adversarial instability."}]}),`
`,e.jsx(n.hr,{}),`
`,e.jsx(f,{title:"TL;DR",children:e.jsxs(n.ul,{children:[`
`,e.jsxs(n.li,{children:["A diffusion model has ",e.jsx(n.strong,{children:"no adversarial game"}),": a fixed, known process gradually destroys data into noise, and a single network is trained with an ordinary regression loss to reverse one small step of that destruction."]}),`
`,e.jsx(n.li,{children:"This sidesteps GAN-style training instability entirely — no competing discriminator. Non-convex optimization, poor coverage and low diversity can still occur."}),`
`,e.jsxs(n.li,{children:["The cost is sampling speed: generating one sample requires many ",e.jsx(n.strong,{children:"sequential"})," network evaluations (undoing corruption one small step at a time), unlike a GAN's single forward pass."]}),`
`]})}),`
`,e.jsx(y,{question:"What is the fundamental structural difference between how a GAN and a diffusion model are trained?",options:["A GAN trains two networks in an adversarial game; a diffusion model trains one network with an ordinary regression loss against a fixed corruption process","A diffusion model also uses a discriminator, just with a different loss function","Both are trained identically — the only difference is at sampling time","A diffusion model requires labeled data while a GAN does not"],correct:0}),`
`,e.jsx(x,{question:"Which statements about diffusion models vs. GANs are correct?",options:["Diffusion models train a single network with a supervised regression loss, not an adversarial game","Diffusion models avoid mode collapse and saddle-point training dynamics because there is no discriminator","Generating one sample from a diffusion model typically requires many sequential network evaluations","Diffusion models generate images in a single forward pass, just like GANs","The sequential nature of diffusion sampling is a direct consequence of only reversing small corruption steps at a time"],correct:[0,1,2,4]})]})}function M(s={}){const{wrapper:n}={...m(),...s.components};return n?e.jsx(n,{...s,children:e.jsx(h,{...s})}):h(s)}export{M as default};
