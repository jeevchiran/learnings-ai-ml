import{u as o,j as e,C as r,Q as t,M as i}from"./index-WYRBR6CI.js";function a(n){const s={h1:"h1",h2:"h2",p:"p",...o(),...n.components};return e.jsxs(e.Fragment,{children:[e.jsx(s.h1,{children:"Track Quiz — Diffusion Models"}),`
`,`
`,e.jsx(r,{title:"End-of-track checkpoint",children:e.jsx(s.p,{children:"Sixteen questions plus three multi-selects spanning the complete Diffusion Models curriculum: the forward and reverse processes, the ELBO connection to VAEs and the simple noise-prediction loss, the U-Net denoiser and timestep conditioning, sampling cost, noise schedules and DDIM, classifier-free guidance, latent diffusion, and the VAE/GAN/diffusion comparison. Work through the reasoning before submitting your answers!"})}),`
`,e.jsx(s.h2,{children:"Single-Choice Questions"}),`
`,e.jsx(t,{question:"What does a diffusion model train, in contrast to a GAN's adversarial game?",options:["A single network with an ordinary regression loss, trained to reverse a fixed, known corruption process","Two competing networks trained against a shared minimax objective","A classifier that distinguishes real images from generated ones","Nothing is trained — diffusion models use a fixed, hand-designed denoising rule"],correct:0}),`
`,e.jsx(t,{question:"Why does generating a sample from a diffusion model typically require many sequential network evaluations?",options:["Because the reverse process only reliably undoes small corruption steps, so producing a sample means applying that step repeatedly in sequence","Because the U-Net architecture is inherently much slower to evaluate than a GAN's generator","Because diffusion models must be run without GPU acceleration","Because a separate model must be trained for each generated sample"],correct:0}),`
`,e.jsx(t,{question:"Why can x_t be sampled directly from x_0 in one step, instead of simulating the forward chain?",options:["Composing many small Gaussian steps is itself Gaussian, giving a closed form for q(x_t | x_0)","The forward process has no randomness at all","x_t does not actually depend on x_0 in any way","The network learns to skip steps during training"],correct:0}),`
`,e.jsx(t,{question:"What happens to ᾱ_t as t approaches T, and what does that mean?",options:["ᾱ_t approaches 0, meaning x_t becomes statistically indistinguishable from pure noise","ᾱ_t approaches 1, meaning x_t retains almost all of the original signal","ᾱ_t oscillates unpredictably with no clear trend","ᾱ_t is undefined for t close to T"],correct:0}),`
`,e.jsx(t,{question:"Why is a Gaussian a reasonable model for a single reverse step, even though reversing the whole forward process exactly is intractable?",options:["For a sufficiently small forward step, the true reverse conditional is itself approximately Gaussian","Gaussians are simply easier to implement in PyTorch, with no theoretical justification","The reverse process is actually identical to the forward process run backwards","Only the first reverse step needs to be Gaussian; the rest can be arbitrary"],correct:0}),`
`,e.jsx(t,{question:"How does a diffusion model's training objective relate to the VAE's ELBO?",options:["It is the same variational lower bound idea, extended to a chain of T latent variables with a fixed rather than learned forward process","The two objectives are completely unrelated mathematical frameworks","Diffusion models use the ELBO only at sampling time, never during training","The ELBO applies only when T = 1"],correct:0}),`
`,e.jsx(t,{question:"What does the simplified DDPM loss L_simple actually train the network to predict?",options:["The noise ε that was added to produce x_t from x_0, given x_t and the timestep t","The exact pixel values of x_0 directly, given only random noise","Whether an image is real or generated","The value of β_t used at each timestep"],correct:0}),`
`,e.jsx(t,{question:"Why does U-Net's architecture transfer well from image segmentation to diffusion noise prediction?",options:["Both tasks need a dense, same-resolution image-to-image mapping with multi-scale features and preserved spatial detail","Segmentation and noise prediction use mathematically identical loss functions","U-Net was originally designed for noise prediction and later adapted for segmentation","It doesn't transfer well — a completely different architecture is required"],correct:0}),`
`,e.jsx(t,{question:"How does a single U-Net handle denoising at every timestep from 1 to T?",options:["A timestep embedding, injected into every block, conditions the shared network's behavior on the current noise level","A separate copy of the network is trained independently for each timestep","The network ignores the timestep entirely and produces the same output regardless of t","The timestep is used only to initialize the network's weights before training"],correct:0}),`
`,e.jsx(t,{question:"Why can't sampling use the same closed-form shortcut that training uses to jump between timesteps?",options:["The closed form requires knowing x_0, which is exactly what sampling is trying to produce and doesn't have yet","Sampling is actually able to use the exact same shortcut as training","The closed form only works for even values of t","Sampling doesn't use any timesteps at all"],correct:0}),`
`,e.jsx(t,{question:"Why does the cosine noise schedule tend to produce better samples than a linear schedule for the same T?",options:["It keeps ᾱ_t closer to 1 for longer near t=0, preserving fine detail before ramping up destruction","It uses a completely different, unrelated training loss","It reduces the total number of timesteps required to exactly half","It removes the need for a U-Net entirely"],correct:0}),`
`,e.jsx(t,{question:"What does DDIM change compared to standard DDPM sampling?",options:["Only the sampling procedure — a non-Markovian reverse process letting the same trained network skip timesteps","It requires training a completely new network from scratch","It replaces the noise-prediction loss with an adversarial loss","It removes randomness from the forward process during training"],correct:0}),`
`,e.jsx(t,{question:"Why isn't conditioning ε_θ on a label y alone considered strong enough for most prompt-following applications?",options:["A single conditional prediction pays only some attention to the prompt, motivating extrapolation past it via classifier-free guidance","Conditioning on y is mathematically impossible for a diffusion model","Labels can only be used during evaluation, never during training","Conditioning on y always produces perfect prompt adherence, making guidance unnecessary"],correct:0}),`
`,e.jsx(t,{question:"What is the trade-off as the classifier-free guidance scale w increases past 1?",options:["Prompt adherence increases, but sample diversity decreases","Both prompt adherence and diversity increase together with no downside","Sampling speed increases at the cost of image resolution","The training loss changes from regression to classification"],correct:0}),`
`,e.jsx(t,{question:"Why does running diffusion in a pretrained autoencoder's latent space reduce compute rather than just relocating the same cost?",options:["The encoder and decoder each run once per image, while the T sequential denoising steps — the dominant cost — now operate on a much smaller tensor","Latent diffusion uses a fundamentally different, cheaper loss function","It reduces the number of timesteps T required by definition","It eliminates the need for a U-Net denoiser entirely"],correct:0}),`
`,e.jsx(t,{question:"What is the deepest structural dividing line between GANs and both VAEs and diffusion models?",options:["GANs optimize an adversarial game with no likelihood bound, while VAEs and diffusion models both maximize a variational lower bound on log p(x)","GANs use convolutional networks and the other two do not","Only GANs can generate images at all","VAEs and diffusion models require labeled training data while GANs do not"],correct:0}),`
`,e.jsx(s.h2,{children:"Multi-Select Questions"}),`
`,e.jsx(i,{question:"Which statements about the forward and reverse diffusion processes are correct? (Select all that apply)",options:["The forward process is a fixed Markov chain with no learned parameters","x_t can be sampled directly from x_0 in closed form, without simulating the chain","The reverse process is modeled as a Gaussian with a learned mean, shared across all timesteps","ᾱ_t increases toward 1 as t approaches T","A single network conditioned on t is used, rather than a separate network per timestep"],correct:[0,1,2,4]}),`
`,e.jsx(i,{question:"Which statements about the diffusion training objective and U-Net denoiser are correct? (Select all that apply)",options:["A diffusion model's objective is a variational lower bound, structurally the same idea as a VAE's ELBO","The simplified loss trains the network to predict the added noise ε via ordinary regression","Training a diffusion model requires an adversarial discriminator","U-Net's skip connections are needed because the output must match the input's full resolution","Timestep embeddings are conceptually the same idea as Transformer positional encodings"],correct:[0,1,3,4]}),`
`,e.jsx(i,{question:"Which statements about sampling speed, DDIM, guidance, and the VAE/GAN/diffusion comparison are correct? (Select all that apply)",options:["The sequential dependency between reverse steps is why diffusion sampling cannot be parallelized across steps","DDIM reuses the same trained network, changing only the sampling procedure","Increasing the classifier-free guidance scale w always increases both adherence and diversity","GAN training's adversarial dynamics tend to become harder to stabilize as models and datasets scale up","Latent diffusion runs the denoising process on a compressed representation rather than raw pixels"],correct:[0,1,3,4]})]})}function d(n={}){const{wrapper:s}={...o(),...n.components};return s?e.jsx(s,{...n,children:e.jsx(a,{...n})}):a(n)}export{d as default};
