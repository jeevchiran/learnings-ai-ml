import{u as a,j as e,B as d,R as o,C as r,Q as c,M as h}from"./index-Ba5-wm3B.js";import{P as s}from"./PredictReveal-CQqAapoB.js";function i(n){const t={code:"code",em:"em",h1:"h1",h2:"h2",hr:"hr",li:"li",p:"p",strong:"strong",ul:"ul",...a(),...n.components};return e.jsxs(e.Fragment,{children:[e.jsx(t.h1,{children:"Module 1: Word Embeddings — Strengths and Limits"}),`
`,e.jsx(d,{children:e.jsx(t.p,{children:`The RNN track's final module ended on a teaser: translation needs an encoder and a decoder, the whole source sentence gets squeezed into one fixed-size vector, and that squeeze is "the through-line that motivated attention, and from there transformers." This track cashes that promise in — properly this time, with the architecture, the math, the training loop, and runnable code. Before any of that: every word going into these models first has to become a vector, so start there.`})}),`
`,e.jsx(t.p,{children:`A word embedding is a dense vector that stands in for a word — learned so that words used in similar contexts end up with similar vectors. "coffee" and "tea" land close together; "coffee" and "bicycle" don't. That single idea — distributional similarity, encoded as geometry — is what every model in this track builds on top of.`}),`
`,e.jsx(t.h2,{children:"One vector per word, learned from context"}),`
`,e.jsxs(s,{prompt:"word2vec trains by predicting a word from its neighbours (or vice versa) across a huge corpus. Two words that never once appear in the same sentence — can they still end up with similar vectors?",options:["No — similarity requires having appeared together","Yes — sharing the same kinds of neighbours is enough"],correct:1,children:[e.jsx(t.p,{children:'"cat" and "dog" rarely sit in the exact same sentence, but both show up near "pet", "vet", "feed", "walk". word2vec (skip-gram or CBOW) and GloVe both exploit this: predict a word from its context (or context from the word) over millions of sentences, and words with interchangeable contexts converge to nearby vectors — without ever being told what "cat" or "dog" mean.'}),e.jsxs(t.p,{children:["The result is a lookup table: one fixed vector per vocabulary word, typically 100–300 dimensions, learned once and reused everywhere. Arithmetic on these vectors captures relationships too — the classic example is ",e.jsx(t.code,{children:"king - man + woman ≈ queen"}),"."]})]}),`
`,e.jsx(o,{items:[{q:"Why do 'cat' and 'dog' end up with similar embeddings even if they rarely co-occur directly?",a:"Because word2vec and GloVe learn from shared context, not direct co-occurrence. Both words tend to appear near the same neighbours (pet, vet, walk, feed), and it's that shared context distribution — not appearing together — that pulls their vectors close."}]}),`
`,e.jsx(t.h2,{children:"The limit: one vector, no matter the sentence"}),`
`,e.jsxs(s,{prompt:'"river bank" and "savings bank" both contain the word "bank". In a plain word-embedding table (word2vec/GloVe), do these two occurrences get the same vector or different vectors?',options:["The same vector","Different vectors, based on context"],correct:0,children:[e.jsxs(t.p,{children:["A classic embedding table is a fixed lookup: ",e.jsx(t.code,{children:'embedding["bank"]'}),' returns one vector, always, regardless of what surrounds it in a given sentence. "bank" in "river bank" and "bank" in "savings bank" are the exact same 300 numbers. The model has folded every sense of the word into a single average-ish point in space.']}),e.jsxs(t.p,{children:["This is the ceiling of this approach: embeddings are ",e.jsx(t.strong,{children:"context-independent"}),". They tell you what a word tends to mean ",e.jsx(t.em,{children:"in general"}),", not what it means ",e.jsx(t.em,{children:"right here"}),`. There's also no mechanism for words the table has never seen (out-of-vocabulary tokens just fail to resolve) and no notion of word order at all — the table has no idea if "bank" was the 1st word or the 41st.`]})]}),`
`,e.jsx(r,{title:"Where this is going",children:e.jsxs(t.p,{children:['Fixing "one vector per word, no matter the sentence" is a big part of what the rest of this track — and eventually self-attention in transformers — is about. The encoder-decoder architecture in the next module is the first step: instead of looking up a word in isolation, an RNN reads it ',e.jsx(t.em,{children:"in sequence"}),", so the representation it builds can depend on everything that came before."]})}),`
`,e.jsx(o,{items:[{q:"Name two limitations of a classic word-embedding table (word2vec/GloVe), besides being context-independent.",a:"It can't handle out-of-vocabulary words — anything not seen during training simply has no vector. And it carries no notion of order or position: the table returns the same vector whether the word was first, last, or anywhere in between."}]}),`
`,e.jsx(t.hr,{}),`
`,e.jsx(r,{title:"TL;DR",children:e.jsxs(t.ul,{children:[`
`,e.jsxs(t.li,{children:["A word embedding is a dense vector learned so that words with similar contexts end up close together — the mechanism behind ",e.jsx(t.code,{children:"king - man + woman ≈ queen"}),"."]}),`
`,e.jsxs(t.li,{children:["word2vec/GloVe learn from ",e.jsx(t.strong,{children:"shared context"}),", not direct co-occurrence, which is why semantically related words cluster even without appearing in the same sentence."]}),`
`,e.jsxs(t.li,{children:["The core limitation: a classic embedding table returns ",e.jsx(t.strong,{children:"one fixed vector per word"}),', identical no matter which sentence it appears in — "bank" (river) and "bank" (money) are indistinguishable.']}),`
`,e.jsxs(t.li,{children:["No handling of out-of-vocabulary words, and no notion of word order — both motivate reading words ",e.jsx(t.em,{children:"in sequence"}),", which is where the encoder-decoder architecture starts."]}),`
`]})}),`
`,e.jsx(c,{question:"In a classic word2vec/GloVe embedding table, what determines a word's vector?",options:["The specific sentence it currently appears in","A single vector learned once from the word's typical contexts across the whole training corpus, fixed thereafter","The position of the word within its current sentence","A hand-crafted rule based on part of speech"],correct:1}),`
`,e.jsx(h,{question:"Which of these are genuine limitations of a classic (word2vec/GloVe-style) word embedding table?",options:["The same word always gets the same vector, regardless of context","It cannot represent semantic similarity between words at all","It has no built-in notion of word order","Out-of-vocabulary words have no vector","It requires labeled training data for every word","Vector arithmetic never captures any relationship between words"],correct:[0,2,3]})]})}function u(n={}){const{wrapper:t}={...a(),...n.components};return t?e.jsx(t,{...n,children:e.jsx(i,{...n})}):i(n)}export{u as default};
