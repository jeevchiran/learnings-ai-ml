# Track Quiz — Foundations of Deep Learning

import { ConceptBox, Bridge, QuizCard, MultiSelectQuiz } from '../../components/learning/index.js'

<ConceptBox title="End-of-track checkpoint">
This quiz covers all eleven modules of the Deep Learning track — from the perceptron and MLPs, through PyTorch tensors, data handling, forward and backward propagation, activation functions, loss functions and optimisers, weight initialisation, regularisation, and overfitting prevention. Some questions require a short calculation; read each option carefully before choosing.
</ConceptBox>

## Questions

<QuizCard
  question="A single perceptron with two inputs computes f(x) = step(w₁x₁ + w₂x₂ + b). Why can a perceptron not learn the XOR function, regardless of the weights chosen?"
  options={[
    "A perceptron's step function is not differentiable, so gradient descent cannot converge on XOR",
    "XOR is not linearly separable — no single hyperplane can partition the four input points (0,0), (0,1), (1,0), (1,1) into the correct output classes",
    "The perceptron has too many parameters to memorise a four-row truth table",
    "XOR requires real-valued outputs, but the step function only produces integers"
  ]}
  correct={1}
/>

<QuizCard
  question="A 3-layer MLP has input dimension 784, hidden layer 1 of size 256, hidden layer 2 of size 128, and output layer of size 10. Counting all weight matrices and bias vectors, how many total trainable parameters does this network have?"
  options={[
    "784×256 + 256×128 + 128×10 = 200 704 + 32 768 + 1 280 = 234 752 (no biases)",
    "(784×256 + 256) + (256×128 + 128) + (128×10 + 10) = 201 216 + 32 896 + 1 290 = 235 402",
    "784 + 256 + 128 + 10 = 1 178 (only the layer sizes)",
    "(784+256) + (256+128) + (128+10) = 1562 (sum of adjacent sizes)"
  ]}
  correct={1}
/>

<QuizCard
  question="In PyTorch, you have a weight matrix W of shape (128, 256) and an activation tensor x of shape (256,). Which expression computes the linear transformation z = Wx correctly?"
  options={[
    "z = W * x  # element-wise multiply",
    "z = W @ x  # matrix-vector product, result shape (128,)",
    "z = x @ W  # produces shape (256, 128), not (128,)",
    "z = torch.dot(W, x)  # dot product between 2-D and 1-D tensors"
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="Which of the following are correct statements about PyTorch's DataLoader and Dataset abstractions? (Select all that apply.)"
  options={[
    "A custom Dataset must implement __len__ and __getitem__ to be compatible with DataLoader",
    "Setting shuffle=True in DataLoader reshuffles the data at the start of every epoch",
    "DataLoader always loads the entire dataset into GPU memory before training begins",
    "The batch_size parameter in DataLoader controls how many samples are returned per iteration",
    "nn.Module's state_dict() returns a dictionary of parameter names mapped to their tensor values"
  ]}
  correct={[0, 1, 3, 4]}
/>

<QuizCard
  question="During forward propagation through a layer z = Wx + b followed by a = ReLU(z), which values must be cached to make backpropagation efficient?"
  options={[
    "Only the final output a, since the chain rule only requires the outermost activation",
    "The pre-activation z (or equivalently the input x and parameters W, b), so that gradients through ReLU and the linear layer can be computed without re-running the forward pass",
    "The gradient of the loss with respect to a, precomputed before the forward pass begins",
    "Only the weight matrix W, since biases do not affect the gradient computation"
  ]}
  correct={1}
/>

<QuizCard
  question="In backpropagation, the gradient of the loss L with respect to the weight matrix W in a linear layer z = Wx is given by ∂L/∂W = δxᵀ, where δ = ∂L/∂z. If δ has shape (128,) and x has shape (256,), what is the shape of ∂L/∂W, and why must it match the shape of W?"
  options={[
    "Shape (256, 128) — the gradient must have the same shape as W so the optimiser can apply the update W ← W − η·∂L/∂W element-wise",
    "Shape (128, 256) — δ is a column vector and xᵀ is a row vector; their outer product is (128, 256), which matches W's shape (128, 256)",
    "Shape (128,) — the gradient is the same shape as the output δ",
    "Shape (256,) — the gradient is the same shape as the input x"
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="Which of the following correctly describe the vanishing gradient problem and strategies to address it? (Select all that apply.)"
  options={[
    "Sigmoid and tanh activation functions saturate for large input magnitudes, producing near-zero gradients that slow learning in early layers",
    "ReLU avoids saturation for positive inputs, which mitigates vanishing gradients, but can cause 'dying ReLU' when neurons get stuck outputting 0",
    "Leaky ReLU addresses dying ReLU by allowing a small negative slope (e.g. 0.01) for negative inputs",
    "Using He initialisation (Var = 2/fan_in) is specifically designed for ReLU networks to maintain signal variance across layers",
    "Vanishing gradients only occur in networks with more than 100 layers and are not a concern in 5-layer MLPs"
  ]}
  correct={[0, 1, 2, 3]}
/>

<QuizCard
  question="The Adam optimiser maintains two moving averages for each parameter. What are they, and what problem does each solve compared to vanilla SGD?"
  options={[
    "m (first moment, mean of gradients) and v (second moment, mean of squared gradients); m provides momentum to smooth updates, v adapts the learning rate per parameter so that rarely-updated parameters take larger steps",
    "m (maximum gradient seen so far) and v (variance of weight values); together they normalise the weight scale",
    "m (gradient from the current batch) and v (gradient from the previous batch); their average reduces gradient noise",
    "m (model weights) and v (gradient velocity); v accumulates momentum while m stores the parameter values for rollback"
  ]}
  correct={0}
/>

<QuizCard
  question="For a layer with fan_in = 256 and fan_out = 128, compute the variance and standard deviation prescribed by Xavier/Glorot initialisation."
  options={[
    "Var = 2 / (256 + 128) = 2/384 ≈ 0.00521; std ≈ 0.0721",
    "Var = 1 / 256 ≈ 0.00391; std ≈ 0.0625",
    "Var = 2 / 256 ≈ 0.00781; std ≈ 0.0884 (He initialisation for ReLU, not Xavier)",
    "Var = 6 / (256 + 128) = 6/384 ≈ 0.01563; std ≈ 0.125 (uniform Xavier bound, not the variance)"
  ]}
  correct={0}
/>

<MultiSelectQuiz
  question="Which of the following statements about Dropout and BatchNorm are correct? (Select all that apply.)"
  options={[
    "During training, Dropout randomly zeros activations with probability p and scales surviving activations by 1/(1−p) so that the expected output magnitude is unchanged",
    "At test time, Dropout is disabled entirely — all activations pass through without scaling",
    "BatchNorm normalises each feature across the mini-batch to zero mean and unit variance, then applies learned scale γ and shift β parameters",
    "BatchNorm and Dropout should always be used together in every layer for best regularisation",
    "L1 regularisation encourages weight sparsity by adding a penalty proportional to the absolute value of weights, while L2 (weight decay) adds a penalty proportional to the squared weights"
  ]}
  correct={[0, 1, 2, 4]}
/>

<QuizCard
  question="The cross-entropy loss for a single correct-class prediction is L = −log(p), where p is the predicted probability assigned to the true class. If the model outputs p = 0.6 for the true class, what is the loss (natural log, rounded to 3 decimal places)?"
  options={[
    "−log(0.6) ≈ 0.511",
    "−log(0.6) ≈ 0.916 (using natural log: ln(0.6) = −0.5108... wait — ln(0.6) ≈ −0.916, so −ln(0.6) ≈ 0.916)",
    "−log₁₀(0.6) ≈ 0.222",
    "−log(0.6) ≈ 1.204 (using log base 2)"
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="Which of the following are valid strategies to prevent overfitting in a deep MLP? (Select all that apply.)"
  options={[
    "Early stopping: monitor validation loss and halt training when it stops improving for a patience window of epochs",
    "Data augmentation: apply label-preserving transformations (e.g. noise injection for tabular data) to increase effective training set size",
    "Increasing model depth and width without bound, since a more powerful model always generalises better",
    "Learning rate scheduling: reduce the learning rate when validation loss plateaus (ReduceLROnPlateau) to fine-tune the weights near convergence",
    "Weight decay (L2 regularisation): add a penalty on large weights to the loss, discouraging the model from fitting noise"
  ]}
  correct={[0, 1, 3, 4]}
/>

<QuizCard
  question="You are reading PyTorch code and see the following snippet. What does it do, and what will `out.shape` be if `x.shape == (32, 784)`?\n\n```python\nlinear = nn.Linear(784, 256)\nout = torch.relu(linear(x))\n```"
  options={[
    "Applies a linear transformation W∈(256,784) plus bias b∈(256,), then zeros all negative values; out.shape == (32, 256)",
    "Applies element-wise ReLU to x, then multiplies by a 784×256 matrix; out.shape == (32, 784)",
    "Applies linear(x) to produce shape (784, 256), then ReLU; out.shape == (784, 256)",
    "Raises a shape mismatch error because nn.Linear(784, 256) expects input shape (784,), not (32, 784)"
  ]}
  correct={0}
/>
