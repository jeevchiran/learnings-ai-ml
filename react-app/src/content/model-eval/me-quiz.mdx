import { ConceptBox, QuizCard, MultiSelectQuiz } from '../../components/learning/index.js'

## Track Quiz — Model Selection & Feature Engineering

<ConceptBox title="End-of-track checkpoint">
This quiz covers all eight modules of the Model Selection & Feature Engineering track — the bias-variance tradeoff, validation sets, cross-validation mechanics, hyperparameter search strategies, the curse of dimensionality, filter/wrapper/embedded feature selection, and leakage-safe pipelines. Several questions require reasoning through a small scenario; read carefully before answering.
</ConceptBox>

## Questions

<QuizCard
  question="A degree-15 polynomial fit to 20 noisy training points achieves near-zero training error but performs far worse than a degree-2 polynomial on held-out data. This is a textbook case of:"
  options={[
    'High bias, low variance',
    'Low bias, high variance (overfitting)',
    'Irreducible noise dominating the error',
    'A labeling error in the training data'
  ]}
  correct={1}
/>

<QuizCard
  question="You evaluate a model with one random train/validation split and get 91% accuracy. A colleague uses a different random seed for the same split and gets 85%. What does this gap most directly tell you?"
  options={[
    'The model is broken and should be discarded',
    'The single holdout validation score has high variance, and one split is not a reliable estimate of generalization',
    'The dataset must be corrupted',
    'Accuracy is the wrong metric for this problem'
  ]}
  correct={1}
/>

<QuizCard
  question="Why is it a mistake to repeatedly check performance on your test set while trying different hyperparameter settings, then report the best test-set score?"
  options={[
    'The test set is too small to compute accuracy correctly',
    'Test sets cannot be used with scikit-learn',
    'Every decision made based on the test score turns it into a de facto validation set, so the final reported number is no longer an honest estimate of generalization',
    'This is actually a completely valid and recommended practice'
  ]}
  correct={2}
/>

<QuizCard
  question="With 5-fold cross-validation on a dataset of 100 samples, how many samples are in each fold's validation set, and how many total model fits does one round of CV require?"
  options={[
    '5 samples per fold; 100 model fits',
    '20 samples per fold; 5 model fits',
    '20 samples per fold; 100 model fits',
    '100 samples per fold; 5 model fits'
  ]}
  correct={1}
/>

<QuizCard
  question="Compared to K=5, using K=10 in K-Fold CV typically:"
  options={[
    'Increases bias in the CV estimate because training sets are smaller',
    'Decreases bias in the CV estimate (larger training sets per fold, closer to using the full dataset) at the cost of more compute',
    'Has no effect on bias, only on runtime',
    'Eliminates the need for a separate test set entirely'
  ]}
  correct={1}
/>

<QuizCard
  question="Why does plain (non-stratified) K-Fold CV risk misleading results on an imbalanced binary classification dataset (e.g. 95% negative, 5% positive)?"
  options={[
    'K-Fold CV cannot be used on binary targets at all',
    'A random fold split can produce validation folds with very few or zero positive examples, making the per-fold score unstable or undefined',
    'K-Fold CV always requires exactly 2 folds for binary targets',
    'It has no risk — stratification is purely a stylistic preference'
  ]}
  correct={1}
/>

<QuizCard
  question="Leave-One-Out CV (LOO) trains on N-1 samples and validates on 1, repeated N times. Despite being nearly unbiased, why can its overall CV variance still be high?"
  options={[
    'Because LOO always uses far too little training data per fold',
    'Because the N training sets overlap almost completely with each other, so the N validation scores are highly correlated rather than independent estimates',
    'Because LOO is mathematically identical to a single 50/50 holdout split',
    'Because scikit-learn implements LOO incorrectly'
  ]}
  correct={1}
/>

<QuizCard
  question="You're forecasting next month's sales from a time-ordered dataset. Why is plain shuffled K-Fold CV inappropriate here, and what should you use instead?"
  options={[
    'Shuffled K-Fold is fine for time series; no change is needed',
    'Shuffled K-Fold can put future data in the training set while validating on the past, leaking information a real deployment would never have; use TimeSeriesSplit (expanding window) instead',
    'Time series data cannot be cross-validated under any method',
    'You should always use Leave-One-Out for time series data'
  ]}
  correct={1}
/>

<QuizCard
  question="You have 3 hyperparameters, each with 5 candidate values, and are using 5-fold CV to score every combination. How many total model fits does an exhaustive Grid Search require?"
  options={[
    '15 (3 x 5)',
    '25 (5 x 5)',
    '625 (5 x 5 x 5 x 5)',
    '125 (5 x 5 x 5), each requiring 5 CV fits = 625 total, but 125 distinct hyperparameter settings'
  ]}
  correct={3}
/>

<QuizCard
  question="Per the Bergstra & Bengio insight, why does Random Search often outperform Grid Search when only a few hyperparameters actually matter?"
  options={[
    'Random Search always uses more total evaluations than Grid Search',
    "Grid search wastes evaluations by repeating the same values along unimportant axes, while random search never repeats a value along any single axis, so it explores the important dimensions more densely for the same budget",
    'Random Search is a deprecated technique that should not be used',
    'Grid Search cannot be parallelized, but Random Search can'
  ]}
  correct={1}
/>

<QuizCard
  question="What is the core idea behind Bayesian Optimization for hyperparameter search?"
  options={[
    'Try every combination on a fixed grid, just in a random order',
    "Build a probabilistic surrogate model of the score-vs-hyperparameters surface from evaluations so far, and use it to pick the next point that balances exploring uncertain regions against exploiting known-good ones",
    'Always evaluate the hyperparameter setting with the smallest possible value',
    'Discard all but the single best configuration after just one evaluation each'
  ]}
  correct={1}
/>

<QuizCard
  question="In a 10-dimensional feature space, why does the notion of 'nearest neighbor' become less meaningful as dimensionality grows (the curse of dimensionality)?"
  options={[
    'High-dimensional data cannot be stored in memory',
    'As dimensions increase, the ratio of the distance to the nearest point vs. the farthest point approaches 1, so all points start looking roughly equidistant',
    'Distance calculations become mathematically undefined above 3 dimensions',
    'Neural networks stop working entirely above 10 dimensions'
  ]}
  correct={1}
/>

<QuizCard
  question="What is the key difference between feature selection and dimensionality reduction techniques like PCA?"
  options={[
    'They are two names for exactly the same technique',
    'Feature selection keeps a subset of the original, interpretable features; PCA creates new features that are linear combinations of the originals',
    'PCA can only be used for classification, never regression',
    'Feature selection always requires more compute than PCA'
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="Which of the following are true about filter-based feature selection methods? (Select all that apply.)"
  options={[
    'They score each feature independently of any downstream model',
    'They can fully account for interactions between pairs of features',
    'Variance Threshold drops near-constant features without ever looking at the target',
    'Mutual information can capture non-linear dependencies that Pearson correlation would miss',
    'Chi-square and ANOVA F-test both require the feature values to be strictly negative'
  ]}
  correct={[0, 2, 3]}
/>

<QuizCard
  question="Recursive Feature Elimination (RFE) selects features by:"
  options={[
    'Randomly dropping features until a target count is reached',
    'Fitting a model, ranking features by the model\'s own importance/coefficients, dropping the weakest, and refitting — repeated until the target count is reached',
    'Computing a correlation matrix once and thresholding it',
    'Training a completely separate neural network to score each feature'
  ]}
  correct={1}
/>

<QuizCard
  question="Why does L1 (Lasso) regularization drive some coefficients exactly to zero, while L2 (Ridge) only shrinks them toward zero without eliminating them?"
  options={[
    'L1 and L2 behave identically; this is a common misconception',
    "The L1 penalty's constraint region has corners aligned with the coordinate axes, which the optimum is likely to land on exactly; the L2 penalty's constraint region is smooth (a sphere/ellipse) with no such corners",
    'L2 regularization is only defined for classification problems',
    'L1 regularization requires categorical features, so numeric coefficients cannot reach exactly zero'
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="Which of the following are genuine limitations or caveats of the methods discussed in the Wrapper & Embedded Methods module? (Select all that apply.)"
  options={[
    'Forward and backward sequential selection are greedy and are not guaranteed to find the globally optimal feature subset',
    'Impurity-based tree feature importance is biased toward high-cardinality/continuous features',
    'Wrapper methods are always faster than filter methods because they use fewer model fits',
    "Lasso's selected features reflect a linear model's view of relevance, which may differ from what a tree-based model would find useful",
    'RFE requires zero additional model fits beyond the original single fit'
  ]}
  correct={[0, 1, 3]}
/>

<QuizCard
  question="You rank 1,000 purely random noise features by their correlation with a random binary target, computed on the FULL dataset, then keep the top 20 and run 5-fold CV using only those 20 features. Why does this produce an artificially inflated accuracy?"
  options={[
    'It doesn\'t — the result would be an honest, unbiased 50% accuracy estimate',
    'The top 20 features were cherry-picked because they happened to correlate with the target, including within what are now the CV validation folds — so of course they look predictive on those same folds',
    'Correlation-based filtering always produces exactly 100% accuracy on noise',
    '5-fold CV is mathematically incapable of evaluating noise features'
  ]}
  correct={1}
/>

<QuizCard
  question="What does wrapping a feature selector and a classifier together in a single scikit-learn Pipeline and passing that pipeline to cross_val_score actually fix?"
  options={[
    'It makes the model train faster, but does not affect correctness',
    'It ensures the entire pipeline — including feature selection — is refit from scratch on each training fold, so selection never sees that fold\'s validation rows',
    'It automatically increases the number of folds used',
    'It removes the need for a train/test split entirely'
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="Which of the following statements about leakage-safe evaluation are correct? (Select all that apply.)"
  options={[
    'Leakage can occur between CV training and validation folds, not only between train and a final held-out test set',
    'Nested CV uses an inner CV loop for hyperparameter/feature-count selection and an outer CV loop to produce a final, unbiased score',
    'As long as you never touch the true test set until the very end, feature selection can safely be done on the full training+validation data beforehand with no risk',
    'Nested CV is generally more computationally expensive than a single CV loop because it multiplies inner and outer fold counts',
    'Fitting a StandardScaler on the full dataset before splitting is safe because scaling does not use the target variable'
  ]}
  correct={[0, 1, 3]}
/>
