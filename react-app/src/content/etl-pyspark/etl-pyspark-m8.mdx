import { ConceptBox, Bridge, CodeBlock, QuizCard, Recall } from '../../components/learning/index.js'

<ConceptBox title="TL;DR">
- `printSchema()` before anything else — column names, types, and nullability decide what operations are even legal.
- **`describe()` omits the median.** It gives count, mean, stddev, min, max only; use `summary()` or `approxQuantile()` for percentiles.
- Percentiles are *approximate* by default in Spark, because an exact median requires a full sort across partitions.
- Every inspection call is an **action** — it runs the whole lineage. On an expensive pipeline, cache before exploring.
</ConceptBox>

<Bridge>
In Module 7, we cleaned our messy dataset — removed nulls, duplicates, fixed casing, and filtered invalid values. Now we explore the clean data to understand what we're working with.
</Bridge>

## Schema Inspection

Before any analysis, know what you're working with. Schema inspection tells you column names, data types, and nullable flags.

<CodeBlock language="python">
{`students_df.printSchema()
# root
#  |-- student_id: integer (nullable = false)
#  |-- name: string (nullable = true)
#  |-- major: string (nullable = true)
#  |-- year: integer (nullable = true)
#  |-- gpa: double (nullable = true)

print("dtypes:", students_df.dtypes)
print("columns:", students_df.columns)`}
</CodeBlock>

## Descriptive Statistics

PySpark provides two methods for quick statistical summaries:

- `df.describe()` — count, mean, stddev, min, max for numeric columns
- `df.summary()` — everything in describe() plus percentiles (25%, 50%, 75%)

<CodeBlock language="python">
{`students_df.describe().show()
# +-------+----------+------+--------+
# |summary|student_id|  year|     gpa|
# +-------+----------+------+--------+
# |  count|        50|    50|      50|
# |   mean|   1025.50|  2.48|    3.45|
# | stddev|    14.58 |  1.12|    0.31|
# |    min|      1001|     1|    2.80|
# |    max|      1050|     4|    3.95|
# +-------+----------+------+--------+

# For percentiles, use summary()
students_df.summary().show()`}
</CodeBlock>

<ConceptBox title="describe() vs summary()">
`describe()` does not show the median (50th percentile) by default — use `summary()` for that. `summary()` also includes "25%" and "75%" rows, giving you the full IQR (interquartile range) for detecting outliers.
</ConceptBox>

## Value Counts and Distributions

For categorical columns, understand how values are distributed:

<CodeBlock language="python">
{`# Frequency count per category
students_df.groupBy("major").count().orderBy("count", ascending=False).show()
# +-----+-----+
# |major|count|
# +-----+-----+
# |   CS|   15|
# |   DS|   14|
# | Math|   11|
# | Stat|   10|
# +-----+-----+

# Count distinct values
print("Distinct years:", students_df.select("year").distinct().count())`}
</CodeBlock>

## Sampling

When working with large datasets, grab a subset for quick inspection:

<CodeBlock language="python">
{`# Random 20% sample (seed for reproducibility)
students_df.sample(fraction=0.2, seed=42).show()

# First 5 rows as list of Row objects
rows = students_df.take(5)

# Limit to 10 rows and display as table
students_df.limit(10).show()`}
</CodeBlock>

## Cross-tabulation

Cross-tabulation creates a pivot-table-like frequency matrix showing how two categorical variables relate:

<CodeBlock language="python">
{`# How many CS students are in year 3?
students_df.crosstab("major", "year").show()
# +-----------+---+---+---+---+
# |major_year |  1|  2|  3|  4|
# +-----------+---+---+---+---+
# |         CS|  3|  4|  4|  4|
# |         DS|  2|  4|  4|  4|
# |       Math|  3|  2|  3|  3|
# |       Stat|  2|  3|  3|  2|
# +-----------+---+---+---+---+`}
</CodeBlock>

<Recall items={[
  { q: "What does describe() give you, and what conspicuous statistic is missing?", a: "Count, mean, standard deviation, min and max. The median is absent. That matters because on skewed data the mean alone is misleading and the median is exactly the robust number you want — reach for summary(), which includes quartiles, or approxQuantile()." },
  { q: "Why is Spark's median approximate by default?", a: "An exact median requires knowing the global ordering, which means a full sort or a shuffle of the entire column across partitions. Spark instead uses a streaming quantile sketch with a tunable error bound, trading a small inaccuracy for avoiding a distributed sort. Set the relative error to 0 for an exact answer, at that cost." },
  { q: "Why check nullability in printSchema before analysing?", a: "Because nullable columns silently propagate nulls through arithmetic and comparisons, and aggregate functions skip them rather than failing. A mean computed over a column that is 30% null is the mean of the remaining 70% with no warning — you need to know that before quoting the number." },
  { q: "Your exploration calls feel slow on a long pipeline. What is happening and what fixes it?", a: "Every describe, show and count is an ACTION, so each one re-executes the entire lineage from the source files. Cache the cleaned DataFrame once before the exploration block, so the pipeline runs a single time and every subsequent inspection reads from memory." },
]} />

<QuizCard
  question="What does df.describe() NOT show by default?"
  options={['Mean', 'Standard deviation', 'Median (50th percentile)', 'Count']}
  correct={2}
/>
