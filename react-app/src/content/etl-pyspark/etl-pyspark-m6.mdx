import { ConceptBox, Bridge, PredictReveal, DerivationSteps, DerivationStep, CodeBlock, QuizCard, Recall } from '../../components/learning/index.js'

<Bridge>
In Module 5, we queried DataFrames using SQL syntax — SELECT, JOIN, GROUP BY, CTEs. These built-in operations handle most transformations. But when you need custom logic, you write a **UDF** (User-Defined Function).
</Bridge>

## What are UDFs?

<PredictReveal
  prompt="A built-in function and a Python user-defined function compute the same thing over a billion rows. Is the difference in speed small?"
  options={["Yes, it is the same arithmetic either way", "No — the Python function forces every row across a process boundary, which can be an order of magnitude slower"]}
  correct={1}
>

User-Defined Functions let you apply custom Python logic to DataFrame columns. Spark ships with a rich library of built-in functions — arithmetic, string manipulation, date handling, aggregations — but sometimes your business rules don't fit neatly into what's already available.

Common use cases include: converting numeric grades to letter grades, applying domain-specific categorizations (risk tiers, customer segments), computing custom metrics that combine multiple columns, or encoding business rules that would be awkward to express with nested `when()` chains.

</PredictReveal>

<Recall items={[
  { q: "Why are Python user-defined functions slow in a JVM-based engine?", a: "Each row must be serialised, sent to a separate Python process, deserialised, computed, and sent back. Built-in expressions execute inside the engine with no such crossing, and the optimiser can also reason about them, which it cannot do with opaque user code." },
]} />

## Creating UDFs — Two Approaches

### Approach 1: @udf decorator

<CodeBlock language="python">
{`from pyspark.sql.functions import udf
from pyspark.sql.types import StringType

@udf(returnType=StringType())
def grade_letter(numeric_grade):
    if numeric_grade >= 90: return 'A'
    elif numeric_grade >= 80: return 'B'
    elif numeric_grade >= 70: return 'C'
    elif numeric_grade >= 60: return 'D'
    else: return 'F'

result = enrollments_df.withColumn("letter_grade", grade_letter("grade"))
result.show(5)
# +-------------+-----------+-----+------------+
# |enrollment_id|course_name|grade|letter_grade|
# +-------------+-----------+-----+------------+
# |        1    |   Python  |   92|           A|
# |        2    |  Calculus |   88|           B|
# +-------------+-----------+-----+------------+`}
</CodeBlock>

### Approach 2: Register for SQL use

<CodeBlock language="python">
{`spark.udf.register("grade_letter", grade_letter, StringType())
spark.sql("""
    SELECT enrollment_id, course_name, grade, grade_letter(grade) as letter
    FROM enrollments
""").show(5)`}
</CodeBlock>

## Return Types Matter

<PredictReveal
  prompt="You declare a user-defined function as returning an integer, but it actually returns a string. What happens?"
  options={["An error is raised at definition time", "No error — the column silently fills with nulls, which is far harder to notice"]}
  correct={1}
>

Every UDF must declare its return type explicitly. Spark uses this to build the schema of the resulting DataFrame. If your function returns a value that doesn't match the declared type, you'll get a runtime error — often a cryptic serialization failure.

For complex return values, use `StructType` to define a multi-field schema:

<CodeBlock language="python">
{`from pyspark.sql.types import StructType, StructField, StringType

grade_schema = StructType([
    StructField("letter", StringType(), False),
    StructField("standing", StringType(), False)
])

@udf(returnType=grade_schema)
def grade_info(numeric_grade):
    if numeric_grade >= 90: return ('A', "Dean's List")
    elif numeric_grade >= 80: return ('B', 'Honor Roll')
    elif numeric_grade >= 70: return ('C', 'Good Standing')
    elif numeric_grade >= 60: return ('D', 'Probation')
    else: return ('F', 'Academic Hold')

result = enrollments_df.withColumn("grade_info", grade_info("grade"))
result.select("enrollment_id", "grade", "grade_info").show(5)
# +-------------+-----+-------------------+
# |enrollment_id|grade|       grade_info  |
# +-------------+-----+-------------------+
# |        1    |   92|{A, Dean's List}   |`}
</CodeBlock>

</PredictReveal>

<Recall items={[
  { q: "What happens if a user-defined function's declared return type does not match what it returns?", a: "The engine trusts the declaration and attempts the cast, which fails silently and produces nulls rather than an exception. The failure surfaces much later as mysteriously empty results, which is why the declared type deserves as much attention as the function body." },
]} />

## Performance Implications

<ConceptBox title="The Row-at-a-Time Penalty">
Regular UDFs come with a significant performance cost. For every row, Spark must:
1. Serialize the data from the JVM
2. Send it to a Python process
3. Execute your function
4. Serialize the result back to the JVM

This round-trip is orders of magnitude slower than Spark's built-in functions, which execute entirely within the JVM using optimized code generation.

Beyond serialization overhead, the **Catalyst optimizer cannot "see inside" a UDF**. When you use built-in functions, Spark can reorder operations, push filters down, and combine steps. With a UDF, the optimizer treats it as an opaque black box.

**Rule of thumb:** Prefer built-in functions whenever possible, and reserve UDFs for logic that truly cannot be expressed any other way.
</ConceptBox>

## Pandas UDFs (Vectorized)

Pandas UDFs (also called vectorized UDFs) address the performance problem by processing data in **batches** rather than one row at a time. Under the hood, Spark uses Apache Arrow to transfer entire columns of data between the JVM and Python as zero-copy arrays. Your function receives a `pd.Series` and returns a `pd.Series`.

Depending on the workload, Pandas UDFs can be **3x to 100x faster** than regular row-at-a-time UDFs.

<CodeBlock language="python">
{`from pyspark.sql.functions import pandas_udf
import pandas as pd

@pandas_udf(StringType())
def grade_letter_vec(grades: pd.Series) -> pd.Series:
    return grades.map(
        lambda g: 'A' if g >= 90 else 'B' if g >= 80
                  else 'C' if g >= 70 else 'D' if g >= 60 else 'F'
    )

result = enrollments_df.withColumn("letter_grade", grade_letter_vec("grade"))
result.show(5)`}
</CodeBlock>

<ConceptBox title="When to Use Each Approach">
| Approach | Speed | Optimizer-visible? | When to use |
|----------|-------|--------------------|-------------|
| Built-in functions | Fastest | Yes | Always prefer this |
| Pandas UDF | Fast | No | Batch-friendly custom logic |
| Regular UDF | Slow | No | Last resort for complex logic |
</ConceptBox>

<Recall items={[
  { q: "Give the two separate reasons a regular UDF is slow.", a: "Transport: every row is serialised from the JVM to a Python process and the result serialised back, so you pay per-row overhead on the whole dataset. Opacity: Catalyst cannot see inside arbitrary Python, so it cannot push a filter through the UDF or reorder around it — the step becomes an optimisation barrier." },
  { q: "Which of those two problems does a Pandas UDF solve, and which remains?", a: "It solves transport, by shipping columnar batches over Apache Arrow instead of one row at a time — usually an order of magnitude faster. Opacity remains: Catalyst still cannot reason about the function's semantics, so the plan around it is still constrained." },
  { q: "State the preference order and the reason for it.", a: "Built-in function first, since Catalyst understands it fully and it runs in the JVM. Then when/otherwise or expr for conditional logic, which are still native expressions. Then a Pandas UDF when you need real Python but want batched transport. A regular row-at-a-time UDF last, when nothing else expresses the logic." },
  { q: "You declare returnType=StringType() and your function returns 3.7. What happens?", a: "You get null, silently. Spark trusts the declared return type and does not verify it — a mismatch produces nulls rather than an exception, so a whole column can quietly become empty. Declared return types are a contract you must honour yourself." },
]} />

---

<ConceptBox title="TL;DR">
- A UDF runs your Python on each row — reach for it only when built-ins and `when()` chains genuinely can't express the rule.
- **UDFs are a Catalyst black box.** Spark can't see inside, so no pushdown, no reordering, no optimisation of that step.
- Worse, a regular UDF serialises every row across the JVM↔Python boundary. That round-trip is the real cost.
- **Pandas UDFs fix the transport**, not the opacity: Arrow ships batches instead of rows, typically an order of magnitude faster.
- Preference order: built-in function → `when()`/`expr` → Pandas UDF → regular UDF.
- `returnType` is mandatory and unchecked — declare `StringType()` and return a float and you get silent nulls.
</ConceptBox>

<QuizCard
  question="Why are Pandas UDFs faster than regular row-at-a-time UDFs?"
  options={['They use less memory because they skip type checking', 'They process data in batches using Apache Arrow serialization, avoiding per-row JVM-Python round-trips', 'They run on the driver node only, bypassing network overhead', 'They skip Catalyst optimization entirely']}
  correct={1}
/>
