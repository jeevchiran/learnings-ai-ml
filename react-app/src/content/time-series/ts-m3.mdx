import { ConceptBox, Bridge, PredictReveal, DerivationSteps, DerivationStep, QuizCard, Recall } from '../../components/learning/index.js'
import SmoothingWidget from '../../components/widgets/time-series/SmoothingWidget.jsx'

# Module 3: Exponential Smoothing — SES, Holt, Holt-Winters

<Bridge>
Module 2's baselines use exactly one past observation and throw the rest away. A moving average uses several but treats them as equals and ignores everything outside its window. This module keeps all of the history and simply cares about it less the further back it goes.
</Bridge>

## The Idea: Weight Recent Points More

<PredictReveal
  prompt="Simple exponential smoothing tracks a level and nothing else. What does its forecast look like ten steps into the future?"
  options={["It continues whatever direction the series was heading", "A flat line at the last level, because nothing in the model represents a direction"]}
  correct={1}
>

A moving average treats every point in its window equally and ignores everything outside it. **Exponential smoothing** instead gives *every* past point some weight — geometrically decaying, so recent points count most but nothing is thrown away entirely.

</PredictReveal>

<Recall items={[
  { q: "Why is a simple exponential smoothing forecast flat?", a: "The model carries a single state, the current level, and the forecast for every future step is that level. Nothing in it represents a slope, so it cannot extrapolate a direction. Adding a second smoothed component for the trend is exactly what Holt's method does." },
]} />

<DerivationSteps>
<DerivationStep number={1} title="Simple Exponential Smoothing (SES) — level only">
$$\hat y_{t+1} = l_t, \qquad l_t = \alpha y_t + (1-\alpha) l_{t-1}$$
One parameter, $\alpha \in (0,1)$. High $\alpha$ → trusts the newest point, chases noise. Low $\alpha$ → smooth, slow to react. Works for **flat series with no trend or seasonality**. The forecast is flat — the last level, repeated forever.
</DerivationStep>
<DerivationStep number={2} title="Holt's Linear — add a trend">
$$l_t = \alpha y_t + (1-\alpha)(l_{t-1}+b_{t-1}) \qquad b_t = \beta(l_t - l_{t-1}) + (1-\beta) b_{t-1}$$
$$\hat y_{t+h} = l_t + h\,b_t$$
A second smoothed component $b_t$ tracks the trend's slope. Forecasts now extend as a straight line instead of a flat one. Still can't handle a repeating seasonal pattern.
</DerivationStep>
<DerivationStep number={3} title="Holt-Winters — add seasonality">
$$l_t = \alpha(y_t - s_{t-m}) + (1-\alpha)(l_{t-1}+b_{t-1})$$
$$b_t = \beta(l_t-l_{t-1}) + (1-\beta) b_{t-1} \qquad s_t = \gamma(y_t - l_t) + (1-\gamma) s_{t-m}$$
A third component $s_t$ (period $m$) tracks the seasonal offset (additive form shown; multiplicative swaps $-$ for $/$ and $+$ for $\times$, same idea as Module 1). Three parameters now — $\alpha$ for level, $\beta$ for trend, $\gamma$ for seasonality.
</DerivationStep>
</DerivationSteps>

## How Are α, β, γ Actually Chosen?

<PredictReveal
  prompt="The smoothing parameter alpha sits between 0 and 1. You set it to 0.95. What behaviour have you asked for?"
  options={["Heavy smoothing, since the value is near 1", "Almost no smoothing — the forecast will chase the newest observation, noise included"]}
  correct={1}
>

Not by hand. In practice you **fit** them by minimizing in-sample squared error (equivalent to maximum likelihood under Gaussian errors) — the same grid-search-and-pick-the-best idea used everywhere else in ML, just over 1–3 parameters:

```python
from statsmodels.tsa.holtwinters import ExponentialSmoothing

model = ExponentialSmoothing(
    y, trend='add', seasonal='add', seasonal_periods=12
).fit()   # optimizes alpha, beta, gamma internally

print(model.params['smoothing_level'], model.params['smoothing_trend'], model.params['smoothing_seasonal'])
forecast = model.forecast(12)
```

</PredictReveal>

<PredictReveal
  prompt="How should the smoothing parameters be chosen in practice?"
  options={["By eye, adjusting until the curve looks right", "By fitting them, minimising in-sample squared error, which is maximum likelihood under Gaussian errors"]}
  correct={1}
>

<SmoothingWidget />

Slide alpha towards 1 in the widget and watch the smoothed line collapse onto the raw data; slide it towards 0 and watch it flatten into a near-constant. The fitted value is whichever point on that spectrum minimises error on the history you have.

</PredictReveal>

<Recall items={[
  { q: "What does a high smoothing parameter do, and how is the value chosen?", a: "A value near 1 puts almost all the weight on the newest observation, so the model reacts instantly and tracks noise; a value near 0 averages over a long history and reacts slowly. The value is not chosen by eye but fitted, by minimising in-sample squared error, which under Gaussian errors is maximum likelihood." },
]} />

<ConceptBox title="Common confusion — which variant do I need?">
Ask two questions: does the series **trend**? does it have **fixed-period seasonality**? No/no → SES. Yes/no → Holt. Yes/yes → Holt-Winters. Picking a model with components the data doesn't have just adds noisy, badly-estimated parameters; picking one with too few underfits systematically (e.g. SES on a trending series always forecasts flat and falls behind).
</ConceptBox>

---

<ConceptBox title="TL;DR">
- A moving average uses a hard window; exponential smoothing gives **every** past point a geometrically decaying weight.
- Three variants, one per component: **SES** = level only (flat forecast), **Holt** = level + trend (straight-line forecast), **Holt-Winters** = level + trend + seasonality.
- Pick by asking two questions: does it trend? does it have fixed-period seasonality?
- $\alpha,\beta,\gamma$ are **fitted** by minimising in-sample squared error, not chosen by hand. High $\alpha$ chases noise, low $\alpha$ reacts slowly.
</ConceptBox>

## Practice

**Problem 1.** A series is flat with random fluctuations, no trend, no seasonality. Which method, and roughly what α?

**Solution:** SES, with a **low** α (e.g. 0.1–0.3) — the true signal isn't moving, so you want to smooth out noise, not chase it.

**Problem 2.** Your Holt-Winters `fit()` returns γ ≈ 0.02. What does that tell you about the seasonal pattern?

**Solution:** Near-zero γ means the seasonal component barely updates from new data — the fitted seasonal shape from early in the series is treated as essentially fixed/reliable throughout.

<Recall items={[
  { q: "Expand l_t = alpha*y_t + (1-alpha)*l_{t-1} one step and say where the 'exponential' comes from.", a: "Substituting l_{t-1} gives alpha*y_t + alpha(1-alpha)*y_{t-1} + (1-alpha)^2 * l_{t-2}, and continuing shows the weight on a point k steps back is alpha*(1-alpha)^k — a geometric, i.e. exponentially decaying, sequence that never quite reaches zero." },
  { q: "Two questions pick your variant. What are they, and what does each answer buy you?", a: "Does the series trend? If yes you need Holt's b_t component, which turns a flat forecast into a sloped line. Does it have fixed-period seasonality? If yes you need Holt-Winters' s_t component, which adds a repeating per-period offset. No to both means SES suffices." },
  { q: "Someone runs SES on a strongly trending series. Describe the failure precisely.", a: "SES forecasts the last level, flat, forever. On an upward trend that is a systematically increasing under-forecast — the error grows linearly with the horizon rather than staying bounded. It is a bias, not noise, so no amount of data fixes it." },
  { q: "Why is fitting alpha, beta and gamma by minimising squared error the same as maximum likelihood?", a: "Under the assumption that the one-step errors are independent Gaussians with constant variance, the log-likelihood is a negative constant times the sum of squared errors. Maximising one is minimising the other — the same connection Module 8 of the maths track draws for MSE generally." },
]} />

<QuizCard
  question="A series has a clear upward trend but no repeating seasonal pattern. Which exponential smoothing variant is the right minimum fit?"
  options={[
    'Simple Exponential Smoothing (SES)',
    "Holt's Linear method",
    'Holt-Winters (additive)',
    'None — exponential smoothing cannot handle a trend'
  ]}
  correct={1}
/>
