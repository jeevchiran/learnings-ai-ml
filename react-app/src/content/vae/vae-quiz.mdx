# Track Quiz — Variational Autoencoders (VAEs)

import { ConceptBox, QuizCard, MultiSelectQuiz } from '../../components/learning/index.js'

<ConceptBox title="End-of-track checkpoint">
Fifteen questions plus three multi-selects spanning the complete VAE curriculum: latent space geometry, probabilistic generative modeling, ELBO derivation, Gaussian KL analytical calculations, the reparameterization trick, reconstruction likelihood assumptions, PyTorch training dynamics, latent space navigation, β-VAE disentanglement, and Conditional VAEs. Work through the calculations before submitting your answers!
</ConceptBox>

## Single-Choice Questions

<QuizCard
  question="Why does a standard deterministic autoencoder fail to generate realistic new samples when given random latent inputs z ~ N(0, I)?"
  options={[
    "Because its encoder weights are initialized with orthogonal matrices.",
    "Because standard autoencoders lack prior regularization, leaving empty gaps and unmapped regions in latent space where the decoder outputs noise.",
    "Because standard autoencoders cannot use non-linear activation functions in the decoder.",
    "Because reconstruction loss enforces that all latent vectors sum to zero."
  ]}
  correct={1}
/>

<QuizCard
  question="Why is the true posterior distribution p_θ(z|x) intractable to compute directly in latent variable models?"
  options={[
    "Because it requires dividing by the marginal likelihood p_θ(x) = ∫ p_θ(x|z)p(z)dz, which is an intractable high-dimensional integral.",
    "Because the normal prior p(z) has zero variance.",
    "Because Bayes' rule does not apply to continuous random variables.",
    "Because the decoder parameters θ are non-deterministic."
  ]}
  correct={0}
/>

<QuizCard
  question="In the ELBO decomposition log p_θ(x) = ELBO(θ, ϕ; x) + D_KL(q_ϕ(z|x) || p_θ(z|x)), what is the relationship between maximizing the ELBO with respect to ϕ and the approximation gap?"
  options={[
    "Maximizing ELBO increases the approximation gap because the encoder gains variance.",
    "Maximizing ELBO directly minimizes the KL divergence gap between the variational posterior q_ϕ(z|x) and the true posterior p_θ(z|x).",
    "The approximation gap is completely independent of the encoder parameters ϕ.",
    "Maximizing ELBO forces the decoder weights θ to zero."
  ]}
  correct={1}
/>

<QuizCard
  question="For a single latent dimension with q(z|x) = N(μ=1.0, σ²=4.0) (so log(σ²) = log(4) ≈ 1.3863) and prior p(z) = N(0, 1), what is the exact analytical KL divergence D_KL(q || p)?"
  options={[
    "1.3069",
    "0.5000",
    "2.5000",
    "0.0000"
  ]}
  correct={0}
/>

<QuizCard
  question="Why does the encoder network predict log-variance log(σ²) rather than standard deviation σ directly?"
  options={[
    "Because log(σ²) is unconstrained in (-∞, +∞), preventing illegal negative variances during unconstrained gradient descent.",
    "Because computing exponentials is faster on GPUs than computing additions.",
    "Because log-variance eliminates the need for activation functions.",
    "Because standard deviation requires complex numbers."
  ]}
  correct={0}
/>

<QuizCard
  question="What is the mathematical formulation of the reparameterization trick for a Gaussian latent variable?"
  options={[
    "z = μ · ε + σ, where ε ~ Uniform(0, 1)",
    "z = μ + σ ⊙ ε, where ε ~ 𝒩(0, I)",
    "z = Sigmoid(μ + σ)",
    "z = argmax(q_ϕ(z|x))"
  ]}
  correct={1}
/>

<QuizCard
  question="Why does the pathwise reparameterization trick exhibit dramatically lower gradient variance than the REINFORCE score-function estimator?"
  options={[
    "Because pathwise derivatives directly evaluate the gradient of the decoder output with respect to z (∂Loss/∂z), utilizing exact loss surface curvature.",
    "Because reparameterization removes all stochasticity from the model.",
    "Because REINFORCE cannot be implemented in PyTorch.",
    "Because the score function requires inverting the covariance matrix."
  ]}
  correct={0}
/>

<QuizCard
  question="Which reconstruction loss corresponds to assuming a multivariate Bernoulli likelihood for the decoder output p_θ(x|z) on normalized images x ∈ [0, 1]?"
  options={[
    "Mean Squared Error (MSE)",
    "Binary Cross-Entropy (BCE)",
    "Huber Loss",
    "Cosine Similarity Loss"
  ]}
  correct={1}
/>

<QuizCard
  question="You train a VAE on 28x28 MNIST images (784 pixels). If you accidentally compute BCE with reduction='mean' (~0.05) while computing KL divergence summed across 20 latent dimensions (~15.0), what happens?"
  options={[
    "The model trains perfectly with higher resolution.",
    "Severe over-regularization / posterior collapse occurs because the reconstruction term is downscaled 784-fold relative to the KL penalty.",
    "The decoder completely ignores the prior and overfits.",
    "The model gradients explode to infinity on the first step."
  ]}
  correct={1}
/>

<QuizCard
  question="During inference (generating a batch of brand-new, unseen images), what is the correct workflow?"
  options={[
    "Pass random images into the encoder, extract μ, and decode.",
    "Sample z directly from the prior p(z) = 𝒩(0, I) and pass z through the decoder p_θ(x|z); the encoder is not used.",
    "Run the reparameterization trick with μ=1 and σ=0.",
    "Optimize z via gradient descent for 100 iterations."
  ]}
  correct={1}
/>

<QuizCard
  question="In high-dimensional latent space (e.g. d = 64), why is Spherical Linear Interpolation (Slerp) preferred over Linear Interpolation (Lerp) when interpolating between two vectors?"
  options={[
    "Because Slerp keeps the interpolation trajectory along the constant-radius hypersphere surface where Gaussian probability mass resides (avoiding low-norm density voids).",
    "Because Lerp produces complex imaginary numbers in dimensions > 2.",
    "Because Slerp requires 50% fewer floating point operations.",
    "Because Lerp cannot be computed with PyTorch tensors."
  ]}
  correct={0}
/>

<QuizCard
  question="In β-VAE, setting β > 1 (e.g. β = 4) encourages which specific representation property?"
  options={[
    "Pixel-perfect reconstruction of sharp high-frequency edges.",
    "Statistically independent, axis-aligned, disentangled latent factors of variation.",
    "Faster sampling speed during inference.",
    "Elimination of the decoder neural network."
  ]}
  correct={1}
/>

<QuizCard
  question="What is 'Posterior Collapse' in variational autoencoders?"
  options={[
    "When the decoder weights overflow to NaN.",
    "When the encoder outputs q_ϕ(z|x) ≈ p(z) for all inputs, carrying zero mutual information, causing the decoder to ignore z and output average blurred images.",
    "When the latent dimension d is set to 0.",
    "When the learning rate is too small."
  ]}
  correct={1}
/>

<QuizCard
  question="How does a Conditional VAE (CVAE) enable targeted, class-specific sample generation?"
  options={[
    "By training 10 separate VAE models, one for each class.",
    "By conditioning both the encoder q_ϕ(z|x, y) and decoder p_θ(x|z, y) on the class label y, allowing z to capture intra-class style while y controls class identity.",
    "By applying a softmax classifier to the latent code z.",
    "By adding class labels directly to the reconstructed pixels."
  ]}
  correct={1}
/>

<QuizCard
  question="Why do Vector Quantized VAEs (VQ-VAEs) completely avoid posterior collapse and Gaussian blurriness?"
  options={[
    "Because they replace the continuous Gaussian distribution and KL divergence with a discrete learned codebook using vector quantization.",
    "Because they do not have a decoder network.",
    "Because they use adversarial discriminator losses instead of reconstruction loss.",
    "Because they only train on binary black-and-white datasets."
  ]}
  correct={0}
/>

## Multi-Select Questions

<MultiSelectQuiz
  question="Which of the following statements about the ELBO and Variational Inference are TRUE? (Select all that apply)"
  options={[
    "The ELBO is a rigorous mathematical lower bound on the marginal log-likelihood log p(x).",
    "The ELBO equals log p(x) if and only if the approximate posterior q_ϕ(z|x) perfectly matches the true posterior p_θ(z|x).",
    "Maximizing the ELBO with respect to encoder parameters ϕ minimizes the KL divergence to the true posterior.",
    "The ELBO can only be computed if the true posterior p_θ(z|x) is known analytically."
  ]}
  correct={[0, 1, 2]}
/>

<MultiSelectQuiz
  question="Which techniques help prevent Posterior Collapse when training deep VAEs? (Select all that apply)"
  options={[
    "KL Annealing / Warmup (gradually increasing β from 0 to 1 over initial training epochs).",
    "Free Bits / Minimum KL constraint (enforcing a minimum KL threshold per latent dimension).",
    "Setting β = 100 on epoch 1 to enforce immediate prior matching.",
    "Using an overly powerful autoregressive decoder that can model pixels without any latent conditioning."
  ]}
  correct={[0, 1]}
/>

<MultiSelectQuiz
  question="Which of the following are advantages of VAEs compared to Generative Adversarial Networks (GANs)? (Select all that apply)"
  options={[
    "VAEs have stable, principled likelihood-based training without minimax game instability.",
    "VAEs provide an explicit probabilistic inference model (encoder) to map observed images to latent vectors.",
    "VAEs have excellent mode coverage and do not suffer from catastrophic mode collapse.",
    "VAEs consistently generate crisper, sharper high-frequency details than state-of-the-art GANs."
  ]}
  correct={[0, 1, 2]}
/>
