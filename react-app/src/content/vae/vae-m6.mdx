import { ConceptBox, Bridge, PredictReveal, QuizCard, MultiSelectQuiz, Recall } from '../../components/learning/index.js'
import ReparameterizationWidget from '../../components/widgets/vae/ReparameterizationWidget.jsx'

# Module 6: The Reparameterization Trick

<ConceptBox title="The Core Engineering Breakthrough">
The reparameterization trick makes backpropagation through stochastic latent layers possible. By expressing the random variable $z \sim \mathcal{N}(\mu, \sigma^2)$ as a deterministic function with an external noise input:

$$z = g_\phi(x, \epsilon) = \mu + \sigma \odot \epsilon, \qquad \text{where } \epsilon \sim \mathcal{N}(0, I)$$

gradients $\nabla_\phi \mathbb{E}_{q_\phi(z|x)}[f(z)]$ can flow smoothly through $\mu$ and $\sigma$ into the encoder parameters $\phi$.
</ConceptBox>

<Bridge>
Module 5 derived the analytical KL loss. However, to evaluate and backpropagate through the reconstruction loss $\mathbb{E}_{q_\phi(z|x)}[\log p_\theta(x|z)]$, the model must sample a latent vector $z$. This module solves the fundamental problem of backpropagating through random sampling.
</Bridge>

## The Problem: Stochastic Bottleneck

Suppose we implement sampling directly:

```python
# Non-differentiable direct sampling
z = torch.normal(mean=mu, std=sigma) # ❌ Breaks computational graph!
```

During the forward pass, this works fine. But during backpropagation, autograd needs to compute:

$$\frac{\partial \mathcal{L}_{\text{recon}}}{\partial \phi} = \frac{\partial \mathcal{L}_{\text{recon}}}{\partial z} \cdot \frac{\partial z}{\partial \phi}$$

What is $\frac{\partial z}{\partial \mu}$ when $z$ is drawn from a random number generator? **It is undefined!** The random sampling operation is a non-differentiable stochastic node that severs the gradient path.

```
       Non-Differentiable Graph:
       
       x ──► [ Encoder ϕ ] ──► (μ, σ²) ──► [ Random Sample z ] ──► [ Decoder θ ] ──► x̂
                                                │
                                    ❌ Gradient cannot cross here
```

## The Score-Function Estimator Alternative (and Why It Fails)

One classical alternative is the **REINFORCE / Score-Function estimator**:

$$\nabla_\phi \mathbb{E}_{q_\phi(z|x)}[f(z)] = \mathbb{E}_{q_\phi(z|x)}\big[ f(z) \nabla_\phi \log q_\phi(z|x) \big]$$

While mathematically unbiased, the variance of this estimator is **extremely high**. In deep neural networks, REINFORCE gradients are so noisy that training a VAE takes millions of iterations or fails to converge entirely.

## The Reparameterization Solution

<PredictReveal
  prompt="The randomness is what breaks the gradient path. We cannot remove it — the model genuinely needs to sample. What is the one remaining option?"
  options={[
    "Approximate the derivative of the sampling operation numerically",
    "Keep the randomness but move it OUT of the path, so it enters as an external input rather than as a step inside the computation"
  ]}
  correct={1}
>

If the random draw happens *outside* the network and arrives as just another input value, then everything inside the network is an ordinary deterministic calculation — and ordinary deterministic calculations are exactly what autograd handles. The sampling still happens on every forward pass; it simply stops sitting between the encoder and the loss.

</PredictReveal>

The reparameterization trick isolates the stochasticity into an **independent, parameter-free noise variable** $\epsilon \sim \mathcal{N}(0, I)$:

$$z = \mu(x) + \sigma(x) \odot \epsilon$$

```
       Reparameterized Differentiable Graph:
       
                 ε ~ 𝒩(0, I)  (Independent noise)
                     │
                     ▼
       x ──► [ Encoder ϕ ] ──► (μ, σ) ──► [ z = μ + σ ⊙ ε ] ──► [ Decoder θ ] ──► x̂
                                                 ▲
                                                 │
                                 ✓ Differentiable deterministic node
```

Now, $z$ is a completely deterministic, differentiable function of $\mu$ and $\sigma$:

$$\frac{\partial z}{\partial \mu} = 1, \qquad \frac{\partial z}{\partial \sigma} = \epsilon$$

Since $\sigma = \exp(0.5 \times \text{logvar})$, the derivative with respect to `logvar` is:

$$\frac{\partial z}{\partial \text{logvar}} = \frac{\partial z}{\partial \sigma} \cdot \frac{\partial \sigma}{\partial \text{logvar}} = \epsilon \cdot \left(0.5 \cdot \exp(0.5 \times \text{logvar})\right) = 0.5 \cdot \sigma \cdot \epsilon$$

## Interactive Computational Graph

Explore the interactive graph below. Toggle between direct sampling and the reparameterization trick, then simulate the backward pass:

<ReparameterizationWidget />

## PyTorch Implementation

In PyTorch, the reparameterization trick is implemented in just three lines:

```python
def reparameterize(mu, logvar):
    """
    Reparameterization trick: sample eps ~ N(0, I), return z = mu + std * eps
    """
    std = torch.exp(0.5 * logvar) # sigma = sqrt(exp(logvar))
    eps = torch.randn_like(std)    # sample noise with same shape and device
    return mu + eps * std          # pathwise differentiable latent code
```

<Recall items={[
  { q: "What is the reparameterization trick formula?", a: "z = μ + σ ⊙ ε, where ε ~ 𝒩(0, I) is standard Gaussian noise." },
  { q: "Why is the pathwise reparameterization trick preferred over REINFORCE?", a: "Because pathwise gradients exploit the exact derivative of the decoder with respect to z, yielding dramatically lower gradient variance and enabling rapid, stable gradient descent training." }
]} />

---

<ConceptBox title="TL;DR">
- Sampling $z$ is required to evaluate the reconstruction term, but a raw sampling operation is a **dead end for gradients** — there is no derivative of "draw a random number" with respect to $\mu$.
- The **reparameterisation trick** moves the randomness out of the path: draw $\epsilon \sim \mathcal{N}(0, I)$ *outside* the network, then compute $z = \mu + \sigma \odot \epsilon$, which is an ordinary differentiable expression.
- Gradients now flow: $\partial z / \partial \mu = 1$ and $\partial z / \partial \sigma = \epsilon$. The sampling still happens, but it happens to an input rather than inside the computation.
- The alternative (REINFORCE / score-function estimator) is unbiased but so high-variance that VAE training effectively fails with it.
</ConceptBox>

## Summary Check

<QuizCard
  question="During evaluation or inference (generating new images from the prior), is the reparameterization trick used?"
  options={[
    "No, during pure generation we directly sample z ~ 𝒩(0, I) and pass it to the decoder; encoder and reparameterization are bypassed.",
    "Yes, reparameterization is always required for every forward pass.",
    "No, the decoder outputs a random seed instead."
  ]}
  correct={0}
/>

<Recall items={[
  { q: "Check your reasoning", a: "During generation, we sample z directly from the standard normal prior p(z) = 𝒩(0, I) and pass it through the decoder p_θ(x|z). The encoder and reparameterization trick are only used during training and posterior inference." }
]} />
