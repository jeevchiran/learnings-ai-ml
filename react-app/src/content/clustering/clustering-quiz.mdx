# Track Quiz — Clustering

import { ConceptBox, Bridge, QuizCard, MultiSelectQuiz } from '../../components/learning/index.js'

<ConceptBox title="End-of-track checkpoint">
This quiz covers all four modules of the Clustering track — from K-means intuition and distance metrics, through WCSS/inertia and the elbow method, to silhouette scoring and cluster quality evaluation. Several questions require short numerical calculations; work through them carefully before selecting an answer.
</ConceptBox>

## Questions

<QuizCard
  question="K-means clustering is described as an unsupervised learning algorithm. What does 'unsupervised' mean in this context?"
  options={[
    "The algorithm does not use a distance metric to assign points to clusters",
    "The training data has no class labels — the algorithm finds structure from the feature values alone",
    "The algorithm requires the user to supervise each iteration manually",
    "The algorithm cannot be applied to high-dimensional data without label guidance"
  ]}
  correct={1}
/>

<QuizCard
  question="What is the correct order of the four steps in the K-means algorithm?"
  options={[
    "Initialize centroids → compute WCSS → assign points → update centroids",
    "Initialize centroids → assign each point to its nearest centroid → update centroids to the mean of their cluster → repeat until convergence",
    "Assign points randomly → compute distances → initialize centroids → update centroids",
    "Choose K → compute pairwise distances → build a dendrogram → cut at K clusters"
  ]}
  correct={1}
/>

<QuizCard
  question="Point A = (1, 2) and Point B = (4, 6). What are the Euclidean and Manhattan distances between A and B?"
  options={[
    "Euclidean = 5, Manhattan = 7",
    "Euclidean = 7, Manhattan = 5",
    "Euclidean = 25, Manhattan = 7",
    "Euclidean = 5, Manhattan = 5"
  ]}
  correct={0}
/>

<MultiSelectQuiz
  question="Which of the following statements about distance metrics used in clustering are correct? (Select all that apply.)"
  options={[
    "Euclidean distance is computed as √(Σ(xᵢ − yᵢ)²) over all dimensions",
    "Manhattan distance sums the absolute differences in each dimension: Σ|xᵢ − yᵢ|",
    "Cosine similarity measures the angle between two vectors and ranges from −1 to 1",
    "Euclidean distance is always smaller than Manhattan distance for the same pair of points",
    "When features are on very different scales, Euclidean distance can be dominated by the largest-magnitude feature"
  ]}
  correct={[0, 1, 2, 4]}
/>

<QuizCard
  question="A cluster contains three points: (1, 1), (3, 1), and (2, 3). What are the coordinates of the centroid of this cluster?"
  options={[
    "(2, 1.5)",
    "(2, 1.67)",
    "(1.5, 2)",
    "(3, 2)"
  ]}
  correct={1}
/>

<QuizCard
  question="After K-means converges, each point is assigned to the cluster whose centroid is closest to it. What geometric property does this create for the cluster boundaries?"
  options={[
    "Curved boundaries that follow the density of the data",
    "Axis-aligned rectangular boundaries determined by feature thresholds",
    "Linear (straight-line) boundaries — specifically, the perpendicular bisectors between centroid pairs, forming a Voronoi diagram",
    "Boundaries that always pass through the origin of the feature space"
  ]}
  correct={2}
/>

<QuizCard
  question="WCSS (Within-Cluster Sum of Squares) is defined as Σₖ Σᵢ∈Cₖ ‖xᵢ − μₖ‖². What happens to WCSS as K increases from 1 to N (number of data points)?"
  options={[
    "WCSS increases monotonically because more clusters create more within-cluster variation",
    "WCSS always decreases monotonically, reaching 0 when K = N (each point is its own cluster)",
    "WCSS first decreases then increases, with a minimum at the true number of clusters",
    "WCSS remains constant once K exceeds the number of natural clusters in the data"
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="Which of the following correctly describe the elbow method for choosing K? (Select all that apply.)"
  options={[
    "You plot WCSS against K and look for the point where the rate of decrease sharply slows — the 'elbow'",
    "The elbow represents a point of diminishing returns: adding more clusters gives little benefit in compactness",
    "K = 1 gives the maximum possible WCSS because all points are measured against a single global centroid",
    "The elbow method always identifies a unique, mathematically exact optimal K",
    "The method can be used alongside the silhouette score to validate the chosen K"
  ]}
  correct={[0, 1, 2, 4]}
/>

<QuizCard
  question="Point p has an average intra-cluster distance a(p) = 2.0 and an average distance to the nearest other cluster b(p) = 5.0. What is the silhouette score s(p)?"
  options={[
    "0.40",
    "0.60",
    "−0.60",
    "2.50"
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="Which of the following statements about the silhouette score are correct? (Select all that apply.)"
  options={[
    "s(i) = (b(i) − a(i)) / max(a(i), b(i))",
    "A score close to +1 means the point is well-matched to its own cluster and poorly matched to neighboring clusters",
    "A score close to 0 means the point is near the boundary between two clusters",
    "A score close to −1 means the point is probably assigned to the wrong cluster",
    "The silhouette score can only be computed for K = 2"
  ]}
  correct={[0, 1, 2, 3]}
/>

<QuizCard
  question="You run K-means five times with random initialization and get noticeably different final WCSS values each time. What is the most likely cause, and what is the standard remedy?"
  options={[
    "K is too small; increase K until results stabilize",
    "K-means converges to different local minima depending on the initial centroid placement; remedy is to run multiple restarts and keep the result with the lowest WCSS, or use K-means++ initialization",
    "WCSS is computed incorrectly; use Euclidean distance squared instead",
    "The dataset needs to be normalized before K-means will give consistent results"
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="A data scientist is comparing K-means clustering results for K=3 and K=5 on the same dataset. Which evaluation approaches are valid for choosing between them without using class labels? (Select all that apply.)"
  options={[
    "Compare the WCSS values — but remember that K=5 will always have lower WCSS regardless of cluster quality",
    "Compute the average silhouette score for each K and prefer the K with the higher average score",
    "Use the elbow plot to visually assess which K sits at the point of diminishing WCSS reduction",
    "Choose K=5 because more clusters always yield a more accurate model",
    "Visualize the cluster assignments (e.g., via PCA or t-SNE) and assess whether the clusters appear cohesive and well-separated"
  ]}
  correct={[0, 1, 2, 4]}
/>

<QuizCard
  question="Cosine similarity between two document vectors is 0.95. What does this value indicate about the documents, and how does cosine similarity differ from Euclidean distance in this context?"
  options={[
    "The documents are nearly orthogonal (unrelated); cosine similarity penalizes documents of different lengths more than Euclidean distance does",
    "The documents are very similar in direction (topic/content); cosine similarity focuses on the angle between vectors and ignores magnitude, making it length-invariant unlike Euclidean distance",
    "The documents are identical; cosine similarity of 0.95 is the maximum possible value",
    "The Euclidean distance between the document vectors must also be close to 0 when cosine similarity is 0.95"
  ]}
  correct={1}
/>
