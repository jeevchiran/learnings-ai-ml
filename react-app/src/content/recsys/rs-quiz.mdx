# Track Quiz — Recommender Systems

import { ConceptBox, QuizCard, MultiSelectQuiz } from '../../components/learning/index.js'

<ConceptBox title="End-of-track checkpoint">
Sixteen questions across both parts — funnel architecture, implicit signals, event contracts, ranking metrics, splits and leakage, baselines, collaborative filtering, implicit MF, learning to rank, features, negative sampling, cold start, and the build pipeline. Several need a short calculation; the reference data is the same NovaCart store used throughout. Work the numeric ones out before answering.
</ConceptBox>

## Questions

<QuizCard
  question="Why is the retrieval stage scored on recall@500 while the ranking stage is scored on NDCG@10?"
  options={[
    'Recall is cheaper to compute than NDCG',
    'Retrieval can only fail by dropping a good item — and nothing downstream can recover it — while ranking has the survivors in hand and can only mis-order them',
    'NDCG is undefined for candidate sets larger than 100',
    'Because retrieval uses embeddings and ranking uses trees'
  ]}
  correct={1}
/>

<QuizCard
  question="A shopper does not click a product shown at position 30. In implicit-feedback terms, what have you observed?"
  options={[
    'A confident negative — they saw it and rejected it',
    'Almost nothing: they most likely never scrolled that far, so absence of a click is not evidence of dislike',
    'A positive, since the impression means engagement',
    'A missing value that must be imputed from the user mean'
  ]}
  correct={1}
/>

<QuizCard
  question="Chen viewed the Laptop Stand (w=1), added it to the cart (w=3), and bought it (w=10). With α=1, what are r, p and c for that cell?"
  options={[
    'r = 10, p = 10, c = 10',
    'r = 14, p = 1, c = 15',
    'r = 14, p = 14, c = 1',
    'r = 3, p = 1, c = 4'
  ]}
  correct={1}
/>

<QuizCard
  question="Why does implicit ALS sum its loss over EVERY cell of the user×item matrix, rather than only the observed ones?"
  options={[
    'To make the matrix dense enough for the linear solver',
    'Because every observed cell is a positive — a model trained on those alone could output 1 everywhere at zero loss, so the zeros must participate as weak negatives',
    'Because unobserved cells are known to be negatives with certainty',
    'To keep the objective convex in X and Y jointly'
  ]}
  correct={1}
/>

<QuizCard
  question="Your mobile app queues events offline. You build 'last 5 items viewed' by sorting on ingest_ts. What goes wrong?"
  options={[
    'Nothing — ingest_ts is the more reliable server clock',
    'The reconstructed sequence is not the order the shopper acted in, so every sequence feature is wrong for exactly the mobile users you most want to model',
    'Duplicate events are no longer removed',
    'The catalog join fails for delisted items'
  ]}
  correct={1}
/>

<QuizCard
  question="A slate has relevant items at ranks 2, 4 and 6. What is NDCG@3? (Use DCG@3 = 1/log₂3 = 0.631 and IDCG@3 = 1 + 0.631 + 0.5.)"
  options={[
    '0.631',
    '0.296',
    '0.333',
    '0.500'
  ]}
  correct={1}
/>

<QuizCard
  question="You hold out exactly one purchase per shopper. Why does reporting both MRR and NDCG@10 add almost no information?"
  options={[
    'MRR is undefined when there is one relevant item',
    'With |R| = 1 the IDCG is 1, so NDCG@k reduces to 1/log₂(rank+1) — a monotone transform of the same rank MRR reads',
    'NDCG ignores the first hit by construction',
    'They differ only when coverage is below 50%'
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="Which of these are genuine problems with a random 80/20 split of an interaction log?"
  options={[
    'The model can train on later events and be graded on earlier ones',
    'Item popularity is computed over a window that includes the test period',
    'Events from one session land on both sides of the split',
    'It produces test sets that are too small to be reliable'
  ]}
  correct={[0, 1, 2]}
/>

<QuizCard
  question="On NovaCart, 'recently viewed' scores NDCG@3 = 0.438 with 89% coverage; 'most popular' scores 0.377 with 33%. Your new model scores 0.40. What is the correct read?"
  options={[
    'Ship it — it beats the popularity baseline, which is the standard bar',
    'It has learned something, but a GROUP BY on the event log still beats it, so it is not yet earning its serving cost',
    'The evaluation is broken, since a trained model cannot lose to a heuristic',
    'Switch to MRR, which will rank the model higher'
  ]}
  correct={1}
/>

<QuizCard
  question="Item-item cosine gives sim(Earbuds, Keyboard) = 0.816 on NovaCart, higher than the intuitive Yoga Mat / Steel Bottle pair. Best explanation and fix?"
  options={[
    'Cosine is the wrong metric; use Jaccard instead',
    'Two broad-history shoppers touched both, so the similarity has almost no support behind it — apply a minimum co-occurrence threshold and shrink low-support pairs toward zero',
    'The matrix should have been binarised first',
    'Earbuds and Keyboards are genuinely similar products'
  ]}
  correct={1}
/>

<QuizCard
  question="Why does a brand-new product get a similarity of exactly 0 to every other product under item-item CF?"
  options={[
    'Because new items are excluded by the minimum-support filter',
    'Its column in the interaction matrix is all zeros, so every cosine numerator is 0 — CF has no signal about it at all',
    'Because its price has not been normalised',
    'Because cosine is undefined for new items and defaults to 0'
  ]}
  correct={1}
/>

<QuizCard
  question="In implicit ALS, what does the Gram trick (precomputing YᵀY once per sweep) actually buy?"
  options={[
    'It makes the loss decrease monotonically',
    'It reduces each user solve from O(n·k²) — a full pass over the catalog — to O(nᵤ·k²) over that user\'s own items, while still summing the objective over every cell',
    'It removes the need to choose a regularisation λ',
    'It guarantees convergence to the global optimum'
  ]}
  correct={1}
/>

<QuizCard
  question="You add a constant to every score in a slate. Pairwise and listwise loss are unchanged; pointwise loss changes a lot. What does that show?"
  options={[
    'The pointwise implementation has a bug',
    'Pairwise and listwise depend only on score differences, so they measure order; pointwise depends on absolute values because it targets a calibrated probability',
    'Listwise loss is insensitive to the ranking',
    'Only pointwise loss is suitable for ranking tasks'
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="Which of these are point-in-time correctness violations when building training features?"
  options={[
    'Computing user_30d_purchases with one GROUP BY over the whole event table',
    'Joining today\'s catalog snapshot (price, return_rate) onto year-old label rows',
    'Using `<=` instead of `<` when the label shares a timestamp with an event',
    'Using a 30-day lookback window instead of a 7-day one'
  ]}
  correct={[0, 1, 2]}
/>

<QuizCard
  question="Your team switches from popularity-sampled to purely hard negatives mined from the catalog. Training loss drops fast; online CTR falls. Most likely cause?"
  options={[
    'The learning rate needs reducing to match harder examples',
    'Many hard negatives are false negatives — items the shopper never saw — so the model is trained that its own correct recommendations are wrong',
    'Hard negatives require a listwise loss to work',
    'The negative-to-positive ratio must always be 1:1'
  ]}
  correct={1}
/>

<QuizCard
  question="In-batch negatives are free, but need a logQ correction. Why?"
  options={[
    'Because batches are sampled without replacement',
    'An item appears in a batch in proportion to how often it is somebody\'s positive — i.e. in proportion to popularity — so sampling is implicitly β=1 and popular items are over-penalised',
    'Because the softmax is numerically unstable without it',
    'Because in-batch negatives are always false negatives'
  ]}
  correct={1}
/>
