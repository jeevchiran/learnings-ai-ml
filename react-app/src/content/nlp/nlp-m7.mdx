import { ConceptBox, Bridge, PredictReveal, DerivationSteps, DerivationStep, CodeBlock, QuizCard, Recall } from '../../components/learning/index.js'
import NERWidget from '../../components/widgets/nlp/NERWidget.jsx'

# NER, IOB, CRF, SRL & Coreference

<Bridge>
We can now tag words (Module 3), parse structure (Module 4), and measure meaning (Module 5–6). This module applies that foundation to three information extraction tasks: identifying *what* entities are named (NER), *who did what to whom* (SRL), and *which mentions refer to the same entity* (coreference).
</Bridge>

## Named Entity Recognition (NER)

**Named Entity Recognition** identifies expressions that refer to real-world objects — people, companies, countries, dates — and classifies them into predefined categories.

<ConceptBox title="Entity Types">
**Core types (CoNLL 2003):**
- **PERSON** — people, fictional or real
- **ORG** — companies, agencies, institutions
- **GPE** — geopolitical entities (countries, cities, states)
- **LOC** — non-GPE locations (mountains, rivers)
- **MISC** — nationalities, products, events

**Extended types (OntoNotes):**
- **DATE**, **TIME** — temporal expressions
- **MONEY**, **PERCENT** — numeric values
- **PRODUCT**, **EVENT**, **LAW**
</ConceptBox>

NER is foundational for downstream tasks: IE (who did what to whom), QA (entity types constrain answer candidates), relation extraction (only extract [ORG] acquired [ORG] for [MONEY] if you first know what is ORG and MONEY).

<CodeBlock language="python">
{`import spacy

nlp = spacy.load("en_core_web_sm")
text = "Barack Obama visited Apple's headquarters in Cupertino on Monday."
doc = nlp(text)

for ent in doc.ents:
    print(f"{ent.text:25} → {ent.label_:8} ({spacy.explain(ent.label_)})")
# Barack Obama              → PERSON   (People, including fictional)
# Apple                     → ORG      (Companies, agencies, institutions)
# Cupertino                 → GPE      (Countries, cities, states)
# Monday                    → DATE     (Absolute or relative dates)`}
</CodeBlock>

## IOB / BIO Tagging Scheme

<PredictReveal
  prompt={"New York City is one entity spanning three tokens. Tagging each token simply as a location loses something. What?"}
  options={["Nothing, three location tokens is enough", "Where one entity ends and the next begins — two adjacent cities would be indistinguishable from one three-word city"]}
  correct={1}
>

NER is a **span identification problem**: a single entity may span multiple tokens ("Barack Obama" = one PERSON, "New York City" = one GPE). The **BIO scheme** converts span-level annotation into per-token labels:

<DerivationSteps>
<DerivationStep number="B" title="B-TYPE — Beginning of entity.">
The first token of an entity of type TYPE. Every entity span starts with exactly one B tag. Example: "Barack" receives `B-PERSON`.
</DerivationStep>

<DerivationStep number="I" title="I-TYPE — Inside (continuation) of entity.">
Any token after the first within the same entity span. "Obama" in "Barack Obama" receives `I-PERSON`. An I tag may only follow a B or another I of the **same TYPE**.
</DerivationStep>

<DerivationStep number="O" title="O — Outside any entity.">
Tokens not part of any named entity. Verbs, prepositions, punctuation receive O. An I tag may **never** follow O.
</DerivationStep>
</DerivationSteps>

"Barack Obama visited New York" labeled:

| Token | IOB tag |
|-------|---------|
| Barack | B-PERSON |
| Obama | I-PERSON |
| visited | O |
| New | B-GPE |
| York | I-GPE |

<CodeBlock language="python">
{`for token in doc:
    iob   = token.ent_iob_
    etype = token.ent_type_ if token.ent_type_ else "O"
    tag   = f"{iob}-{etype}" if iob != "O" else "O"
    print(f"{token.text:15} {tag}")
# Barack          B-PERSON
# Obama           I-PERSON
# visited         O
# Apple's         B-ORG`}
</CodeBlock>

</PredictReveal>

<Recall items={[
  { q: "What problem does the beginning-inside-outside tagging scheme solve?", a: "It marks boundaries. Labelling every token of a multi-word entity with the same type leaves no way to tell one long entity from two adjacent short ones. Marking the first token as beginning and the rest as inside makes the boundary explicit." },
]} />

## Conditional Random Fields (CRF) for Sequence Labeling

<PredictReveal
  prompt="A classifier that tags each token independently might output an inside tag with no preceding beginning tag. Why can a sequence model avoid that?"
  options={["It is more accurate per token", "It scores whole label sequences, so it can learn that an impossible transition should never occur"]}
  correct={1}
>

A simple per-token classifier cannot enforce BIO structural constraints (I-PERSON must never follow O). A **CRF** models the conditional distribution of the *entire* label sequence:

$$P(\mathbf{y} \mid \mathbf{x}) = \frac{\exp\!\Bigl(\sum_{i=1}^{n} \sum_{k} \lambda_k f_k(y_{i-1}, y_i, \mathbf{x}, i)\Bigr)}{Z(\mathbf{x})}$$

where $f_k$ are feature functions over $(y_{i-1}, y_i, \mathbf{x}, i)$ and $\lambda_k$ are learned weights.

**Transition features** make invalid label pairs impossible: a feature with weight $-\infty$ for (O → I-PERSON) ensures that transition never occurs. **Observation features** include word identity, capitalization, prefix/suffix, POS tag, neighboring words.

**Viterbi decoding** finds the highest-scoring label sequence in O(n · |T|²):

$$V_i(y) = \max_{y'} \Bigl[ V_{i-1}(y') + \sum_k \lambda_k f_k(y', y, \mathbf{x}, i) \Bigr]$$

<ConceptBox title="Modern NER: BiLSTM-CRF">
The standard architecture combines a bidirectional LSTM (rich contextual features for each token) with a CRF output layer (enforces valid label transitions globally). The BiLSTM replaces hand-crafted feature functions; the CRF replaces a simple softmax output.

Transformer-based models (BERT, RoBERTa) have largely superseded BiLSTM, but the CRF output layer is still commonly added even on top of transformers for structured prediction — it ensures globally consistent BIO sequences.
</ConceptBox>

</PredictReveal>

<Recall items={[
  { q: "Why is a sequence model preferred over per-token classification for entity tagging?", a: "Per-token classification chooses each label in isolation, so it can produce sequences that are structurally impossible, such as an inside tag with no beginning before it. A sequence model scores the whole label sequence including transitions, so it learns that such transitions never occur and cannot emit them." },
]} />

## Semantic Role Labeling (SRL)

SRL identifies the **predicate–argument structure** of each verb — who did what to whom, when, where, and how.

**PropBank core roles:**

| Role | Semantic meaning | Example |
|------|----------------|---------|
| `ARG0` | Agent / doer | **The dog** chased the cat |
| `ARG1` | Patient / theme | The dog chased **the cat** |
| `ARG2` | Instrument / beneficiary | She cut the bread **with a knife** |
| `ARGM-TMP` | Temporal modifier | He left **yesterday** |
| `ARGM-LOC` | Location modifier | They met **in the garden** |
| `ARGM-MNR` | Manner modifier | She ran **quickly** |

**Example.** "The dog chased the cat in the garden yesterday." → Predicate: *chased* | ARG0: *The dog* | ARG1: *the cat* | ARGM-LOC: *in the garden* | ARGM-TMP: *yesterday*

<ConceptBox title="SRL vs. NER">
NER classifies expressions by their *type* (PERSON, ORG, GPE) — the label is absolute, independent of context. SRL assigns *roles relative to a predicate* — the same expression "the dog" is ARG0 of "chased" but might be ARG1 of "was bathed by". SRL is about event semantics; NER is about entity typing.
</ConceptBox>

## Coreference Resolution

**Coreference resolution** identifies all expressions in a text that refer to the same real-world entity — linking them into *coreference chains*.

"**Obama** was born in Hawaii. **He** later became president. The **44th president** introduced landmark healthcare legislation."

All three expressions (Obama, He, the 44th president) form one coreference chain referring to a single entity.

**Why it matters:** Without coreference resolution, information extraction cannot connect facts spread across sentences. "Tesla's CEO **announced** a new product. **He** said it would ship in 2025." — without knowing that "He" = Elon Musk = Tesla's CEO, we cannot attribute the statement.

<NERWidget />

<Recall items={[
  { q: "Why does NER need BIO tags rather than just labelling each token with its entity type?", a: "Because you must be able to tell where one entity ends and the next begins. With bare type labels, 'New York Boston' is an unbroken run of GPE tokens and you cannot recover whether it is one entity or two. B- marks a new span, I- marks continuation, so adjacent same-type entities stay separable." },
  { q: "Why is I-PERSON after O invalid, and what enforces the constraint?", a: "An inside tag with no preceding begin tag describes the middle of an entity that never started — it corresponds to no valid span. A CRF enforces it by scoring TRANSITIONS between adjacent labels alongside per-token scores, learning a strongly negative weight for O to I-anything, so the decoder never emits that path." },
  { q: "Connect this module back to the normalization warning in Module 1.", a: "Lowercasing destroys capitalization, and capitalization is one of the strongest NER features in English — it is most of what separates 'Apple' the company from 'apple' the fruit, and it marks proper names the model has never seen. This is the concrete task where a standard normalization step causes real damage." },
  { q: "Why can't information extraction work without coreference resolution?", a: "Because facts are distributed across sentences and referred to by pronouns and shortened names. 'Tesla's CEO announced a product. He said it ships in 2025' contains an attributable claim only if you resolve 'He' to the CEO and 'it' to the product. Without that, each sentence yields a fragment attached to nothing." },
]} />

---

<ConceptBox title="TL;DR">
- Three extraction tasks: **NER** (what entities are named), **SRL** (who did what to whom), **coreference** (which mentions are the same thing).
- **BIO tagging** turns span-finding into per-token classification: `B-` begins an entity, `I-` continues it, `O` is outside.
- That encoding has invalid states — `I-PERSON` directly after `O` names no entity. A **CRF** learns to forbid them by scoring transitions, not just tokens.
- NER is why Module 1's lowercasing warning mattered: capitalization is a top feature here.
- **Coreference is what connects facts across sentences.** Without it, "He said it would ship" attaches to nobody.
</ConceptBox>

<QuizCard
  question="In BIO tagging, why is an 'I-PERSON' tag after an 'O' tag invalid, and which model enforces this constraint during decoding?"
  options={['It is invalid because PERSON entities are always single tokens; a Viterbi decoder in an HMM enforces this', 'It is invalid because an inside tag without a preceding begin tag cannot form a valid entity span; a CRF enforces valid transitions by learning very negative weights for (O → I-*) transitions', 'It is invalid by definition of BIO but no model enforces it — post-processing converts I-X after O to B-X', 'It is not truly invalid; some corpora allow I tags to follow O to indicate entity continuations']}
  correct={1}
/>
