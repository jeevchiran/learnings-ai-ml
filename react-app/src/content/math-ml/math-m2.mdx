import { ConceptBox, Bridge, PredictReveal, DerivationSteps, DerivationStep, QuizCard, Recall } from '../../components/learning/index.js'
import MatrixTransformWidget from '../../components/widgets/math-ml/MatrixTransformWidget.jsx'

# Module 2: Matrices & Linear Transformations

<Bridge>
Module 1 treated a vector as a single arrow and measured one against another with the dot product. This module moves up a level: instead of asking about two arrows, it asks what happens when the same rule is applied to every arrow in the plane at once. That rule is a matrix, and the dot product turns out to be the machinery inside it.
</Bridge>

## A Matrix Is a Transformation

Stop reading a matrix as "a grid of numbers." Read it as a **function that moves every point in space** — a linear one: straight lines stay straight, the origin stays fixed, and grid spacing stays even (parallel lines stay parallel).

<PredictReveal
  prompt="You are handed an unfamiliar 2x2 matrix and five seconds to say what it does to the plane. Four numbers, no calculator. Which part of it do you look at?"
  options={["The determinant, since one number summarises the whole matrix", "The two columns, since each one is where an axis direction ends up"]}
  correct={1}
>

<DerivationSteps>
<DerivationStep number={1} title="Matrix-vector multiplication">
$$M\mathbf{v} = \begin{bmatrix}a & b\\ c& d\end{bmatrix}\begin{bmatrix}v_1\\v_2\end{bmatrix} = \begin{bmatrix}av_1+bv_2\\cv_1+dv_2\end{bmatrix}$$
Each output coordinate is a dot product of a row of $M$ with $\mathbf{v}$.
</DerivationStep>
<DerivationStep number={2} title="The columns ARE where the basis vectors land">
$$M\begin{bmatrix}1\\0\end{bmatrix} = \begin{bmatrix}a\\c\end{bmatrix}, \qquad M\begin{bmatrix}0\\1\end{bmatrix}=\begin{bmatrix}b\\d\end{bmatrix}$$
This is the single most useful fact about matrices: to know what a matrix *does*, just look at its columns — they tell you exactly where each axis direction is sent.
</DerivationStep>
<DerivationStep number={3} title="Matrix-matrix multiplication = composing transformations">
$$(AB)\mathbf{v} = A(B\mathbf{v})$$
Multiplying two matrices builds a new matrix that applies $B$ first, then $A$. This is exactly what stacking neural network layers does: each layer's weight matrix is one more transformation composed onto the input.
</DerivationStep>
</DerivationSteps>

<MatrixTransformWidget />

Change one column in the widget and watch only one axis move. The whole transformation is nothing more than a choice of where the two axes go, because every other vector is a combination of those two.

</PredictReveal>

<Recall items={[
  { q: "A 2x2 matrix has columns [0, 1] and [1, 0]. Without multiplying anything, what does it do?", a: "It sends the x-axis to the y-axis and the y-axis to the x-axis, so it reflects the plane across the line y = x. Reading the columns as destinations of the basis vectors is enough, because a linear map is completely determined by where the basis lands." },
]} />

## Why This Matters for ML

A dense/linear layer computes $\mathbf{y} = W\mathbf{x} + \mathbf{b}$ — literally a matrix transformation plus a shift. Stack layers, and you're composing transformations (with a nonlinearity between each, or every composition would collapse back into one single matrix). Convolutions, attention projections, PCA — all matrix multiplication under the hood.

<PredictReveal
  prompt="A transformation squashes the whole plane flat onto a single line, so every point now sits on that line. What is the area of the image of the unit square?"
  options={["Still 1 — linear maps preserve area", "Zero, because a line has no area"]}
  correct={1}
>

<ConceptBox title="The determinant — how much does the transformation inflate space?">
$$\det\begin{bmatrix}a&b\\c&d\end{bmatrix} = ad - bc$$
Take the unit square, apply $M$, and measure the parallelogram you get: its area is $|\det(M)|$. So $\det = 3$ triples areas, $\det = 0.5$ halves them, and a **negative** determinant means the transformation flipped orientation (a reflection — the x-axis and y-axis swapped handedness). The special case $\det(M) = 0$ means the square was squashed flat onto a line: area zero, information destroyed. Module 3 picks this up.
</ConceptBox>

</PredictReveal>

<Recall items={[
  { q: "What does a determinant of zero mean geometrically, and what does it cost you?", a: "The transformation flattens space onto something lower-dimensional, so the image of the unit square has zero area. The cost is reversibility: many different inputs now land on the same output, so nothing can undo the map." },
]} />

<PredictReveal
  prompt="Take a square sheet of paper. Rotate it 90 degrees, then stretch it sideways. Now start again: stretch it sideways first, then rotate 90 degrees. Same final shape in the same place?"
  options={["Yes, the two operations are independent", "No, the two results differ"]}
  correct={1}
>

<ConceptBox title="Common confusion — matrix multiplication doesn't commute">
$AB \neq BA$ in general — "rotate then stretch" lands somewhere different than "stretch then rotate." Order matters, which is exactly why layer order in a network changes what it computes.
</ConceptBox>

</PredictReveal>

<Recall items={[
  { q: "In the product AB applied to a vector v, which matrix acts first?", a: "B, because (AB)v means A(Bv) — the matrix written closest to the vector is applied first. That is why the notation reads right to left, and why AB and BA are generally different transformations." },
]} />

## In Python (NumPy)

```python
import numpy as np

M = np.array([[1.5, 0.5], [0, 1]])
v = np.array([1, 1])

Mv = M @ v                 # matrix-vector product
e1_dest = M @ np.array([1, 0])   # where the x-axis basis vector lands
e2_dest = M @ np.array([0, 1])   # where the y-axis basis vector lands

A = np.array([[0, -1], [1, 0]])   # 90° rotation
B = np.array([[2, 0], [0, 2]])    # scale by 2
composed = A @ B                  # scale then rotate
```

---

<ConceptBox title="TL;DR">
- A matrix is not a grid of numbers — it's a **function that moves every point**, keeping lines straight and the origin fixed.
- **Read the columns.** Column $j$ is where basis vector $j$ lands. That one fact tells you what any matrix does.
- $AB$ means "apply $B$, then $A$" — so stacking layers is composing transformations, and $AB \neq BA$ is why layer order matters.
</ConceptBox>

## Practice

**Problem 1.** $M = \begin{bmatrix}2&0\\0&3\end{bmatrix}$. Where does the point $(1,1)$ land after $M$?

**Solution:** $(2\cdot1, 3\cdot1) = (2,3)$ — stretched 2× along x, 3× along y.

**Problem 2.** Without computing anything, what do the columns of $M = \begin{bmatrix}0&1\\1&0\end{bmatrix}$ tell you about the transformation?

**Solution:** Column 1 is $[0,1]$ (x-axis lands on y-axis), column 2 is $[1,0]$ (y-axis lands on x-axis) — this matrix swaps/reflects across the line $y=x$.

<Recall items={[
  { q: "You are handed a 2x2 matrix and 5 seconds. What do you look at to know what it does?", a: "The columns. Column 1 is where [1,0] lands, column 2 is where [0,1] lands. Since a linear map is fully determined by what it does to the basis, those two arrows describe the whole transformation." },
  { q: "Why does a stack of dense layers need a nonlinearity between them?", a: "Without one, y = W3(W2(W1 x)) = (W3 W2 W1) x — the composition of linear maps is itself a single linear map. The depth collapses into one equivalent matrix, so the network gains no expressive power over a single layer." },
  { q: "In the product AB applied to a vector, which matrix touches the vector first — and why does the notation feel backwards?", a: "B first: (AB)v = A(Bv). It reads right-to-left because the matrix nearest the vector is applied first. This is why 'rotate then stretch' is written stretch-matrix times rotate-matrix." },
]} />

<QuizCard
  question="What does the determinant of a 2x2 transformation matrix measure?"
  options={[
    'The angle of rotation applied',
    'The signed factor by which the matrix scales area (negative means orientation is flipped)',
    'The number of dimensions in the input',
    'Whether the matrix is symmetric'
  ]}
  correct={1}
/>
