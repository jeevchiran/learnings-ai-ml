# Track Quiz — Mathematics for Machine Learning

import { ConceptBox, QuizCard, MultiSelectQuiz } from '../../components/learning/index.js'

<ConceptBox title="End-of-track checkpoint">
Questions across all eleven modules — vectors and dot products, matrix transformations, eigenvalues/rank, PCA, Bayes' theorem, distributions, expectation/covariance, MLE, derivatives/chain rule, gradient descent, and Jacobians/Hessians.
</ConceptBox>

## Questions

<QuizCard
  question="a = [3,4], b = [0,5]. What is a·b?"
  options={['12', '20', '15', '35']}
  correct={1}
/>

<QuizCard
  question="Why is cosine similarity preferred over raw dot product for comparing embedding vectors of very different lengths?"
  options={[
    'Cosine similarity is always larger',
    'It divides out magnitude, isolating direction (semantic similarity) from vector length',
    'Dot product cannot be negative',
    'They give identical results for embeddings'
  ]}
  correct={1}
/>

<QuizCard
  question="What do the columns of a transformation matrix M tell you?"
  options={[
    'Nothing useful without computing eigenvalues first',
    'Exactly where each standard basis vector (e.g. [1,0] and [0,1]) lands after the transformation',
    'The determinant of M',
    'The rank of M only'
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="Which statements about eigenvalues/eigenvectors are correct? (Select all that apply.)"
  options={[
    'An eigenvector keeps its direction under the transformation, only getting scaled by its eigenvalue',
    'det(M) equals the product of M’s eigenvalues',
    'Every real matrix has real eigenvalues',
    'A matrix with a zero eigenvalue is singular (not invertible)',
    'Eigenvectors are unique in both direction and length'
  ]}
  correct={[0, 1, 3]}
/>

<QuizCard
  question="PCA's first principal component (PC1) is defined as..."
  options={[
    'A randomly chosen original feature',
    'The eigenvector of the covariance matrix with the largest eigenvalue — the direction of maximum variance',
    'The mean of all features',
    'The smallest singular value of the data matrix'
  ]}
  correct={1}
/>

<QuizCard
  question="A rare disease (1% prevalence) has a 95%-sensitive test with a 10% false-positive rate. Why is P(disease | positive) far below 95%?"
  options={[
    'It isn’t — it should be close to 95%',
    'False positives from the large healthy population outnumber true positives from the small diseased population',
    'Bayes theorem only works for prevalence above 50%',
    'Sensitivity and false-positive rate cancel out exactly'
  ]}
  correct={1}
/>

<QuizCard
  question="What is the key difference between a PMF and a PDF?"
  options={[
    'No difference',
    'PMF gives P(X=k) directly for discrete variables; PDF is a density whose integral over a range gives probability',
    'PDF is only for discrete variables',
    'PMF values can exceed 1'
  ]}
  correct={1}
/>

<QuizCard
  question="Cov(X,Y) = 8, Var(X) = 4, Var(Y) = 16. What is the correlation r?"
  options={['0.5', '1.0', '2.0', '0.25']}
  correct={1}
/>

<QuizCard
  question="In one sentence, what does Maximum Likelihood Estimation choose as the best parameter?"
  options={[
    'The parameter that minimizes the sample variance',
    'The parameter under which the observed data would have been most probable',
    'The parameter closest to zero',
    'The average of all possible parameter values'
  ]}
  correct={1}
/>

<QuizCard
  question="Why do ML loss functions typically use negative log-likelihood instead of raw likelihood?"
  options={[
    'Raw likelihood is undefined for continuous data',
    'Multiplying many small probabilities underflows numerically; log turns the product into a stable sum, and negating fits the “minimize a cost” optimizer convention',
    'Log-likelihood produces a different optimal parameter',
    'It is purely a stylistic choice with no numerical benefit'
  ]}
  correct={1}
/>

<QuizCard
  question="What does the chain rule let backpropagation compute?"
  options={[
    'The integral of the loss function',
    'The gradient of the loss with respect to a weight buried inside a composition of layer functions, by multiplying local derivatives layer by layer',
    'The eigenvalues of the weight matrices',
    'The learning rate to use'
  ]}
  correct={1}
/>

<QuizCard
  question="Gradient descent uses learning rate η = 1.5 on f(x) = x², starting at x₀ = 4. What happens?"
  options={[
    'Converges smoothly to 0',
    'Diverges — each step overshoots further than the last, since η is too large for this curvature',
    'Gets stuck immediately at x₀',
    'Converges in exactly one step'
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="Which statements about gradient descent are correct? (Select all that apply.)"
  options={[
    'The gradient points in the direction of steepest ascent, so descent moves opposite to it',
    'A learning rate that is too large can cause divergence instead of convergence',
    'On a convex bowl with a small enough learning rate, gradient descent converges to the minimum',
    'Gradient descent always finds the global minimum on any loss surface, convex or not',
    'An elongated valley (very different curvature per direction) causes zig-zagging with a single shared learning rate'
  ]}
  correct={[0, 1, 2, 4]}
/>

<QuizCard
  question="A Hessian at a critical point has eigenvalues 3 and -5. What kind of point is it?"
  options={[
    'A local minimum',
    'A local maximum',
    'A saddle point',
    'Cannot be determined from eigenvalues'
  ]}
  correct={2}
/>

<QuizCard
  question="What is the key structural difference between a Jacobian and a Hessian?"
  options={[
    'They are the same matrix under different names',
    'A Jacobian holds first-order partials of a vector-valued function; a Hessian holds second-order partials of a scalar-valued function',
    'A Jacobian is always symmetric, a Hessian never is',
    'A Hessian only exists for functions of one variable'
  ]}
  correct={1}
/>
