import { ConceptBox, Bridge, PredictReveal, DerivationSteps, DerivationStep, QuizCard, MultiSelectQuiz, Recall } from '../../components/learning/index.js'
import GradientDescentWidget from '../../components/widgets/math-ml/GradientDescentWidget.jsx'

# Module 11: Jacobians & Hessians

<Bridge>
Two loose ends are still hanging. Module 9 admitted that a zero derivative might be a minimum, a maximum or something else, and could not tell which. Module 10 said the safe learning rate is decided by "the curvature" without defining it. Both answers are the same object, and this module is it.
</Bridge>

## Beyond the First Derivative

The gradient (Module 9) tells you the slope. It says nothing about whether that slope is about to flatten out, keep going, or reverse — that's **curvature**, and it's the last piece needed to fully explain why gradient descent behaves the way it does.

<ConceptBox title="Jacobian vs. Hessian — don't mix them up">
- **Jacobian** — for a *vector-valued* function $f:\mathbb{R}^n\to\mathbb{R}^m$, the matrix of all first-order partial derivatives, $J_{ij}=\frac{\partial f_i}{\partial x_j}$. This is the general object backprop computes layer-to-layer (each layer's output is a vector, not a scalar).
- **Hessian** — for a *scalar-valued* function $f:\mathbb{R}^n\to\mathbb{R}$, the matrix of all **second**-order partial derivatives, $H_{ij}=\frac{\partial^2 f}{\partial x_i \partial x_j}$. This is curvature — the Jacobian of the gradient.
</ConceptBox>

## Read a Small Jacobian First

Suppose a layer takes two inputs and produces $f(x,y)=[2x+y,\;xy]$. At $(x,y)=(3,4)$ its output is $[10,12]$. Its Jacobian is

$$
J(x,y)=\begin{bmatrix}2&1\\y&x\end{bmatrix},\qquad
J(3,4)=\begin{bmatrix}2&1\\4&3\end{bmatrix}.
$$

Read **rows as outputs, columns as inputs**. The bottom-left 4 means that increasing $x$ by a small amount changes the second output at about four times that rate, with $y$ held fixed. For an input step $[0.1,0]$, multiplying by $J$ predicts an output step $[0.2,0.4]$. Check directly: $f(3.1,4)=[10.2,12.4]$.

The Jacobian describes how the **output** changes. Next, the Hessian describes how a scalar function's **slope** changes.

## The Hessian, for a Quadratic

<DerivationSteps>
<DerivationStep number={1} title="Set up">
$$f(x,y) = ax^2+bxy+cy^2 \implies H = \begin{bmatrix}2a & b\\ b & 2c\end{bmatrix}$$
For this family the Hessian is *constant* everywhere — the curvature doesn't change as you move, which is exactly why it's a useful teaching example (and a reasonable local approximation of any smooth loss surface near a point, via a second-order Taylor expansion).
</DerivationStep>
<DerivationStep number={2} title="Read the eigenvalues (Module 3) of H">
Both eigenvalues positive → **convex bowl**, a genuine local minimum. Both negative → local maximum. **Mixed signs → saddle point** — curves up in one direction, down in another. This is the formal version of the "flat point" ambiguity from Module 9.
</DerivationStep>
</DerivationSteps>

<PredictReveal
  prompt="For f(x,y) = x² − y², the gradient at the origin is zero. An x-step raises the loss, while a y-step lowers it. What kind of point is the origin?"
  options={["A minimum, because its gradient is zero", "A saddle, because nearby directions can raise or lower the loss"]}
  correct={1}
>

<GradientDescentWidget defaultShape="Saddle point" />

Start the widget from different points on the saddle preset and watch some runs stall near the centre while others slide off an edge. The flat point is not a floor — it only looks like one from the directions that curve upward.

</PredictReveal>

<Recall items={[
  { q: "Does a zero Hessian eigenvalue rule out a minimum?", a: "No. Positive eigenvalues are sufficient for a strict local minimum at a critical point, but not necessary. For example, x⁴ has a minimum at zero even though its second derivative there is zero. The second-derivative test is then inconclusive." },
]} />

<ConceptBox title="Common confusion — sufficient does not mean necessary">
At a critical point of a twice continuously differentiable function, all-positive Hessian eigenvalues imply a strict local minimum; mixed signs imply a saddle. Zero eigenvalues can make the test inconclusive. For $f(x)=x^4$, both first and second derivatives vanish at 0, yet 0 is a minimum. Parameter count alone does not tell us what fraction of a particular model's critical points are saddles.
</ConceptBox>

<PredictReveal
  prompt="Newton's method can converge quickly near a suitable minimum. What makes a full, dense Newton step expensive for a million-parameter network?"
  options={["It only works on convex problems", "The Hessian has a trillion entries, and solving the associated linear system is costly"]}
  correct={1}
>

For n parameters a dense Hessian holds n squared entries. At a million parameters that is a trillion numbers, or about 4 TB with four-byte values, before other training memory. A general dense solve costs roughly n cubed operations. Hessian-vector methods and approximations can avoid forming the full matrix. Adam takes a different approach: it adapts updates from averages of gradients and squared gradients, not from a Hessian.

</PredictReveal>

<Recall items={[
  { q: "Why is a full dense Newton step expensive for large networks?", a: "A dense Hessian has n squared entries and a general dense linear solve takes roughly n cubed work. Practical second-order methods can use approximations or Hessian-vector products instead. Computing an explicit matrix inverse is not required." },
]} />

## Where This Shows Up in Practice

- **Newton's method** solves $H\Delta=-\nabla f$ for an update. Under suitable conditions it converges quickly near a minimum, but a full dense Hessian and solve are expensive at neural-network scale.
- **Adam** uses first-order gradient statistics to adapt each parameter's update; it does not compute Hessian entries.
- **Second-order Taylor expansion**: $f(\theta+\Delta) \approx f(\theta) + \nabla f^\top \Delta + \tfrac12 \Delta^\top H \Delta$ — the Hessian is literally the quadratic term describing how the loss curves around your current point.

## In Python

```python
import torch

x = torch.tensor([1.0, 1.0], requires_grad=True)

def f(v):
    return v[0]**2 - v[1]**2   # saddle: eigenvalues [2, -2]

hessian = torch.autograd.functional.hessian(f, x)
print(hessian)
# tensor([[ 2.,  0.],
#         [ 0., -2.]])   -- matches the analytic [[2a,b],[b,2c]] formula, a=1,b=0,c=-1
```

---

<ConceptBox title="TL;DR">
- **Jacobian** = first derivatives of a *vector*-valued function. **Hessian** = second derivatives of a *scalar*-valued one. The Hessian is the Jacobian of the gradient.
- The Hessian is curvature, and its **eigenvalues classify a flat point**: all positive → minimum, all negative → maximum, mixed → saddle.
- Zero eigenvalues can make the second-derivative test inconclusive. Both flat regions and saddle points can complicate optimization.
- Newton's method uses curvature; a dense Hessian is expensive at network scale. Adam instead adapts updates using gradient statistics.
</ConceptBox>

## Practice

**Problem 1.** $f(x,y)=2x^2+2y^2$. Write the Hessian and classify the critical point at the origin.

**Solution:** $H=\begin{bmatrix}4&0\\0&4\end{bmatrix}$, eigenvalues both 4 (positive) → the origin is a local (here, global) minimum.

**Problem 2.** $f(x,y)=x^2-4y^2$. Same question.

**Solution:** $H=\begin{bmatrix}2&0\\0&-8\end{bmatrix}$, eigenvalues 2 and -8 (mixed signs) → a saddle point, not a minimum.

<Recall items={[
  { q: "Jacobian or Hessian — which for a layer mapping R^512 to R^256, and which for the scalar loss?", a: "The layer is vector-to-vector, so its matrix of first partials is a Jacobian (256 x 512). The loss is vector-to-scalar, so its matrix of second partials is a Hessian (over the parameters). Backprop chains Jacobians; curvature analysis uses the Hessian." },
  { q: "What can you conclude when a Hessian has both positive and negative eigenvalues at a critical point?", a: "There are uphill and downhill directions, so the point is a saddle. This conclusion requires mixed signs; a zero eigenvalue on its own is not enough." },
  { q: "Do second-order methods always need to form and invert a full Hessian?", a: "No. A dense Newton step is costly, but approximations and Hessian-vector products can avoid storing the full matrix. Even with a full matrix, one usually solves a linear system rather than computing its inverse explicitly." },
  { q: "Write the second-order Taylor expansion of a loss and say what each term does.", a: "f(theta + d) ≈ f(theta) + grad^T d + (1/2) d^T H d. The first term is the current loss, the second is the linear slope that plain gradient descent uses, and the third is the curvature correction — it is what tells you whether a step of a given size will overshoot." },
]} />

<QuizCard
  question="What does the Hessian matrix represent that the gradient alone does not?"
  options={[
    'The direction of steepest ascent',
    "The curvature of the function — how the gradient itself changes — letting you distinguish a minimum, maximum, or saddle at a flat point",
    'The learning rate to use',
    'The number of parameters in the model'
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="Which statements about Jacobians and Hessians are correct? (Select all that apply.)"
  options={[
    'A Jacobian applies to vector-valued functions; a Hessian applies to scalar-valued functions',
    'A Hessian with all-positive eigenvalues at a critical point indicates a local minimum',
    'A Hessian with mixed-sign eigenvalues at a critical point indicates a saddle point',
    "Newton's method uses the Hessian to account for curvature, converging faster near a minimum but at higher per-step cost",
    'The Hessian is always the same matrix as the Jacobian'
  ]}
  correct={[0, 1, 2, 3]}
/>
