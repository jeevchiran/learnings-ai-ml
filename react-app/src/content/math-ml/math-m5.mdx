import { ConceptBox, Bridge, PredictReveal, DerivationSteps, DerivationStep, QuizCard, Recall } from '../../components/learning/index.js'
import BayesWidget from '../../components/widgets/math-ml/BayesWidget.jsx'

# Module 5: Probability Foundations & Bayes' Theorem

<Bridge>
Modules 1 to 4 were all linear algebra: vectors, matrices, and the directions data spreads along. Everything there was exact. From here the track changes subject to uncertainty, which is what you actually face with noisy labels and imperfect models. The linear algebra does not go away; it becomes the thing probability is computed over.
</Bridge>

## Starting the Probability Track

Linear algebra described *space*; probability describes *uncertainty* — the language for noisy labels, model confidence, and the loss functions used to fit almost every model in this curriculum.

<ConceptBox title="The vocabulary">
- **Sample space** $\Omega$ — the set of all possible outcomes (e.g. all 6 faces of a die).
- **Event** — any subset of outcomes you care about (e.g. "rolled even").
- **P(A)** — a number in $[0,1]$ measuring how likely event A is; $P(\Omega)=1$.
- **Independence** — A and B are independent iff $P(A \cap B) = P(A)P(B)$ — knowing one tells you nothing about the other.
</ConceptBox>

## Conditional Probability

<PredictReveal
  prompt={"A lab reports that 90% of people who have a disease test positive for it. A patient tests positive. Does that mean there is a 90% chance they have the disease?"}
  options={["Yes, the number transfers directly", "No — those are two different conditional probabilities, and they can be wildly different"]}
  correct={1}
>

$$P(A\mid B) = \frac{P(A\cap B)}{P(B)}$$

"Given that B happened, how likely is A?" — this is the single most-used probability tool in ML: a classifier's output *is* $P(\text{class}\mid \text{features})$.

The order of the condition is the whole content of the statement. "Positive given diseased" is a property of the test. "Diseased given positive" is a property of the test *and* of how common the disease is. Bayes' theorem is the machinery that converts one into the other.

</PredictReveal>

<Recall items={[
  { q: "Why is P(B given A) different from P(A given B)?", a: "They condition on different things and are divided by different denominators. P(B|A) asks how often the evidence appears among cases of A; P(A|B) asks how often A holds among cases showing the evidence. When A is rare, the second can be far smaller than the first." },
]} />

## Bayes' Theorem

<DerivationSteps>
<DerivationStep number={1} title="Start from conditional probability, both directions">
$$P(A\cap B) = P(A\mid B)P(B) = P(B\mid A)P(A)$$
</DerivationStep>
<DerivationStep number={2} title="Solve for what you want">
$$P(A\mid B) = \frac{P(B\mid A)P(A)}{P(B)}$$
Bayes lets you **flip** a conditional: turn "P(evidence | hypothesis)," which is often what you can measure or model, into "P(hypothesis | evidence)," which is usually what you actually want.
</DerivationStep>
<DerivationStep number={3} title="Expand the denominator">
$$P(B) = P(B\mid A)P(A) + P(B\mid \neg A)P(\neg A)$$
The **law of total probability** — B happens either alongside A or alongside not-A, so sum both paths.
</DerivationStep>
</DerivationSteps>

<PredictReveal
  prompt="A disease affects 1 person in 100. A test catches 90% of real cases and wrongly flags 5% of healthy people. Out of everyone who tests positive, roughly what fraction actually has the disease?"
  options={["About 90%, matching the test accuracy", "About 15%, because the healthy group is so much larger"]}
  correct={1}
>

<BayesWidget />

Move the prevalence slider down and watch the posterior collapse even though the test never changed. In 1,000 people there are about 9 true positives and about 50 false ones, so most positives are false alarms. This is the base-rate fallacy, and it is the single most expensive mistake in applied probability.

<ConceptBox title="Common confusion — P(B|A) ≠ P(A|B)">
"90% of positive tests are accurate" and "90% of people who test positive have the disease" are *different claims* — confusing them is the prosecutor's fallacy. The widget above is built specifically to make that gap visible: a rare condition plus an imperfect test means most positives are false, even at high sensitivity.
</ConceptBox>

</PredictReveal>

<Recall items={[
  { q: "A test is 99% accurate for a disease with 1-in-10,000 prevalence. Roughly what fraction of positives are real?", a: "About 1 in 101. Among 10,000 people there is 1 true case, caught almost certainly, against roughly 1% of the 9,999 healthy people, which is about 100 false positives. The healthy population is so much larger that its small error rate produces far more positives than the disease does." },
]} />

## In Python

```python
def bayes(p_a, p_b_given_a, p_b_given_not_a):
    p_b = p_b_given_a * p_a + p_b_given_not_a * (1 - p_a)
    return (p_b_given_a * p_a) / p_b

# 1% prevalence, 90% sensitivity, 5% false-positive rate
posterior = bayes(p_a=0.01, p_b_given_a=0.90, p_b_given_not_a=0.05)
print(f'{posterior:.1%}')   # ~15.4% — most positives are false alarms
```

---

<ConceptBox title="TL;DR">
- $P(A\mid B) = P(A\cap B)/P(B)$ — and a classifier's output literally *is* $P(\text{class}\mid\text{features})$.
- **Bayes flips a conditional**: it converts $P(\text{evidence}\mid\text{hypothesis})$, which you can measure, into $P(\text{hypothesis}\mid\text{evidence})$, which you want.
- The denominator is the law of total probability — sum over *every* way the evidence could have arisen.
- $P(B\mid A) \neq P(A\mid B)$. When the base rate is low, a highly accurate test still yields mostly false positives.
</ConceptBox>

## Practice

**Problem 1.** $P(A)=0.3$, $P(B\mid A)=0.6$, $P(B\mid \neg A)=0.2$. Find $P(A\mid B)$.

**Solution:** $P(B) = 0.6(0.3)+0.2(0.7) = 0.18+0.14=0.32$. $P(A\mid B) = 0.18/0.32 = 0.5625$.

**Problem 2.** Two events have $P(A)=0.4$, $P(B)=0.5$, $P(A\cap B)=0.2$. Are they independent?

**Solution:** Independence requires $P(A)P(B)=0.2$ — it matches exactly, so yes, independent.

<Recall items={[
  { q: "Derive Bayes' theorem from the definition of conditional probability in two lines.", a: "P(A and B) can be written two ways: P(A|B)P(B) and P(B|A)P(A). Set them equal and divide by P(B): P(A|B) = P(B|A)P(A)/P(B). That is the whole derivation." },
  { q: "Name each of the four pieces of Bayes' theorem and say which one people forget.", a: "Posterior P(A|B) — what you want. Likelihood P(B|A) — how well the hypothesis explains the evidence. Prior P(A) — the base rate. Evidence P(B) — the normaliser. The prior is the forgotten one, and ignoring it is exactly the base-rate fallacy." },
  { q: "A test is 99% accurate for a disease with 1-in-10,000 prevalence. Reason about P(disease | positive) without computing exactly.", a: "In 10,000 people: 1 true case, caught with near-certainty, versus about 1% of the 9,999 healthy people = roughly 100 false positives. So about 1 in 101 positives is real — under 1%. The healthy population is so much larger that its small error rate produces far more positives than the disease does." },
  { q: "P(A)=0.4, P(B)=0.5, P(A and B)=0.25. Independent?", a: "No. Independence needs P(A)P(B) = P(A and B), i.e. 0.20, but the joint is 0.25. B occurring makes A more likely than its base rate, so the two events carry information about each other." },
]} />

<QuizCard
  question="A disease has 1% prevalence. A test is 95% sensitive and has a 10% false-positive rate. Roughly, why is P(disease | positive test) much lower than 95%?"
  options={[
    'The test is broken',
    'Because false positives from the much larger healthy population (10% of 99%) swamp the true positives from the small diseased population (95% of 1%)',
    'Bayes theorem does not apply to medical tests',
    'Sensitivity and false-positive rate must always sum to 100%'
  ]}
  correct={1}
/>
