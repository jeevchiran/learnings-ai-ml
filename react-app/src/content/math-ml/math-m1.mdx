import { ConceptBox, PredictReveal, QuizCard, Recall } from '../../components/learning/index.js'
import VectorWidget from '../../components/widgets/math-ml/VectorWidget.jsx'

# Module 1: Vectors & Dot Products

<ConceptBox title="Before you start">
This is the first module of the first track, so nothing is assumed beyond school algebra: you should be comfortable with square roots and with reading a formula that has a summation sign in it. Everything else is built here. If a symbol looks unfamiliar later on, it was defined in an earlier module of this track.
</ConceptBox>

## Why Linear Algebra, First

Every dataset you feed a model is a table of numbers — rows are **vectors**. Every layer of a neural net multiplies its input by a **matrix**. Every embedding, every similarity score, every gradient step is vector arithmetic. This track builds that machinery from the ground up, so the rest of the curriculum reads as "applying linear algebra" rather than magic.

## Vectors

A vector is just an ordered list of numbers, $\mathbf{v} = [v_1, v_2, \dots, v_n]$ — and geometrically, an arrow from the origin to that point. In ML, a vector is usually a **feature row**: `[age, income, clicks]`, or a word/image **embedding**.

<ConceptBox title="Norm — how long is a vector?">
$$\|\mathbf{v}\| = \sqrt{v_1^2 + v_2^2 + \cdots + v_n^2}$$
This is the Euclidean (L2) length — Pythagoras generalized to $n$ dimensions. It's the "magnitude" used in weight decay, gradient clipping, and normalizing embeddings to unit length.
</ConceptBox>

## The Dot Product

<PredictReveal
  prompt="One arrow points east, another points north. Multiply their matching components and add them up: 1*0 + 0*1 = 0. Is that zero a coincidence of these particular numbers, or does it mean something?"
  options={["A coincidence of these particular numbers", "It always means the two vectors sit at right angles"]}
  correct={1}
>

$$\mathbf{a}\cdot\mathbf{b} = \sum_i a_i b_i = \|\mathbf{a}\|\,\|\mathbf{b}\|\cos\theta$$

Two equivalent views: **algebraic** (multiply matching components, sum them) and **geometric** (lengths times the cosine of the angle between them). The geometric form is why the dot product is the standard *similarity* measure — it's large and positive when vectors point the same way, zero when perpendicular, negative when opposite.

</PredictReveal>

<Recall items={[
  { q: "What does a dot product of exactly zero tell you about two non-zero vectors?", a: "They are perpendicular. The geometric form says the dot product is the two lengths multiplied by cos(theta); if neither length is zero, only cos(theta) = 0 can make the result zero, and that is a 90 degree angle." },
]} />

## Direction versus size

<PredictReveal
  prompt="A search engine scores documents against your query with a raw dot product. One document is ten pages long and only loosely on topic. Another is one paragraph and exactly on topic. Which one is likely to win?"
  options={["The short, on-topic one, because relevance is what the dot product measures", "The long one, because a longer vector scores higher just for being long"]}
  correct={1}
>

<ConceptBox title="Common confusion — cosine similarity vs. dot product">
$$\cos\theta = \frac{\mathbf{a}\cdot\mathbf{b}}{\|\mathbf{a}\|\,\|\mathbf{b}\|}$$
Cosine similarity is the dot product with length divided out — it measures *direction* only, ignoring magnitude. Raw dot product conflates "same direction" with "large vectors," which is exactly why recommender systems and embedding search almost always cosine-normalize first.
</ConceptBox>

<VectorWidget />

Drag either arrow in the widget and watch the two readouts move independently. Lengthening a vector changes the dot product but leaves the cosine untouched. That is the whole distinction, and it is why embedding search normalises first.

</PredictReveal>

<Recall items={[
  { q: "When should you prefer cosine similarity over a raw dot product?", a: "Whenever direction should count and size should not, which is almost always the case in embedding search and recommendation. A raw dot product rewards long vectors, so an item can rank highly for being big rather than for being relevant." },
]} />

## In Python (NumPy)

```python
import numpy as np

a = np.array([3, 2])
b = np.array([1, 3])

dot_product = a @ b                              # or np.dot(a, b)
norm_a = np.linalg.norm(a)
cosine_sim = (a @ b) / (norm_a * np.linalg.norm(b))

print(dot_product, norm_a, cosine_sim)
```

---

<ConceptBox title="TL;DR">
- A vector is a feature row or an embedding; its **norm** $\|\mathbf{v}\|$ is its length.
- The dot product has two faces: $\sum_i a_ib_i$ (compute it) and $\|\mathbf{a}\|\|\mathbf{b}\|\cos\theta$ (interpret it) — positive = same direction, zero = perpendicular.
- **Cosine similarity is the dot product with lengths divided out.** Use it when direction matters and magnitude doesn't — which is nearly always, in embedding search.
</ConceptBox>

## Practice

**Problem 1.** $\mathbf{a} = [3, 4]$. What is $\|\mathbf{a}\|$?

**Solution:** $\sqrt{3^2+4^2} = \sqrt{25} = 5$.

**Problem 2.** $\mathbf{a}=[1,0]$, $\mathbf{b}=[0,1]$. What is $\mathbf{a}\cdot\mathbf{b}$, and what does that tell you geometrically?

**Solution:** $1\cdot0 + 0\cdot1 = 0$ — the vectors are perpendicular ($\theta=90°$).

<Recall items={[
  { q: "Write both forms of the dot product and say what each is FOR.", a: "Algebraic: a·b = sum of a_i·b_i — this is the one you compute. Geometric: a·b = ||a||·||b||·cos(theta) — this is the one you reason with, because it says the dot product is alignment scaled by both lengths." },
  { q: "A search index returns a long document vector as the top hit for a short query. Which similarity measure was probably used, and what would fix it?", a: "Raw dot product, which rewards large norms — a long document scores high just for being long. Cosine similarity divides both norms out, so only the direction (topic overlap) counts." },
  { q: "Two vectors have dot product 0. What is the angle between them, and does the answer depend on their lengths?", a: "90 degrees. From a·b = ||a||||b||cos(theta), if neither vector is the zero vector then cos(theta) must be 0. Lengths cannot change that — they only scale the magnitude of the result, not its sign or zero-ness." },
]} />

<QuizCard
  question="Two embedding vectors have a large positive dot product but very different norms (one much longer). What does cosine similarity do that raw dot product doesn't?"
  options={[
    'Nothing — they measure the same thing',
    'Cosine similarity divides out the magnitudes, isolating how aligned the directions are regardless of vector length',
    'Cosine similarity is only defined for unit vectors',
    'Cosine similarity always equals the dot product for embeddings'
  ]}
  correct={1}
/>
