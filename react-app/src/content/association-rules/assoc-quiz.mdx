# Track Quiz — Association Rule Mining

import { ConceptBox, QuizCard, MultiSelectQuiz } from '../../components/learning/index.js'

<ConceptBox title="End-of-track checkpoint">
Thirteen questions across all four modules — support/confidence/lift, the Apriori algorithm and its anti-monotone property, rule generation and evaluation, and FP-Growth. Several need short calculations; the reference database is the same 10-basket grocery set used throughout the track. Work each numeric one out before answering.
</ConceptBox>

## Questions

<QuizCard
  question="Association rule mining is best described as which kind of task?"
  options={[
    'Supervised classification of transactions into categories',
    'Unsupervised discovery of co-occurrence patterns among items in transactions',
    'A clustering method that groups similar customers',
    'A regression method predicting basket size'
  ]}
  correct={1}
/>

<QuizCard
  question="For the rule {A} ⇒ {B}, which formula gives its confidence?"
  options={[
    'support(A ∪ B) / N',
    'support(A ∪ B) / support(A)',
    'support(A ∪ B) / support(B)',
    'support(A) × support(B)'
  ]}
  correct={1}
/>

<QuizCard
  question="Milk appears in 8 of 10 baskets, Diaper in 7, and both together in 6. What is the confidence of {Milk} ⇒ {Diaper}?"
  options={[
    '0.60',
    '0.75',
    '0.86',
    '0.70'
  ]}
  correct={1}
/>

<QuizCard
  question="The rule {Beer} ⇒ {Milk} has confidence ≈ 0.71, while Milk's support is 0.80. What is the lift, and what does it mean?"
  options={[
    'lift ≈ 0.89 — beer buyers are LESS likely than average to buy milk, despite the high confidence',
    'lift ≈ 1.14 — a strong positive association',
    'lift ≈ 0.71 — the same as confidence, so the rule is reliable',
    'lift ≈ 1.00 — beer and milk are perfectly independent'
  ]}
  correct={0}
/>

<MultiSelectQuiz
  question="Which statements about support, confidence, and lift are correct? (Select all that apply.)"
  options={[
    'Support measures how frequently an itemset appears across all transactions',
    'A high-confidence rule is always a strong, actionable association',
    'Lift = 1 indicates the antecedent and consequent are statistically independent',
    'Lift is symmetric: lift(A ⇒ B) equals lift(B ⇒ A)',
    'Lift < 1 means the antecedent makes the consequent less likely than its baseline rate'
  ]}
  correct={[0, 2, 3, 4]}
/>

<QuizCard
  question="What does the anti-monotone (downward-closure) property state?"
  options={[
    'If an itemset is frequent, at least one of its subsets is infrequent',
    'If an itemset is infrequent, all of its supersets are also infrequent',
    'Adding an item to an itemset can only increase its support',
    'Support increases monotonically as itemsets grow'
  ]}
  correct={1}
/>

<QuizCard
  question="With min_support = 0.3 on the 10-basket database, Cola appears in only 2 baskets. What does Apriori do with every itemset containing Cola?"
  options={[
    'Counts them all, then discards the infrequent ones at the end',
    'Prunes them immediately — since {Cola} is infrequent, no superset of it can be frequent, so they are never counted',
    'Keeps them because Cola might combine with a frequent item',
    'Lowers the threshold automatically so Cola survives'
  ]}
  correct={1}
/>

<QuizCard
  question="In Apriori candidate generation, two frequent itemsets are joined into a larger candidate only when…"
  options={[
    'they share no items',
    'they share their first k−1 items (a common prefix)',
    'they have identical support values',
    'one is a subset of the other'
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="Which statements about the Apriori algorithm are correct? (Select all that apply.)"
  options={[
    'It processes itemsets level by level: first 1-itemsets, then 2-itemsets, and so on',
    'A candidate is pruned before counting if any of its subsets is not frequent',
    'It requires only a single scan of the database regardless of itemset size',
    'It stops when a level produces no frequent itemsets',
    'Raising min_support generally produces fewer frequent itemsets'
  ]}
  correct={[0, 1, 3, 4]}
/>

<QuizCard
  question="A frequent 3-itemset {Bread, Milk, Diaper} is used to generate rules. How many candidate rules (non-empty antecedent and consequent) does it produce?"
  options={[
    '3',
    '6',
    '8',
    '9'
  ]}
  correct={1}
/>

<QuizCard
  question="{Bread, Milk} ⇒ {Diaper} has support 0.40, support({Bread,Milk}) = 0.50, and support({Diaper}) = 0.70. What are its confidence and lift?"
  options={[
    'confidence = 0.80, lift ≈ 1.14',
    'confidence = 0.57, lift ≈ 0.95',
    'confidence = 0.40, lift = 1.00',
    'confidence = 0.70, lift ≈ 1.75'
  ]}
  correct={0}
/>

<MultiSelectQuiz
  question="Which statements about evaluating rules with lift, leverage, and conviction are correct? (Select all that apply.)"
  options={[
    'Leverage = support(A ∪ B) − support(A)·support(B), and equals 0 under independence',
    'Conviction is directional, so it can distinguish A ⇒ B from B ⇒ A',
    'Ranking rules by confidence alone tends to favor rules with very common consequents',
    'Because lift is symmetric, it alone tells you the direction of a rule',
    'Conviction approaches infinity for a rule that is never violated'
  ]}
  correct={[0, 1, 2, 4]}
/>

<QuizCard
  question="What is the main reason to prefer FP-Growth over Apriori on large, dense datasets?"
  options={[
    'FP-Growth discovers frequent itemsets that Apriori cannot find',
    'FP-Growth avoids candidate generation and needs only two database scans, so it is typically much faster',
    'FP-Growth removes the need to choose a min_support threshold',
    'FP-Growth guarantees higher-confidence rules'
  ]}
  correct={1}
/>
