# Track Quiz — Pandas EDA

import { ConceptBox, Bridge, QuizCard, MultiSelectQuiz } from '../../components/learning/index.js'

<ConceptBox title="End-of-track checkpoint">
This quiz covers all nine modules of the Pandas EDA track — from first-look inspection and missing-value handling, through outlier detection, datetime engineering, and visualization, all the way to the full capstone workflow. Work through each question carefully; some require a quick calculation.
</ConceptBox>

## Questions

<QuizCard
  question="You call `df.info()` on a DataFrame and notice that a column listed as `object` dtype has entries that look like numbers. What is the most likely cause and the correct fix?"
  options={[
    "The column contains integers stored as floats; use `df[col].astype(int)`",
    "The column contains mixed types or non-numeric strings, preventing automatic numeric inference; use `pd.to_numeric(df[col], errors='coerce')`",
    "Pandas always stores numeric columns as object by default; use `df.convert_dtypes()`",
    "`df.info()` is unreliable for dtype inspection; use `df.dtypes` instead"
  ]}
  correct={1}
/>

<QuizCard
  question="A taxi dataset has 5 000 rows. The column `payment_type` contains 850 NaN values. What is the missing-value ratio, to one decimal place?"
  options={[
    "8.5 %",
    "17.0 %",
    "1.7 %",
    "0.85 %"
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="Which of the following are valid reasons to use `fillna()` with the **median** instead of the mean when imputing a numeric column? (Select all that apply.)"
  options={[
    "The column has a heavily right-skewed distribution",
    "The column contains extreme outliers that inflate the mean",
    "The column stores categorical codes encoded as integers",
    "The column has fewer than 5 % missing values and is roughly symmetric",
    "You want imputed values to be unaffected by very large or very small observations"
  ]}
  correct={[0, 1, 4]}
/>

<QuizCard
  question="For a fare-amount column, Q1 = 12 and Q3 = 28. Using Tukey fences (factor 1.5), what are the lower and upper outlier thresholds?"
  options={[
    "Lower = −12, Upper = 52",
    "Lower = −4, Upper = 52",
    "Lower = −12, Upper = 44",
    "Lower = −4, Upper = 44"
  ]}
  correct={0}
/>

<QuizCard
  question="After parsing timestamps with `pd.to_datetime`, you compute `df['duration_min'] = (df['dropoff'] - df['pickup']).dt.total_seconds() / 60`. A row shows `duration_min = −3.2`. What should you do?"
  options={[
    "Replace it with 0, because negative durations are physically impossible",
    "Flag it with a boolean mask and remove or investigate the row, because pickup and dropoff may be swapped or corrupted",
    "Take the absolute value, because sign errors are common in raw data",
    "Forward-fill using the previous row's duration"
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="Which of the following business-rule filters are appropriate data-quality checks for a NYC taxi dataset? (Select all that apply.)"
  options={[
    "`df[df['fare_amount'] > 0]` — remove non-positive fares",
    "`df[df['trip_distance'] >= 0]` — remove negative distances",
    "`df[df['passenger_count'] <= 6]` — cap at legal vehicle capacity",
    "`df[df['fare_amount'] < 500]` — drop implausibly high fares before any IQR analysis",
    "`df[df['tip_amount'] == 0]` — keep only cash-paying passengers for fare analysis"
  ]}
  correct={[0, 1, 2]}
/>

<QuizCard
  question="A histogram of `fare_amount` has a long tail stretching to the right, with most values clustered near the left. Which term best describes this distribution shape, and what does it imply about the mean vs. median?"
  options={[
    "Left-skewed; mean is less than the median",
    "Right-skewed; mean is greater than the median",
    "Right-skewed; mean is less than the median",
    "Bimodal; mean and median are approximately equal"
  ]}
  correct={1}
/>

<QuizCard
  question="You run `df.groupby('hour')['fare_amount'].agg(['mean', 'count'])` and see that the mean fare at hour 3 is very high but the count is only 12. What is the most appropriate next step?"
  options={[
    "Report the 3 AM mean as a reliable insight since groupby is accurate",
    "Treat the 3 AM mean with caution — small sample sizes make aggregated statistics unstable and potentially misleading",
    "Remove all 3 AM rows from the dataset before further analysis",
    "Replace the 3 AM mean with the overall mean to reduce noise"
  ]}
  correct={1}
/>

<MultiSelectQuiz
  question="The Pearson correlation between `trip_distance` and `fare_amount` is 0.87. Which statements are correct interpretations? (Select all that apply.)"
  options={[
    "There is a strong positive linear relationship between trip distance and fare amount",
    "Longer trips cause higher fares — the correlation proves causation",
    "About 75.7 % of the variance in fare_amount is explained by trip_distance (R² ≈ 0.87²)",
    "The relationship is stronger than if the correlation were 0.60",
    "A correlation of 0.87 guarantees the relationship is linear with no curvature"
  ]}
  correct={[0, 2, 3]}
/>

<QuizCard
  question="You create a pivot table: `df.pivot_table(values='fare_amount', index='day_of_week', columns='hour', aggfunc='mean')`. A heatmap of the result shows very high fares in the top-right corner. What does the top-right corner represent?"
  options={[
    "Low day-of-week index combined with early morning hours",
    "High day-of-week index combined with late evening hours — e.g., Saturday/Sunday late night",
    "Weekday morning rush-hour trips",
    "Missing values that were imputed with the column maximum"
  ]}
  correct={1}
/>

<QuizCard
  question="During a capstone EDA workflow, at which stage is it most appropriate to revisit and re-inspect the data?"
  options={[
    "Only at the very beginning, before any transformations",
    "After every single transformation step, regardless of its significance",
    "After major transformations — such as outlier removal or feature engineering — that substantially change the distribution",
    "Only when the final model performs poorly"
  ]}
  correct={2}
/>

<MultiSelectQuiz
  question="Which of the following describe properties of a well-designed EDA workflow? (Select all that apply.)"
  options={[
    "Transformations are idempotent — running the same cleaning step twice produces the same result as running it once",
    "Each analytical decision is documented so the analysis can be reproduced",
    "Outlier removal is always the first step, before missing-value handling",
    "The order of steps matters: inspect → clean → transform → visualize → summarize",
    "A boxplot's whiskers always extend to the minimum and maximum values in the dataset"
  ]}
  correct={[0, 1, 3]}
/>

<QuizCard
  question="A boxplot of `trip_distance` shows the median line at 2.1, the box from 1.0 to 4.5, whiskers reaching to 0.1 and 9.8, and several points beyond 9.8. What does the IQR equal, and what do the points beyond the upper whisker represent?"
  options={[
    "IQR = 3.5; the points beyond 9.8 are data errors that must be deleted",
    "IQR = 3.5; the points beyond 9.8 are potential outliers flagged by the Tukey fence (Q3 + 1.5 × IQR = 4.5 + 5.25 = 9.75)",
    "IQR = 2.1; the points beyond 9.8 are the upper quartile values",
    "IQR = 5.3; the whiskers represent the 5th and 95th percentiles"
  ]}
  correct={1}
/>
