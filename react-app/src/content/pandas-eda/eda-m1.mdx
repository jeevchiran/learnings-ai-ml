import { ConceptBox, Bridge, PredictReveal, DerivationSteps, DerivationStep, QuizCard, Recall } from '../../components/learning/index.js'

# EDA Foundations & First Look

<ConceptBox title="Before you start">
You need basic Python — lists, dictionaries, calling a method on an object — and nothing else. No statistics is assumed; the few summary numbers that appear are explained where they are used. This is the track everything else in the curriculum starts from, because every model begins with a table someone had to understand first.
</ConceptBox>

## Why This Topic?

You can't clean what you don't understand. Before dropping a single missing value or capping a single outlier, every analyst starts in the same place: looking at the raw shape of the data. Skip this step and every later decision — which column to fix, which fill strategy to use, which rows to drop — is a guess instead of a judgment call.

Use the supplied **synthetic taxi practice dataset: 30 rows and 12 columns**. It deliberately includes missing values, zero-distance trips, negative amounts and one unusually large fare. It is teaching data, not a real NYC trip extract or a source of current fare rules. Worked questions later in the track state their own inputs; compute actual outputs when running the practice data.

<ConceptBox title="Before running the Python examples">
Install `pandas`, `matplotlib` and `seaborn` in your notebook environment. <a href={`${import.meta.env.BASE_URL}data/taxi_practice.csv`} download>Download taxi_practice.csv</a> and save it in the same folder as your notebook. Run the loading cell below first; later examples reuse the variable `df`. Restarting the notebook clears that variable, so rerun the earlier cells after a restart.
</ConceptBox>

<ConceptBox title="The dataset, end to end">
Each row represents one synthetic trip. Columns cover `id`, `tpep_pickup_datetime`, `tpep_dropoff_datetime`, trip facts (`passenger_count`, `trip_distance`, `RatecodeID`, `payment_type`), money (`fare_amount`, `tip_amount`, `airport_fee`), and geography (`PUBorough`, `DOBorough`). Keep these column names unchanged when following the code.
</ConceptBox>

<Bridge>
Before touching any of that, we need the five methods every pandas session opens with: `.shape`, `.dtypes`, `.head()`, `.info()`, and `.describe()`.
</Bridge>

## What / How — First-Look Methods

<PredictReveal
  prompt="You load a CSV you expect to have 30 rows, and the DataFrame reports 30 rows and 12 columns. Are you ready to start analysing?"
  options={["Yes, the shape is what you expected", "Not quite — the column types matter as much as the count, and a numeric column read as text will break everything downstream"]}
  correct={1}
>

Every pandas exploratory session starts with the same handful of calls. Each answers a different question about the DataFrame, and together they form a quick "vitals check" before any cleaning begins.

</PredictReveal>

<Recall items={[
  { q: "Why is checking column types as important as checking the row count?", a: "A numeric column silently loaded as text will not aggregate, will not plot, and will sort alphabetically, all without raising an error. The type report is the first place a malformed value, a stray currency symbol or a wrongly parsed date announces itself." },
]} />

<DerivationSteps>
<DerivationStep number={1} title=".shape — how big is this?">
Returns a tuple `(n_rows, n_columns)`. The very first thing to check: does the row count match what you expected to load? Did a join silently multiply or drop rows?
</DerivationStep>
<DerivationStep number={2} title=".dtypes — what type is each column?">
Returns the storage type pandas inferred for every column: `int64`, `float64`, `object` (usually strings), `datetime64`, and so on. A column you expect to be numeric but that shows up as `object` usually means it contains stray text or mixed formatting.
</DerivationStep>
<DerivationStep number={3} title=".head(n) — what does a row actually look like?">
Prints the first `n` rows (default 5). Numbers and dtypes are abstract; seeing real values grounds your intuition for what "normal" looks like in this dataset.
</DerivationStep>
<DerivationStep number={4} title=".info() — dtypes and completeness together">
Combines `.dtypes` with a non-null count per column and total memory usage. This is the fastest way to spot missing data: if a column's non-null count is less than the row count, it has nulls.
</DerivationStep>
<DerivationStep number={5} title=".describe() — summary statistics">
For numeric columns: count, mean, std, min, the 25/50/75% quantiles, and max. This is where shape problems (skew) and correctness problems (impossible values) first become visible — often before you've looked at a single row.
</DerivationStep>
</DerivationSteps>

Run the first check — confirm the dataset is the size we expect:

```python
import pandas as pd
df = pd.read_csv("taxi_practice.csv")
print(df.shape)
```

```
(30, 12)
```

30 rows confirms we have the expected sample; 12 columns confirms no columns were dropped or merged unexpectedly during loading. Now look at one numeric column's summary statistics in detail:

```python
print(df.describe()[['fare_amount']])
```

```
       fare_amount
count        30.00
mean          26.17
std           71.52
min          -12.50
25%            6.125
50%           11.75
75%           19.125
max          399.00
```

**The mean is more than double the median.** Mean fare (26.17) is more than double the median fare (11.75). When mean and median diverge this much, it's a strong hint the column is **right-skewed** — a few extreme values are pulling the average up while the median, which only cares about the middle-ranked value, stays low. Flag this now; Module 4 (Outlier Detection) explains exactly which rows are responsible and why.

**`min` is negative.** The minimum fare is **-12.50**. A taxi fare cannot be negative — this isn't a distribution quirk like skew, it's a data error (likely a refund or trip-correction record that leaked into the fare column). Module 3 (Data Quality & Invalid Values) deals with rows like this directly.

<Bridge>
Try `df.head()`, `df.dtypes` and `df.info()` on the loaded table. Before each call, say what question it answers. Then identify one column with missing values and one value worth investigating. The next module explains how to handle missing data.
</Bridge>

---

<ConceptBox title="TL;DR">
- Five calls open every pandas session: `.shape`, `.dtypes`, `.head()`, `.info()`, `.describe()`.
- **`.shape` first** — a row count that doesn't match expectations means a join silently duplicated or dropped rows.
- A numeric column showing up as `object` in `.dtypes` means stray text or mixed formatting is hiding in it.
- `.info()` is the fastest null detector: any non-null count below the row count means missing data.
- **`.describe()` computes happily on dirty data.** Read its min and max as a bug report — that's where the USD -12.50 fare and the USD 399 fare surface.
</ConceptBox>

## Practice

### Problem 1: Reading `.shape`

`df.shape` returns `(30, 12)`. What do the two numbers mean?

**Solution:** **30** rows — one per taxi trip in the sample. **12** columns — one per recorded field per trip (timestamps, fare amounts, passenger count, borough, and so on). `.shape` always returns `(n_rows, n_columns)` in that order, matching NumPy's array-shape convention that pandas builds on.

### Problem 2: Mean vs. Median Gap

Given mean = 26.17 and median = 11.75 for `fare_amount`, what does the gap suggest about the distribution's shape, and which row do you suspect is driving it?

**Solution:** The distribution is **right-skewed**: a few very large fares pull the mean well above the median, while the median — being robust to outliers, since it only depends on rank order — stays close to where most of the data sits. The dataset's **max (399.00)** is the prime suspect. One USD 399 fare sitting among mostly USD 3–USD 30 fares pulls the mean up substantially, while the median barely moves because it only looks at the middle-ranked value.

### Problem 3: Why Is `RatecodeID` a Float?

`.info()` reports `RatecodeID` as `float64`, even though rate codes are small whole numbers (1, 2, 3...). Why would pandas store whole numbers as floats?

**Solution:** The column has **missing values** (`NaN`), and NumPy's `int64` dtype has no representation for `NaN` — only floating-point dtypes can hold it (`NaN` is an IEEE-754 float concept). When pandas builds a column that mixes whole numbers with missing values, it silently **upcasts** the entire column to `float64` so the nulls have somewhere to live.

```python
print(pd.array([1, 2, None]))
```

```
<IntegerArray>
[1, 2, <NA>]
Length: 3, dtype: Int64
```

Even pandas's own nullable `Int64` extension type (capital "I") needs a special sentinel `<NA>` to do this — the plain NumPy-backed `int64` (lowercase "i") that `read_parquet` defaults to cannot represent missingness at all, so it falls back to `float64` and uses `NaN` instead.

## Quiz

### Module 1 — Check Your Understanding

**1. What does `df.shape` return?**
- A: (rows, columns) ✓
- B: (columns, rows)
- C: The total cell count
- D: The column names

**2. Which single method shows dtypes *and* non-null counts together?**
- A: .describe()
- B: .head()
- C: .info() ✓
- D: .shape

**3. By default, when a DataFrame mixes numeric and text columns, `.describe()` summarizes:**
- A: All columns, numeric and text alike
- B: Only the numeric columns ✓
- C: Only the text columns
- D: Only the index

**4. Why is `fare_amount`'s mean (26.17) so much higher than its median (11.75) in this sample?**
- A: The sample size (30 rows) is too small for mean and median to agree
- B: A few large outlier fares pull the mean up; the median is robust to outliers ✓
- C: pandas rounds the median down by convention
- D: Mean and median are always different for floating-point columns

<Bridge>
**What's next: how much data is actually here?** `.describe()` already flagged something off — a USD 399 fare and a negative one. Before trusting any of these numbers, find out how much of the data is even there to compute on. Module 2 starts with missing values.
</Bridge>

<Recall items={[
  { q: "You load a CSV and .shape says 4.2 million rows when you expected 900,000. What happened?", a: "Almost certainly a join that fanned out — a many-to-many merge where the key was not unique on one side multiplies rows. Checking shape immediately after loading or merging is the cheapest way to catch it, before any statistic computed on the duplicated rows misleads you." },
  { q: "fare_amount shows dtype object instead of float64. What does that imply and what breaks?", a: "The column contains something non-numeric — currency symbols, thousands separators, an 'N/A' string, or mixed types. Arithmetic and .describe() will either fail or silently skip the column, and any model will treat it as categorical. It needs cleaning and an explicit conversion before use." },
  { q: "Which single call tells you both the dtype and the completeness of every column?", a: ".info(), which combines dtypes with a non-null count per column plus memory usage. Any column whose non-null count is below the total row count has missing values — making it the fastest first pass at spotting nulls." },
  { q: "Why is .describe() dangerous if you only read the mean?", a: "Because it computes on whatever is there, ignoring NaNs silently and including invalid values happily. The mean is the number most distorted by both. The min and max rows are where the real information is: a negative fare and a USD 399 fare are visible there and invisible in the mean." },
]} />

<QuizCard
  question="What does df.shape return?"
  options={['A list of column names', 'A tuple (rows, columns)', 'The data types of each column', 'Summary statistics']}
  correct={1}
/>
