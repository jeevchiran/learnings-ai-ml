import { ConceptBox, Bridge, PredictReveal, DerivationSteps, DerivationStep, QuizCard, Recall } from '../../components/learning/index.js'
import BayesTheoremWidget from '../../components/widgets/naive-bayes/BayesTheoremWidget.jsx'

# Module 2: Deriving Bayes' Theorem from Conditional Probability

## Conditional Probability

The probability of $A$ given $B$ occurred is *defined* as:

$$P(A \mid B) = \frac{P(A \cap B)}{P(B)}, \qquad P(B) > 0$$

Restrict attention to the world where $B$ is true, then ask what fraction of it also has $A$ true. Rearranging gives the **multiplication rule** $P(A \cap B) = P(A \mid B)\,P(B)$; swapping roles gives $P(A \cap B) = P(B \mid A)\,P(A)$. Both equal the same joint probability — that is the entire trick behind Bayes' theorem.

## Deriving Bayes' Theorem

<PredictReveal
  prompt={"You can easily measure how often a spam email contains the word free. What you actually want is how often an email containing free is spam. What converts one into the other?"}
  options={["Nothing, they are the same quantity", "Bayes' theorem, which flips the condition using the base rate of spam"]}
  correct={1}
>

<DerivationSteps>
<DerivationStep number={1} title="Two expressions for the joint">
$$P(A \cap B) = P(A \mid B)\, P(B) \qquad\text{and}\qquad P(A \cap B) = P(B \mid A)\, P(A)$$
</DerivationStep>
<DerivationStep number={2} title="Set them equal">
$$P(A \mid B)\, P(B) = P(B \mid A)\, P(A)$$
</DerivationStep>
<DerivationStep number={3} title="Solve for P(A | B)">
$$P(A \mid B) = \frac{P(B \mid A)\, P(A)}{P(B)}$$
It flips a conditional: knowing how likely evidence $B$ is *given* cause $A$, you recover how likely cause $A$ is *given* evidence $B$.
</DerivationStep>
</DerivationSteps>

<ConceptBox title="The four terms">
- **Posterior** $P(A \mid B)$ — updated belief after seeing $B$.
- **Likelihood** $P(B \mid A)$ — how probable the evidence is if $A$ were true.
- **Prior** $P(A)$ — belief before any evidence.
- **Evidence** $P(B)$ — total probability of $B$; a normalizing constant.
</ConceptBox>

</PredictReveal>

<Recall items={[
  { q: "Which direction of conditional probability is easy to measure, and which do you actually want?", a: "Measuring the evidence given the class is easy, because you can count word frequencies inside each labelled group. What you want is the class given the evidence, for a new unlabelled item. Bayes' theorem converts one into the other by bringing in the class's base rate." },
]} />

## The Evidence Term

$P(B)$ is rarely known directly — build it from the pieces you have. With $A$ and $\bar A$ partitioning all cases, the **law of total probability** gives $P(B) = P(B \mid A)P(A) + P(B \mid \bar A)P(\bar A)$, or for $K$ classes:

$$P(A_i \mid B) = \frac{P(B \mid A_i)\, P(A_i)}{\displaystyle\sum_{k=1}^{K} P(B \mid A_k)\, P(A_k)}$$

The denominator sums over *every* class, so it doesn't depend on which $A_i$ you score — a shared constant. That becomes the key shortcut in the next module.

## Worked Example: The Medical-Test Paradox

<PredictReveal
  prompt="A test detects 99% of cases of a disease affecting 1% of people, and wrongly flags 5% of healthy people. Among positive results, roughly what fraction are real cases?"
  options={["About 99%, matching sensitivity", "About 17%, because false positives from the much larger healthy group outnumber true positives"]}
  correct={1}
>

Disease prevalence $P(D) = 0.01$. Test detects disease 99% of the time ($P(\text{pos}\mid D) = 0.99$), false-positives 5% of healthy people ($P(\text{pos}\mid \bar D) = 0.05$). A patient tests positive — what is $P(D \mid \text{pos})$?

<DerivationSteps>
<DerivationStep number={1} title="Evidence P(pos)">
$$P(\text{pos}) = (0.99)(0.01) + (0.05)(0.99) = 0.0099 + 0.0495 = 0.0594$$
</DerivationStep>
<DerivationStep number={2} title="Apply Bayes' theorem">
$$P(D \mid \text{pos}) = \frac{0.0099}{0.0594} \approx 0.167$$
</DerivationStep>
</DerivationSteps>

In this hypothetical population, **99% sensitivity is not 99% positive predictive value**. Imagine 10,000 people: 100 have the disease, giving about 99 true positives; 9,900 do not, giving about 495 false positives. Only $99/(99+495)\approx16.7\%$ of positive results are real cases. This is a teaching example, not an interpretation of an individual's medical test.

<BayesTheoremWidget />

Drag the prevalence slider down and watch the posterior collapse while sensitivity and the false-positive rate stay fixed. The classifier's prior term does exactly this job, which is why a rare class is not predicted without strong evidence.

</PredictReveal>

<Recall items={[
  { q: "Why does a highly accurate test still produce mostly false positives for a rare condition?", a: "The false positives come from the healthy group, which is vastly larger. In this example, 5% of 9,900 healthy people gives 495 false positives, while 99% of 100 affected people gives 99 true positives. The base rate, not the test's accuracy, dominates the answer." },
]} />

<Bridge>
Bayes' theorem here relates one event $A$ to one evidence $B$. Classification relates a class $y$ to a whole feature vector $\mathbf{x} = (x_1,\ldots,x_n)$, giving $P(y \mid \mathbf{x}) \propto P(\mathbf{x} \mid y)\,P(y)$ — but $P(\mathbf{x}\mid y)$ over many correlated features is intractable to estimate. The next module introduces the one assumption — conditional independence — that makes it tractable, and names the classifier.
</Bridge>

---

<ConceptBox title="TL;DR">
- The whole derivation is one move: **the joint can be written two ways**, $P(A\mid B)P(B)=P(B\mid A)P(A)$. Solve for what you want.
- Four terms worth naming: posterior, likelihood, prior, evidence. The **evidence is just a normaliser** built by the law of total probability.
- Because the denominator sums over every class, it's the same for all of them — which is why it can be dropped when you only need the arg-max.
- **The medical-test paradox**: a test with 99% sensitivity and a 5% false-positive rate on a 1%-prevalence disease gives a positive patient only ~17% chance of being ill. Rare disease ⟹ false positives outnumber true ones.
</ConceptBox>

## Practice

#### Problem 1: Spam Filter Update
$P(\text{spam}) = 0.05$. "free" appears in 40% of spam, 2% of legit. An email contains "free". Find $P(\text{spam} \mid \text{"free"})$.

**Solution:**
$$P(\text{"free"}) = (0.40)(0.05) + (0.02)(0.95) = 0.039$$
$$P(\text{spam} \mid \text{"free"}) = \frac{0.02}{0.039} \approx 0.513$$
One word roughly doubles a 5% prior into 51.3% — a big update, still far from certainty.

#### Problem 2: Symmetry Check
Show $P(A\mid B)P(B) = P(B\mid A)P(A)$ implies $P(A \mid B)/P(B\mid A) = P(A)/P(B)$.

**Solution:** Divide both sides by $P(B\mid A)P(B)$: $\frac{P(A\mid B)}{P(B\mid A)} = \frac{P(A)}{P(B)}$. The relative pull of $A$ vs $B$ as the "given" mirrors how common $A$ is versus $B$ to begin with.

<Recall items={[
  { q: "Derive Bayes' theorem from scratch in two lines.", a: "The joint P(A and B) equals P(A|B)P(B) and also equals P(B|A)P(A), since conditioning either way describes the same intersection. Set them equal and divide by P(B) to get P(A|B) = P(B|A)P(A)/P(B)." },
  { q: "Why is a 99%-accurate test on a rare disease still mostly false alarms?", a: "Because the 1% false-positive rate applies to the 99% of people who are healthy, producing about 5 false positives per 100 people, while the 99% detection rate applies to only the 1% who are ill, producing about 1 true positive. The healthy pool is so much larger that its small error rate dominates." },
  { q: "Where does the denominator P(B) come from when you cannot measure it directly?", a: "The law of total probability: sum P(B|A_k)P(A_k) over every class A_k. The evidence must have arisen through one of the classes, so summing all the routes to it reconstructs its total probability." },
  { q: "Explain why classification can ignore the evidence term but calibration cannot.", a: "The denominator is identical for every class, so it scales all scores by one constant and cannot change which is largest — the arg-max is unaffected. But it IS what makes the numbers sum to 1, so without it your 'probabilities' are unnormalised scores and cannot be reported or thresholded meaningfully." },
  { q: "P(spam) = 0.05; 'free' appears in 40% of spam and 2% of legit mail. Estimate the posterior.", a: "Evidence = 0.40*0.05 + 0.02*0.95 = 0.02 + 0.019 = 0.039. Posterior = 0.02/0.039 = about 0.513. One word takes a 5% prior to roughly 51% — a large update, and still a coin flip, which is why real filters use many words." },
]} />

<QuizCard
  question="In Bayes' theorem P(A|B) = P(B|A)P(A) / P(B), what role does the denominator P(B) play?"
  options={[
    'It is the prior probability of A',
    'It is a normalizing constant (the total probability of the evidence, summed over all classes)',
    'It is the likelihood of B given A',
    'It has no effect on the posterior and can always be dropped'
  ]}
  correct={1}
/>
