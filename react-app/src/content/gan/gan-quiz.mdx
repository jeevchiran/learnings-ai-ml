# Track Quiz — Generative Adversarial Networks (GANs)

import { ConceptBox, QuizCard, MultiSelectQuiz } from '../../components/learning/index.js'

<ConceptBox title="End-of-track checkpoint">
Sixteen questions plus three multi-selects spanning the complete GAN curriculum: implicit vs. explicit density, the minimax game and its Nash equilibrium, vanishing gradients and the non-saturating fix, mode collapse, DCGAN's architectural guidelines, Wasserstein distance and the Lipschitz constraint, conditional GANs, CycleGAN's unpaired translation, and Inception Score vs. FID. Work through the reasoning before submitting your answers!
</ConceptBox>

## Single-Choice Questions

<QuizCard
  question="What makes a GAN an 'implicit' density model, in contrast to a VAE?"
  options={[
    "It can sample from a data distribution without ever writing down a formula for p(x)",
    "It computes an exact closed-form likelihood, unlike a VAE's approximate bound",
    "It has no generator network, only a discriminator",
    "It requires labeled data for every training example"
  ]}
  correct={0}
/>

<QuizCard
  question="Why does minimizing average pixel-wise reconstruction error tend to produce blurry outputs on ambiguous inputs, while an adversarial loss does not?"
  options={[
    "Pixel-wise loss is minimized by the mean of plausible outputs, which averages away detail; adversarial loss instead rewards plausibility, not closeness to an average",
    "Pixel-wise loss requires more GPU memory, which forces lower image resolution",
    "Adversarial loss is mathematically identical to pixel-wise loss, just computed differently",
    "Blur is caused by using too small a batch size, unrelated to the choice of loss"
  ]}
  correct={0}
/>

<QuizCard
  question="At the theoretical Nash equilibrium of the GAN minimax game, what does the optimal discriminator D*(x) equal, for every input?"
  options={[
    "1 for real inputs and 0 for generated inputs, permanently",
    "1/2 everywhere, because p_g has come to exactly match p_data",
    "It oscillates without ever settling to a fixed value",
    "It equals the generator's current loss value"
  ]}
  correct={1}
/>

<QuizCard
  question="GAN training is often described as searching for a saddle point of V(D, G) rather than a minimum. What does that mean?"
  options={[
    "V(D, G) is maximized over D's parameters and minimized over G's parameters at the same time",
    "V(D, G) has two separate global minima that the training must choose between",
    "The saddle point is found analytically before training begins",
    "It means training never modifies G's parameters, only D's"
  ]}
  correct={0}
/>

<QuizCard
  question="Why does the original minimax loss log(1 - D(G(z))) give the generator a weak gradient early in training?"
  options={[
    "Because the function is nearly flat when D(G(z)) is close to 0, which is exactly where an untrained generator starts",
    "Because the discriminator's weights are frozen for the first several epochs by convention",
    "Because early training always uses a learning rate of exactly zero",
    "Because the loss is undefined when D(G(z)) is small"
  ]}
  correct={0}
/>

<QuizCard
  question="What does the non-saturating generator loss change compared to the original minimax formulation, and what stays the same?"
  options={[
    "It changes the fixed point of the game entirely, targeting a different equilibrium",
    "It keeps the same fixed point and direction of improvement, but supplies a much steeper gradient when D(G(z)) is close to 0",
    "It removes the discriminator from the training loop",
    "It replaces the generator with a second discriminator"
  ]}
  correct={1}
/>

<QuizCard
  question="Why can a standard, per-sample discriminator fail to detect that a generator has collapsed onto a single mode?"
  options={[
    "Because it scores each generated sample independently, and a collapsed generator's individual samples can still look realistic",
    "Because mode collapse only happens after training has already finished",
    "Because the discriminator is retrained from scratch every epoch",
    "Because collapsed samples are always visually distorted and easy to spot"
  ]}
  correct={0}
/>

<QuizCard
  question="What does minibatch discrimination give the discriminator access to that fixes the mode-collapse blind spot?"
  options={[
    "A summary of how similar the samples within a minibatch are to each other, so lack of diversity becomes directly detectable",
    "The generator's internal weights, copied directly into the discriminator",
    "A pretrained ImageNet classifier's opinion of each sample",
    "The exact value of z used to generate each sample"
  ]}
  correct={0}
/>

<QuizCard
  question="Why does DCGAN replace pooling layers with strided and transposed convolutions?"
  options={[
    "Pooling and unpooling are fixed, non-learned resizing rules; strided/transposed convolutions let the network learn its own spatial resizing",
    "Pooling layers are incompatible with GPU acceleration",
    "It reduces the total number of layers required in both networks",
    "It removes the need for a loss function entirely"
  ]}
  correct={0}
/>

<QuizCard
  question="Why does DCGAN skip BatchNorm specifically on the generator's output layer and the discriminator's input layer?"
  options={[
    "Those two layers touch real pixel values directly, and normalizing them would distort the desired output range or the true input statistics",
    "BatchNorm cannot mathematically be applied to convolutional layers",
    "Those layers are removed entirely in DCGAN, so there is nothing to normalize",
    "It is purely a computational speed optimization with no effect on training quality"
  ]}
  correct={0}
/>

<QuizCard
  question="Why does the original GAN loss's underlying Jensen-Shannon divergence give little useful gradient when p_data and p_g have little overlapping support?"
  options={[
    "JS divergence is flat or saturates in that regime, providing weak gradient regardless of which generator loss variant is used",
    "JS divergence only applies to discrete distributions, not continuous image data",
    "The discriminator cannot be trained at all when supports are disjoint",
    "Disjoint supports never actually occur in practice with image data"
  ]}
  correct={0}
/>

<QuizCard
  question="Why must a WGAN's critic satisfy a 1-Lipschitz constraint?"
  options={[
    "Because the dual formulation of Wasserstein distance the critic approximates is only mathematically valid under that constraint",
    "Because 1-Lipschitz networks train faster on any hardware",
    "Because it is required for the critic to output a probability between 0 and 1",
    "It is an arbitrary convention with no mathematical necessity"
  ]}
  correct={0}
/>

<QuizCard
  question="In a conditional GAN, why must the discriminator see the class label y, not just the generator?"
  options={[
    "If only G saw y, D could not penalize G for ignoring the requested class, since D would only ever check general realism",
    "The discriminator needs y purely to compute its own loss function's normalization constant",
    "It is not actually necessary — most conditional GANs only condition the generator",
    "Feeding y to D would make gradients unable to flow back to G at all"
  ]}
  correct={0}
/>

<QuizCard
  question="Why can Inception Score stay respectable even when a generator has partially collapsed onto a handful of modes?"
  options={[
    "Because IS combines per-image confidence with cross-image diversity into one number, and strong per-image confidence can offset reduced diversity",
    "Because Inception Score does not use a neural network at all",
    "Because mode collapse always increases Inception Score by mathematical definition",
    "Because IS is computed only on the first generated image in a batch"
  ]}
  correct={0}
/>

<QuizCard
  question="Why does FID tend to catch mode collapse more reliably than Inception Score?"
  options={[
    "FID compares the full distribution of real vs. generated Inception features directly, so missing modes appear as a distributional gap",
    "FID does not rely on any pretrained network, unlike Inception Score",
    "FID is computed using the discriminator's own weights rather than a separate classifier",
    "FID only evaluates a single generated image at a time, making it more precise"
  ]}
  correct={0}
/>

<QuizCard
  question="Why can't CycleGAN be trained with only a cycle-consistency loss, dropping the adversarial discriminators entirely?"
  options={[
    "Because the identity function (G(x) = x, F(y) = y) trivially minimizes cycle-consistency loss while performing no actual translation",
    "Because cycle-consistency loss cannot be computed without paired ground-truth images",
    "Because a single generator cannot be trained on two domains at once",
    "Because dropping the discriminators would make the cycle loss undefined"
  ]}
  correct={0}
/>

## Multi-Select Questions

<MultiSelectQuiz
  question="Which statements about the GAN minimax game are correct? (Select all that apply)"
  options={[
    "D is trained to maximize the shared value function V(D, G); G is trained to minimize it",
    "At the Nash equilibrium, p_g equals p_data and D(x) = 1/2 for every input",
    "GAN training searches for a saddle point of V(D, G), not a single global minimum",
    "GAN training is guaranteed to converge the same way gradient descent on a convex loss does"
  ]}
  correct={[0, 1, 2]}
/>

<MultiSelectQuiz
  question="Which statements about mode collapse and DCGAN are correct? (Select all that apply)"
  options={[
    "Mode collapse means the generator produces near-identical outputs regardless of the sampled z",
    "DCGAN replaces pooling with strided convolutions in D and transposed convolutions in G",
    "DCGAN applies BatchNorm to every single layer of both networks with no exceptions",
    "Minibatch discrimination gives the discriminator access to batch-level diversity statistics",
    "Mode collapse is mathematically impossible once the non-saturating loss from Module 3 is used"
  ]}
  correct={[0, 1, 3]}
/>

<MultiSelectQuiz
  question="Which statements about Wasserstein GANs, conditional GANs, CycleGAN, and GAN evaluation are correct? (Select all that apply)"
  options={[
    "Wasserstein distance can stay smooth and informative even when two distributions have disjoint support",
    "A conditional GAN's discriminator D(x, y) judges both realism and whether x matches the requested class y",
    "WGAN-GP enforces the Lipschitz constraint with a gradient penalty rather than hard weight clipping",
    "Higher Inception Score and higher FID both indicate a better-performing generator",
    "DCGAN, WGAN, conditioning, and CycleGAN's unpaired setup are independent design choices that can be freely combined",
    "CycleGAN requires paired before/after images of the same scene, exactly like pix2pix"
  ]}
  correct={[0, 1, 2, 4]}
/>
